# Boiler Room — CLAUDE.md (read this first, every session)

Boiler Room is the construction day-log PWA for **Eric Clore, Clore Construction, Kenai Peninsula, Alaska**. Eric dictates messages and reads on a phone — keep replies **short, plain, no jargon**. He is the owner and only decision-maker; Logan is the bookkeeper, Shevaun handles C&C properties paperwork, Phil/Kevin/Jason/Elodie/Milo are crew.

## What this repo is

- **The repo root IS the live site.** Netlify auto-deploys on every push to `main` at https://cloreconstdailylog.netlify.app
- `index.html` — the entire app: ONE self-contained file (~15,000 lines, HTML+CSS+JS, no build step, no framework). This is deliberate. Keep it one file.
- `c/index.html` — the homeowner (client) page, served at `/c/?c=<code>`; data via `/.netlify/functions/client-portal`
- `netlify/functions/` — `client-portal.mjs`, `notify.mjs` (web-push, VAPID keys in Netlify env), `plan-reminder.mjs` (scheduled 5pm). Functions are pre-BUNDLED single files (esbuild) — bundle locally before committing changes.
- `sw.js` — push service worker. `version.txt` — must match the app version every release.

## Release rules (never skip)

1. Bump `const APP_VER = 'vX.YY'` AND the `<footer>vX.YY` line — both, every build. The app polls `version.txt` and offers "tap to load" when it's newer.
2. Update `version.txt` in the same commit.
3. **Commit author must be `Eric Clore <cloreconstruction@yahoo.com>`** — Netlify's contributor check rejects other authors. Committer may be Claude.
4. **NEVER drag-and-drop files onto Netlify.** A single-file drop replaces the ENTIRE published site (it killed all nine client pages on 2026-08-25). GitHub push is the only deploy route.
5. Test before shipping: Playwright suites named `_testNNN.js` load the file:// page on a 390×844 mobile viewport and assert behavior (see `tests/` if present; earlier suites are in build logs). Run the newest suite plus the last few before any release.

## Hard rules (Eric's, standing, non-negotiable)

- **Personal entries (`personal:true` / a Personal tag) NEVER leave the phone** — not in sync payloads, summaries, crew publishes, or anything client-facing. The lock wins over everything.
- **Markup and vendor names NEVER appear client-side.** Markup folds into client numbers invisibly (`estMk()`), never named. Breakdowns must sum exactly.
- **Per-person wage isolation**: a crew member's files carry ONLY their own rates.
- **No guess-based matching.** Eric rejected AI scoring (v5.26). Prefer tap-to-learn exact-name allow-lists (`prefs.vendCat`, `prefs.jobCats`); pre-light, never auto-commit — his tap decides.
- **Accessibility: nothing depends on colour alone.** Every state = brightness + glyph + spelled word (e.g. "⚠ SURE?", "✓ ON PAGE", "▸ NEXT").
- **Nothing opens in external tabs on the phone** (PWA trap): no target=_blank flows Eric must return from.
- **Bank/loan account numbers: last-four only** in anything summarized or displayed.
- Never put dollar amounts or client names in push-notification text (lock screens).
- Client data files in Dropbox use **create-first replacement**: CREATE `.new` → verify → MOVE old to Backups/Archive → MOVE `.new` live. Never delete in Dropbox; move instead.
- `pending.json` writer contract: writers APPEND unique ids only; the app owns removal.
- **Client page rewrite contract (v6.64) — for the books session and anything else that rewrites `Client Portal/<code>.json`:** READ, MODIFY, WRITE. The books run owns `name`, `stage`, `invoiced`, `paid`, `open`, `openBrk`, `phases`, `updated`, `show.money`, `show.phases` and nothing else. Every other key is the APP's and must be carried over untouched: `budget`, `upcoming`, `journal`, `boardsOff`, the rest of `show` (`budget`, `upcoming`, `journal`, `photos`, `ask`, `boards`, `mat` — Eric's hide switches from the owner view), and any key you do not recognise. Josten's `$15,600` receipt vanished from ESTIMATED UPCOMING COSTS on 2026-09-11 because a rewrite dropped `upcoming`; the app now self-heals that block when Eric opens the estimates board or the receipts window (`estUpcomingStale`), but a dropped `show.boards=false` would put the option boards back in front of a homeowner with no warning — so carry the keys.
- **`Client Portal/paid-<code>.json` contract (v6.35, light ③):** written by the books session from the QuickBooks run, read by the app: `{ "eids": [entry ids], "byId": { "<id>": { "inv": "<QB invoice #>", "paidOn": "YYYY-MM-DD" } } }`. Append only — an id listed here means that receipt was invoiced to the homeowner and paid, and the app drops it from the job's receipts window. Until the file exists the app shows "③ ○ NOT YET — comes from the books". Receipt ids are `entries.json` ids; the amount/vendor to match against is in each client's `estimates-<code>.json` under `cats[].pend[]` (open) and `cleared[]` (paired: `why` = `qb` automatic, `qb-eric` Eric's tap, `eric` = taken back, never paid).
- Never shrink lifetime invoiced figures from a WINDOWED QuickBooks export; a true All-Dates ledger IS lifetime truth and may reduce figures.
- **Park ideas, build on Eric's go.** When Eric muses, give feedback and park it (see `claude/next-up-cards.md` in the claude.ai Project); build only when he says go.

## Where the data lives (NOT in this repo)

All app data syncs to Eric's Dropbox under `/Clore DayLog/` (entries.json, master-log.csv, qb-refresh.json + heartbeat/state, profit-ticker.html, qb-costs.json, Client Portal/*.json + estimates, Crew/<name>/). QuickBooks exports land in `/Clore Project Tracker/QB Exports/`. Sessions without Dropbox access work on the app code only — that's fine; the chat (Cowork) sessions handle data runs (a weekly Tuesday 13:00Z chain refreshes the books by hand).

## Current state (as of 2026-09-07)

- Latest LIVE: **v7.30** (pushed 2026-09-26): 🏦 THE BANK'S OWN CSV, THE OFX, ONE LINE ACROSS BOTH, START FROM ZERO. First, his credit union's site in the built-in browser (read-only, nothing downloaded, no setting touched): the STATEMENTS page gives PDFs only, but the account-history DOWNLOAD page gives OFX / QFX / QIF / QBO / IIF / CSV for any account and dates back three years (the form defaults to the last 30 days) — told to him; its Manage Alerts can email only a deposit, a low balance, a new statement or a bounced check, NO per-purchase alert, so the "daily numbers by email" road is closed at that bank: it is the download or a live-link service. Then Eric, with a picture of TO SORT: "i did the csv and this happened [every line wrong] … i downloaded the ofx, fix this, add the ofx to the budget. make sure future ofx's will work. and maybe csv's too … when i don't remember what a receipt is for maybe i can take it off and start from zero so that i don't have to sort tons of receipts." **The fault:** their CSV has NO heading row and reads `day, the words, an empty cell, the amount`; v7.26's no-heading rule (day · amount · the words in the last cell) read the words as the amount — a 22-digit card reference inside them became a 45-digit dollar figure and the amount became the store. **Now the cells say what they are, whatever their order** (`PB_MONEY_RE`, `pbMoneyCell`): the day is the cell that reads as one, the amount the first cell that reads as MONEY (a signed or decimal figure beats a bare number such as a check number), a second money cell is the balance (the newest line that carries one), the words the longest of the rest; `pbAmtOf` refuses anything over a billion (a heading-row file with such a cell reads no line rather than a wrong one). **The words:** `pbMemoClean` cuts only the bank's own tails and prefixes — `%% Card … MCC …`, the glued `AKDate 09/25/26 PURCH 4029…`, an ACH's `TYPE: … CO: …`, and the leading WITHDRAWAL · DEPOSIT · DEBIT CARD PURCHASE · POS <digits> · ACH · AT ATM (the sign says which way the money went) — a line reads `SUBWAY 12345 SOLDOTNA AK` / `STASH CAPITAL (S` / `ATM #…`; words with none of those stay as written (the CSV and OFX paths; the PDF path is untouched). **The account:** every account at that credit union is ONE member number + a share tag (`…-S70`), so `pbAcctTag` knows such an id by its tag — never the member number; a plain number is still its last four — and `pbAcctShow` draws `S70` / `···1005` everywhere the account is named. **One line across both files:** every line carries `alt` = day | amount | store words | its place among identical lines (older lines get one on the next import); a newcomer whose alt is already here is a dup when it is the same account (`k`) or when both are bank exports (a CSV carries no account id, so CSV↔OFX match blind); a PDF's lines (`t.pdf`) match only within their own account; ⇅ keeps alt true. **🧹 Start from zero** (`pbTxZero`, `.pb-zero`, at the foot of the 🏦 card): two taps (`⚠ SURE? Tap again — all n bank lines come off (Undo after)`, 4 s, then it lets go), Undo brings them all back; the accounts, their balances and the stores he sorted stay. **🤷 Don't remember — take it off** on every wheel (`__off` → `pbTxDel`, Undo, nothing learned). Rehearsed read-only on his two real files (`scratchpad/rehearse-730.js`, figures masked): 110 lines each, every store clean, the OFX after the CSV = 0 new, the CSV after the OFX = 0 new, the member number nowhere in `prefs.pb`. `tests/_test730.js` (16 checks: made-up stores and figures in the bank's shapes). **Asked mid-build — "i did a personal budget study, maybe with chatgpt, is that on here?":** no — not in any session on this PC (searched, archived too), not in the app, not in Dropbox (the v6.84 note: his claude.ai summary lived only in a chat). **Next for him:** 🧹 Start from zero, then drop `EXPORT.OFX` from Downloads; pick OFX on the bank's Download page from now on; Milo's budget question still open.
- Before that: **v7.29** (pushed 2026-09-26): ⏱ THE HOUR TRACKER OFF A CREW PHONE'S PAGE. Eric: "take the hour tracker off phils main page, he uses another hours tracker." `hoursOffSync()` = `body.hours-off` when `isCrew() && !clockedInAt` — thrown in `applyRole` and after every `clockedInAt` assignment (clock in, clock out ×3, a job switch, a restored shift) — and `body.crew-mode.hours-off` hides `.clock-card` (the shift clock), `#weekCard` (Hours this week — the card got its id) and the `⏱ Hours` bar plate; a shift already running on the phone keeps its clock until it is punched out, so nobody is left clocked in with no lever. Eric's phone is untouched; the crew time clock (Phil's own app, v6.99's PPE file) is where their hours live. `tests/_test729.js` (7 checks: Eric's phone, a real reload as Phil, a running shift bringing the clock back).
- Before that: **v7.28** (pushed 2026-09-26): 📄 THE DROP BOX READS A PDF STATEMENT. Eric: "my banks statements are pdf only" … "go, make it read the pdf." `pbStmtFiles` takes `.pdf`: `pbPdfLib()` fetches pdf.js once from cdnjs the way the photo reader (Tesseract) is fetched — `PB_PDFJS` = the CDN path with the version spelled in pieces, because the budget block may hold no figure and `3.11` reads as one — offline it says `📄 The PDF reader needs the internet once to fetch itself`; `pbPdfLines(buf)` = every page's text items gathered into lines by where they sit (y within 3px, left to right); `pbPdfLineParse` = a line that starts with a day (`MM/DD`, `MM/DD/YY(YY)`, `Sep 12`, or a checks-table `1234 09/10 50.00` → `Check 1234`), an optional second day, then the words, then one or two amounts at the end (`PB_PDF_AMT`: `$`, commas, brackets, a trailing minus / CR / DR; two amounts = the transaction and the running balance); a following line with no day and no amount is the rest of the words. **The sign is never guessed when it can be known:** the running balances first (this balance − the last = the amount to the cent, walked in time order — the opening balance seeds it), else the line's own minus / CR / brackets, else the section it sits under (`PB_PDF_IN` Deposits… / `PB_PDF_OUT` Withdrawals · Checks · Purchases…), else `PB_PDF_INWORDS` (deposit · payroll · direct dep · interest · refund · credit → in) and everything else out — those count as `unsure` and the toast adds `📄 check the ins and outs — ⇅ on the account turns them all`. A card's statement (`minimum payment due` · `credit limit` · `new charges` · `card member` · `account ending`) is read in the card's own terms (a charge plus, a payment minus) and flipped by v7.27's `cardFile` rule. `pbPdfEnd` = the latest full date on the pages (the statement's closing day): a bare day takes its year, a month past it is the year before; the account = the last four after "Account Number" / "ending"; the balance = the first `ending|closing|new balance` amount, as of the closing day. Summary lines (`PB_PDF_SKIP`: beginning · ending · total · average · minimum · interest…) are never transactions. A scan (no words) is refused in words. `tests/_test728.js` (12 checks: two made-up statements built as real PDFs with pdf-lib, pdf.js served from `boiler-room-tools/node_modules/pdfjs-dist` in place of the CDN — `npm i -D pdfjs-dist@3.11.174` there — the offline path, the bank's running balances, the two-line words, the checks table, the card's flip and its New Balance, the bare fallback, the refusal, the wall). **Not tested on a real bank PDF** — every bank lays its pages out its own way; when Eric's first real one reads wrong, ask for its lines (numbers changed) and teach the parser that shape.
- Before that: **v7.27** (pushed 2026-09-26): 💳 THE CARD STATEMENT — CHARGES AS PLUS, THE PAYMENT ON BOTH SIDES, EVERY SORT REMEMBERED. Eric, the morning after v7.26: "i added my amex statement, when i pay my amex card how will it know those charges were already accounted for? and my banks statements are pdf only, how do i convert or get this to you easily?" **(1)** A card company's file counts a CHARGE as plus (Amex's export does) — v7.26 read those as money in. Now `pbCsvParse` marks `cardFile` when a heading is one of `PB_CARD_HEADS` (`Card Member` · `Appears On Your Statement As` · `Extended Details` — Amex's own column names, never a guess) and `pbStmtImport` turns the signs (`accts[k].flip = true`, said in the toast `⇅ a card file: charges read as money out`); every ACCOUNTS row wears a `⇅` switch (`pbAcctFlip(k)`: flips every line of that account, remembered for its next file, tap again = back). The account key `k` rides on every line (`pbAcctKey`: the last four, else the file name with `(2)` and digits stripped — "activity (2).csv" is "activity"). **(2)** The payment shows on both sides — money out of the bank, money into the card — and neither is spending: `pbTxPair(t)` finds the same money on another account (same amount, opposite sign, within five days) and the TO SORT row offers `↔ Same money on ···1005 (mm-dd) — both are a transfer` (`.pb-pair`) → `pbTxPairMark`: both lines Transfer, both stores remembered. **(3)** `pbTxCat` now remembers EVERY sort — Income, Transfer and Bill too, money in as well as out — and `pbStmtImport` applies the learned store to every line, so next month's payment lands as Transfer on both sides and payroll as Income. `tests/_test727.js` (10 checks). **The PDF question, answered:** a bank's STATEMENTS page gives PDFs, but its ACTIVITY / TRANSACTIONS page has a Download / Export with a date range — CSV or Quicken (QFX) — which the drop box reads; he was asked which bank so the exact clicks can be given. Offered, on his go: the drop box reading a text PDF statement itself (pdf.js in the browser, the sign from the section headings, the year from the statement period; a scanned PDF would need the OCR road) — NOT built.
- Before that: **v7.26** (pushed 2026-09-25): 🏦 THE BANK STATEMENT DROP ON THE PERSONAL BUDGET. Eric: "id like to expand on the personal budget part and also want to use it for milo to use. the biggest question i have is how could we get it to get statements or better daily updated numbers from a bank account? i think after that the budget app would be easy." Three roads offered — the bank's per-transaction email alerts through the email pipe · a statement drop read in the browser · a live link through a bank-connection service (SimpleFIN Bridge ≈ $1.50/month, or Plaid/Teller) pulled daily by a scheduled function — he answered "2". **Built:** a `🏦 BANK` card on the budget page between the month card and SPEND (`pbBankCardHtml`): a drop box (`.pb-drop`, `#pbStmtIn`, tap or drag; `.csv .txt .tsv .ofx .qfx .qbo`) → `pbStmtFiles` reads the file ON THE PHONE (no AI, nothing sent): `pbCsvParse` (a quoted-CSV reader `pbCsvRows`; the heading row found by name — `PB_CSV_HEADS` date / amount or debit+credit / balance / card no. / type, the words column by `PB_DESC_RANK` (Description > Payee > Merchant > Name > Memo > … > Details — "Details" is a bank's DEBIT/CREDIT word); no heading = day, amount, the words in the last cell; an unsigned amount with a DEBIT type turns negative; the newest day's balance is the account's) or `pbOfxParse` (SGML or XML OFX: `STMTTRN` blocks, `FITID`, `DTPOSTED`, `TRNAMT`, `NAME`/`MEMO`, `ACCTID` cut to the last four, `LEDGERBAL`). `pbStmtImport`: a line's id = the bank's FITID, else day|amount|store words|its place among identical lines in that file — a file dropped twice adds nothing; a future-dated line is skipped; `prefs.pb.tx` (newest first, 3000 kept), `prefs.pb.accts[last4]` = balance + as-of. **Sorting:** a line waits in TO SORT (30 shown) with a wheel (`pbTxWheel`: his categories + `🏠 Bill — already counted` · `↔ Transfer — not spending` · `💵 Income`, Income first on a deposit); `pbTxCat` sets it AND remembers the store by its exact name (`prefs.pb.storeCat[pbStoreKey(desc)]` — lower-cased, digits and #/* stripped) for every unsorted line of the same store now and every one dropped later (tap-to-learn, never a guess); `PB_TX_SPECIAL` never count as spending; the rest join `pbSpendAll()` → SPENT, WHERE IT WENT and the SPEND list (a 🏦 on the row, ✎ = unsort, no ✕); `pbTxDel` with Undo; `💵 $n came in this month` for the Income lines. **The wall:** a long digit run in the words is masked (`pbTxDesc`), an account is its last four only, `prefs.pb` still never reaches the Wizard, the crew or a client; no figure in the code (the `_test684` scan covers the new block). `tests/_test726.js` (16 checks: four bank file shapes built from today's dates, dedupe, the wheel, the learned store, the wall). **Open — Eric's word needed:** Milo's budget (the plate is hidden on crew phones today): his alone (kept on his phone + a passphrase-locked file like the Vault), or does Eric see it too?
- Before that: **v7.25** (pushed 2026-09-25): ⏳ GETTING OLD AND 🚧 STUCK ON THE BUILD LIST — Eric's "yes to 1 and 2" on the v7.24 plan. **⏳ Getting old:** `matWaitsOnUs(it)` = an unfinished row that waits on HIM — to pick, picked-not-ordered, here-not-installed, a checklist row not done, and an ORDERED row only once `it.eta` (expected arrival) has passed (waiting on a vendor is not on him); `matAgeDays(it)` = days since the newest of the 🕘 history's `at`, `gat`, `odate`, `ins`, `ddate`, `seen`; `matSeenStamp()` on open dates a row that carries no date at all with today (`it.seen`, once, then saved) so its age counts from somewhere; `matAgeCubes` = `MAT_AGE_STEPS` [3, 7, 14] → `matAgeHtml`: `⏳ ▪▪▫ 12 days` on the name line right after the row's word (`.mat-age`, three 7px cubes lit in order + the days in words — `n wks` from 21; the label says "waiting on you …"); `matOld` = a week or more → the heading counts `⏳ n getting old` and a working-list chip `⏳ getting old (n)`. **🚧 Stuck:** `it.stuck` + `it.need` — the second mark beside ⚠ FIRST (FIRST = which one first; STUCK = what is missing; a row can wear both): a `🚧 stuck` plate between ⚠ 1st and ⇄ on every unfinished row (`matStuckToggle` — one tap; the 🕘 history writes `🚧 stuck no → yes`); the row wears `.mi-stuck` (a coral `--err` edge) and a band `🚧 STUCK — needs: [box]` (`matStuckBandHtml`, `#matNeed-<id>`, takes the focus on the tap; `matSet(id, 'need')` → history `what it needs`); the edit window has the plate and its own `What has to happen first` box after ⚠ FIRST; the heading counts `🚧 n stuck`, folded it wears a coral `🚧 n STUCK` pill (`.mat-stuck-lit`); a working-list chip `🚧 stuck (n)`; clearing it drops `need` (the history keeps the words); a finished row is never stuck or old. The client cut strips `stuck`, `need`, `seen`. **Eight plates on one phone line:** `.mat-box` 34px on ≤700px (was 37; padding 2px), `.mat-line2` gap 4px, `.mat-rowbtns` margin 2px — asserted at 390px. `? how this works` says both. `tests/_test725.js` (16 checks); `_test698`/`_test708` re-pinned (eight plates; ⚠ · 🚧 · ⇄).
- Before that: **v7.24** (pushed 2026-09-25): 🧹 THE BUILD LIST ROW ON THE PHONE. Eric, with pictures of the board on his phone and PC: "The build list looks really good on PC … But it's getting a little crowded on the phone. See if there's anything you can do to clean it up. Although I like the size of the buttons quite a bit I feel like the main button where the check mark actually goes when the thing is done next to the title I don't feel like that's really used because my eyes are jumping straight to the yellow cards that are lit up or not." His phone picture showed the real fault: the name's small words (`· pick needed · 🏷 Siding · 🏠 client`, `white-space: nowrap`) ran UNDER the 🕘 ✎ ✕ plates. **Now:** the round lamp at the front of the row is gone (`matMgrItem` no longer draws it; `matCycle`/`matLampPlan` stay as functions for the old suites, unreachable from the screen); the small words wrap under the name (`white-space: normal; overflow-wrap: anywhere` — the `.mat-nm` column ends before the plates, asserted at 390 and 1280); the lights start under the name (`.mat-line2` and `.mat-arm-say` lost their 40px indent); `? how this works` no longer speaks of a lamp. `tests/_test724.js` (12 checks, phone and PC); `_test691`/`_test720` re-pinned (no lamp; the checklist row walks by its lights). **His two ideas, answered with a plan and PARKED for his go:** (1) "some kind of timer … like the cube lights for the QuickBooks updater and the journal updater … show that something's getting old. But … if it's ordered and you're just waiting for it to arrive, it won't notify you" → an age mark on rows that wait on US (to pick · picked-not-ordered · here-not-installed; an ordered row only once its expected arrival has passed): three cubes + the days in words (1 lit at 3 days, 2 at 7, 3 at 14), counted from the row's last change (the 🕘 history, else its order/grinder date); a working-list chip `⏳ getting old (n)`; never on a finished row. (2) "one method of making something a top priority in the category, but also have a way to show that there's something ready to be done … can't move forward until it's done … they want tan soffit, and Northstar Metal doesn't have a tan … somebody needs to go do some research … how do we mark that?" → keep ⚠ FIRST as the ORDER mark (top of its category) and add a second plate beside it, `🚧 stuck`, with a short "what it needs" box: the row wears a `🚧 STUCK — needs: …` band in the coral `--err` (words + glyph, never colour alone), the folded heading counts `🚧 n stuck` like ⚠ FIRST, a working-list chip `🚧 stuck (n)` scrolls straight to them, a row can wear both; clearing it writes the 🕘 history. His "maybe the ⚠ triangle could be that instead" answered: two different questions (which first / what is missing) → two marks.
- Before that: **v7.23** (pushed 2026-09-25): 👆 THE HOMEOWNER'S PICK INTO THE PICKED BOX. Eric: "In the build list, I click on 'Edit an item.' The Picked — what it is box is empty. But if I scroll down and unfold the homeowner tab, it has their pick (what's decided). There should be a button down there that says 'send to: Picked — what it is box'." The homeowner's pick (`it.pick`, client-facing, v6.89) and what it IS (`it.desc`, office-side, v7.21) are two boxes. In the 🏠 fold, right under "Their pick (what's decided)", `matPickToDescBtn(it)` draws `👆 Send to "Picked — what it is"` whenever a pick is there → `matPickToDesc(id)` copies it through `matSet(id, 'desc', …)` (the 🕘 history writes it down) and toasts; once they match the plate reads `✓ In the Picked — what it is box` (`.sel`, `aria-pressed`); a 👆 box that already holds OTHER words asks `⚠ SURE? It replaces "…"` first (`_matPickArm`, four seconds, `.mat-pick2desc.armed`). The 👆 box itself says when the pick is waiting below — `🏠 The homeowner picked "…" — it sits in the 🏠 fold below · 👆 Use it here` (`.mat-pick-wait`, only while the box is empty and a pick exists). Typing a pick into the 🏠 box redraws the window so the plate appears at once. The wall is unchanged (`desc` never rides to their file; `pick` still does). `tests/_test723.js` (9 checks).
- Before that: **v7.22** (pushed 2026-09-25): THE POCKET LEFTOVER'S PLATES. Eric, with a picture of 🎒 POCKET — not done: "The 'Just Me' and 'All Crew' buttons could be shrunk by width and all made on one line and the green outline around the Done button, I think, is confusing. Does that mean it's already done, or is it just a green outline? … I think a gray outline to match will work." `.pk-sendrow` is four across on the phone too (`repeat(4, minmax(0, 1fr))`, 4px gaps; `.pk-mini.pk-send` narrow, its word may wrap — "Needs / attention"; the PC keeps the 560px cap); the `.pk-mini.pk-ok` green edge is gone — ✓ Done wears the same gray as ↩ Today / ➡ Tomorrow (the ✓ and the word say what it does). **His question, answered with a rehearsal (and a picture of a simulated Phil phone):** "if I push Just Phil right now, what happens? Show what would come up on his phone." — the tap sends nothing yet: the Summary shuts, the words sit in ② SAY IT, ⑤ reads ✓ 📨 Just Phil; he picks the job and taps SEND; the same note is updated in place (never a second copy: `pocket: 'sent'`), his phone writes it into `Crew/Phil/shared.json` (Kevin's file does not get it), and Phil's FROM ERIC card reads `📨 <the words> — <job> · 🏷 pocket · <day>` with `✎ Add your notes` and `✓ Done — tell Eric`; with ⚠ off it never lands on his "Eric needs an answer" card. `tests/_test722.js` (8 checks: the plates measured at 390px, the tap, the SEND, a real reload as Phil reading the written file).
- Before that: **v7.21** (pushed 2026-09-25): SIX OF ERIC'S ASKS IN ONE SITTING. **(1) The edit window's order** — "I want the photo window to be at the top, just under the name. Then the note next then the rest can stay in that order": `matEditSheetHtml` reads Name · 📷 Photos · 👆 Picked — what it is · Note · What kind of row · Its lights · ⚠ FIRST · the folds (💵 Money · 🏷 · 🏠 · ☑ Steps · 📁 Pocket · 🕘 Changes). **(2) 👆 Picked — what it is** (`it.desc`) — "when I put the photo in and the Wizard reads it and puts the description … that description needs to go at the top. But I still want the note to be able to have notes in it … the text description needs to be up next to the note, or in a different place than the note": a box of its own on every thing to buy (a checklist row has none); `matPhotoRead` puts the product it read there (never into `sel` any more; empty boxes only; `filled` says `what it is`; the fill is written in the 🕘 history `by the Wizard`, the SKU and price too); on the row it LEADS the line — `👆 Delta pull-down, matte black · his note` (`.mat-desc`, bold; a tap opens the row; the note beside it still edits in place); a 🏠 row's leading words are the homeowner's own `pick`; the client cut strips `desc` (the wall). **(3) Sonnet 5** — "sonnet 5 is good to go": `AI_SMART = 'claude-sonnet-5'`; `aiShape(m, kind)` in `aiCall`: the Wizard (`kind === 'wizard'`, `max_tokens` 1200 → 4000) sends `output_config: { effort: 'medium' }` and thinks inside that budget (its thinking has no text; every reader joins the TEXT blocks only); every other smart call (journal, advice, a long paste — max_tokens 300–700) sends `thinking: { type: 'disabled' }` so its answer keeps the whole budget, as Sonnet 4.5 did; the quick brain (`claude-haiku-4-5`) and the fallback are untouched. Facts checked in the docs the same day: Sonnet 5 thinks by default, thinking counts against `max_tokens` and is billed as output, $2/$10 per million (the introductory price is now the standard price), effort is a GA field, no beta header. **(4) 👷 From Phil on the running log** — "Where is my 'From Phil' button on the running log?": v7.07 had taken the 👷 Crew plate off at his ask, and the FROM PHIL card (v7.13) empties as he flushes it. The plate is back (`chip('crew', …)`), named after the crew whose notes are IN the log (`crewIn` from `entries`): `👷 From Phil` with one name, `👷 From the crew` with more, none with none, never on a crew phone; it leads the second row of three, and `➕ Say something` wears `.rl-two` (spans the two columns beside 💬 Feedback) while the plate makes ten; a stale `crew` filter with nobody in the log clears itself. **(5) The Build List opens folded** — "When I click on the build list, all the categories should be folded. It's immediately overwhelming to scroll way down": `openMaterials` sets `_matRmShut` to every category (the heading keeps its counts and `n open`); a working-list or 🏷 filter unfolds them all (he asked to SEE those rows), ➕ item unfolds its own. Every Build List suite unfolds after `openMaterials` (`_matRmShut = new Set(); renderMatMgr();`) — the checks look at open rows. **(6) The Wizard answers about the job the question names** — "When I ask the wizard a question about a particular job, I think it is more focused on keywords than the job, as it brings up items from other jobs. And I did have in the text the job name" (his question named Josten/Weiser; a Mery note that shared "shower floor" came up): `askJobWords()` = each job's own words (3+ letters, not `ASK_STOP`, not `ASK_JOB_SKIP` — general/internal/admin/personal/home/house/duplex/remodel/roof/garage/deck/shop/project/pavilion/framing/addition/little/head — and never a word two jobs share; the Personal job never), `askJobsNamed(q)` = the jobs whose word (or whole name) is in the question, exactly. ONE job named → `base` = that job's records + the unfiled ones, and the pools, the word sweep, OPEN TO-DOS and the BOARD blocks all come from it (`jobOk`); the context says `JOB NAMED IN THE QUESTION: "…" — every OTHER job's records were left out on purpose`. Two or more → theirs first, nothing left out, `JOBS NAMED IN THE QUESTION`. Rehearsed read-only on his real log (4,025 entries, 26 jobs): "For the Justin Weiser job, bring up the floor tile for the master bathroom shower" names Josten/Weiser alone ("Weiser" is its word; "Justin" is nobody's), entry 4044 (Mery) is out, 57 Josten/Weiser rows + the unfiled ride. `tests/_test721.js`; `_test691`/`_test707`/`_test708` re-pinned.
- Before that: **v7.20** (pushed 2026-09-25): 🔗 THE BUILD LIST'S LIGHTS ARE ONE CHAIN, EVERY CHANGE TWO TAPS; ⚙️ THE GRINDER WEARS ITS NAME. **(1)** Eric: "When you click scheduled, but previous lights were not lit, it should light them all. And it should go both ways. If you advance a light by skipping one, it should light that one up, and if you de-advance it, it should go the other way too. And each light change should be a click and then a second click, because if any lights accidentally get changed, it could be missed or skipped on an order." A thing to buy walks P → O → R → S → I: lighting one lights every one before it, turning one off turns off every one after it (`matLightPlan(it, k)` = `{k, on, off, refuse}`, `matLightDo` — P O R stay the ONE state the homeowner's page reads, `it.s`, always a run from P; S = `it.sch` (+ `schd`), I = `it.ins`; Ordered still stamps `odate`). A checklist row walks 📅 → ✅ the same way; 👷 is a NAME he picks (the tags), never lit for him, and stays one tap (it opens "whose list?"). **Two taps:** the first only ARMS (`_matLightArm = {id, k}`, `matLightArmSet`, `MAT_LIGHT_ARM_MS` = 5 s, then it lets go by itself): the plate reads ⚠ sure? on a solid orange edge (`.mat-box.armed`), every plate the second tap would change wears a dashed orange edge (`.will`; its label says `lights with the next tap` / `goes off with the next tap`), and a line under the row says it in words (`matArmSayHtml`: `⚠ SURE? Tap 📅 Scheduled again to light 👆 Picked · 🛒 Ordered · 📦 Received · 📅 Scheduled — or leave it and nothing changes.`); a tap on ANOTHER light moves the ⚠ there; the ✎ edit window works the same ("Its lights — tap one, then tap it again"). ✅ with a step open is refused at the FIRST tap, in words (`matStepsOpenWhy`), nothing armed. The round lamp (`matCycle` → `matLampPlan`) arms the NEXT light the same way (`.mat-lamp-armed`, ⚠ on it) and no longer wraps round (an arrived faucet went back to "pick needed" on one stray tap); a checklist row's lamp goes to do → ⚙ in work → ✅ done. One tap = ONE line in the row's 🕘 changes (`f: 'lights'`, `matLitWords`: `none lit` → `Picked · Ordered · Received · Scheduled`). Opening or closing the board lets go of an armed light. ⚠ 1st stays one tap (a flag, not an order step); ⇄ keeps its own arm (v6.98). `? how this works` says all of it, and that a row's steps are in its window. **Answered, not built:** "Where are the sublists displayed when a item has a sublist?" — only in the row's ✎ window (☑ Steps under it); the row itself shows `· 2 open` / `· steps ✓` after its words. Offered: list them under the row, a box to tick each, on his go. **(2)** "I think the grinder should be labeled like the pocket list window is labeled. The running log is labeled. That will help separate the beginning of the grinder step 1 from everything else, like the pocket list." — `#grinderTitle` `⚙️ GRINDER` (`.g-card-title`, the `.pk-title` lettering; the emoji gear so it stands as tall as 🎒) is the first thing in `#qnCard`, tucked into the card's top padding (`margin: -8px 0 6px`) so the writing box moves down only ~15px (598 < 600 on the default Calm skin — `_test612`/`_test677` unchanged). `tests/_test720.js` (23 checks); `_test689`/`_test691`/`_test719` re-pinned (two taps; a light lights the ones before it; one `lights` line per tap).
- Before that: **v7.19** (pushed 2026-09-25): 🕘 THE BUILD LIST REMEMBERS WHAT CHANGED; ⚠ FIRST LIGHTS A FOLDED CATEGORY; ✓ FINISHED ROWS FOLD AND SINK; 🔤 LETTERS IN SETUP. **(1) History** — Eric: "some sort of signal or some sort of history … what it was changed from, and why maybe there could be a little button or light that lights up if something's been changed in the last couple of days … who changed it and a date and timestamp." Every row keeps `it.chg` (`{at, by, f, from, to}`, the newest `MAT_CHG_KEEP` = 30; `matChgPush`): `matSet` on the fields in `MAT_CHG_WORD` (name, note, what was picked, install date, estimate, paid, SKU / model, estimate line, kind of item, due date), every light (`Ordered light: ○ off → ✓ on`), ⇄ the kind, ⚠ first, 🏠 on their page, a step ticked, the steps list, the round lamp — and the homeowner's own pick when `matBoardRead` merges it (by `the homeowner`); who = `CREW_NAME || 'Eric'` (`you` on your own phone). Changed in the last `MAT_CHG_DAYS` = 2: a lit 🕘 plate on the name line (`.mat-q-chg`) AND the words under the row `🕘 changed today 3:39 PM by Phil · see what changed` (`.mat-chg-line`); a tap → `matChgOpen(id)` (`#matChgHost`, the `.we-sheet` look): newest first — the day and time, who, the field, `was: …` struck through, `now: …`; the edit window gains a `🕘 Changes` fold with all of them. No "why" box — the note on the row says why. The client cut strips `chg`. **(2)** "when there's something in the category marked with the exclamation point triangle, maybe make that light up orange, red, or something when the category is folded": a folded heading with a ⚠ FIRST row wears a 4px orange edge (`.mat-rm-head.mat-rm-first`) and an orange plate `⚠ 1 FIRST` in words (`.mat-first-lit`, dark words on orange); `matRoomCounts(its, firstShown)` leaves its own "⚠ n first" off then. **(3)** "when the item is completed, it should be folded to just the name. And a green light that says 'finished' … And they should move to the bottom of that category": a `matItemDone` row (installed / ✅ done) is its name + `✓ FINISHED` (`.mat-fin-lamp`: green edge, ✓ and the word), dimmed; `renderMatMgr` sorts it last (stable — ⚠ FIRST still leads); the name opens the edit window, where its lights take it back. **(4) Letters** — Eric: "The small bold letters are still hard for me to read more than the non-bold letters" → a tryout page (https://claude.ai/artifact/DyzyvEa17Ta3hfXty2yXci) → "make an option for all these fonts to be changed in the setup and appearance, similar to the theme … go ahead and do all of them." Setup → Appearance → `🔤 Letters` (`#fontChips`, `renderFontChips`, `FONT_OPTS`): A Today (the phone's own) · B Bigger, softer (same letters, less bold) · C Atkinson Hyperlegible · D Lexend · E Public Sans (C/D/E from Google Fonts, 400 + one bold weight; each chip is drawn in its own letters). `setFont` → `prefs.font` (synced) + `daylog-font` (painted at boot before the pull, `fontPaint` sets `data-font` on the root); `pullRemote` applies his pick. **Under it:** 519 spots in the file — every `font-weight: 900/800/700` is `var(--fw9/8/7, …)` and every `font-size: 10–12px` is `var(--fs10/105/11/115/12, …)`, the fallback being the old value, so A draws exactly as before (asserted); a pick sets the variables on `:root[data-font]` (heavy weights 600–700, the smallest sizes 12–13px), `font-synthesis: none`, `b`/`strong` → `--fwb`, buttons and boxes inherit the family. Sizes written any other way (12.5px, 13px…) are untouched. `tests/_test719.js` (24 checks); `_test689`/`_test691`/`_test698` re-pinned (a finished row folds; its lights are reached through the edit window), `_test685` (the six THEME picks are the `#skinChips` row).
- Before that: **v7.18** (pushed 2026-09-25): 🎒 THE POCKET IS PRIVATE TO THE PHONE; ⚙ TO THE GRINDER FROM THE SUMMARY. Eric asked "is pocket list entries searchable by other viewers? … nothing gets crossed until it gets into the grinder and manually tagged correct?" — true for his, NOT for a crew phone's: a crew pocket note (✓ done, flushed, overflow, not done) is a plain entry, the crew phone synced it to `Crew/<name>/App Data/entries.json` (Eric's Dropbox) and Eric's phone carded it and put it on FROM PHIL (no `vis` reads as "for Eric"); the live list rode the same file inside `prefs`. Then: "Make everyone's pocket list private to their phone. But if we want to get it to the grinder and tag it, you open the summary button, click on Pocket List or Pocket Not Done. Both will have a send-to-grinder with the same four unlock it buttons. Adjust me: just Eric or Phil, or all crew, and needs attention." **Private:** `phonePrivate(e)` = `e.mine` or a pocket note not sent on (`e.pocket && !e.pocketSent`); a crew phone's `livePayload` leaves those out and writes `prefs.pocket` as `[]`; they live on the phone's **private shelf** (`daylog-private` — `privShelfSave` from every `scheduleSave`, `privShelfLoad`), and `pullRemote` merges the shelf back (before the id count) — a pull REPLACES the log with the file's copy, which had also been wiping a crew phone's "🔒 Just me" notes since v5.44 (never in the file, so gone on the next pull); Eric's `checkCrewLogs` and `crewFeedTake` skip a crew pocket note not sent on (for a crew app that has not loaded v7.18). Eric's own pocket rides his own Dropbox as before (phone, PC, iPad — all his) and never had a `vis`. **⚙ TO THE GRINDER** (`pocketSendHtml(kind, id)`, `pocketToGrinder`): under every row of 🎒 POCKET LIST (the live items and the ✓ ones) and 🎒 POCKET — not done, the four plates of ⑤ worded for the phone — `🔒 Just me · 📨 Just Phil` (a crew phone: `Just Eric`, `visBuddy`) `· 🔓 All crew · ⚠ Needs attention` — two by two on the phone, four across on a PC (`.pk-sendrow`); a tap shuts the Summary, puts the words in ② and lights that unlock exactly as ⑤ would (`setVis`, `toggleAttn`); he picks the job, tags it, SENDs. `saveNoteFrom` then finishes it (`_grindPocket`, only while the box still starts with those words): a pocket NOTE is updated IN PLACE (words, job, tags, unlock; `pocket: 'sent'`, `pocketSent` — never a second copy; the running log's 🎒 plate reads `to the grinder — …`), a live item leaves the pocket (and ↩ Undo of a 🔒 Just me send puts it back). `tests/_test718.js` (15 checks: Eric's phone, his side's guard, then a real reload as Phil with a real push and pull).
- Before that: **v7.17** (pushed 2026-09-25): ✕ THAT WORKS FROM EVERY DOOR; HIS NOTES FIRST. **(1)** Eric, minutes after v7.16: "No longer relevant. Button doesn't light up or do anything. I'm not sure if it's working." It worked on a freshly asked answer, but an answer opened again from the running log's 🧙 row (`reopenAnswer`) came up WITHOUT `_lastAnswer` — empty after a reload (loading v7.16 was one), or still the last one asked — so `wizLinesHtml` stored no lines and ✕ returned silently (✎ still opened its box, with an empty locked line). Now `reopenAnswer` opens the KEPT copy of the same answer (`wizLog()` row with the same q AND a — exact) through `wizReopen`, else sets `_lastAnswer`; `wizOnScreen(q, md, srcIds)` (from `showWizFull` and `showInstantAnswer`) makes whatever answer is on the screen the one the app knows; `wizLineGone` says so in words when it cannot find its line; `wizGoneSave` puts the mark on the kept answer only when it knows which (`wizKeepFix` alone falls back to the NEWEST kept answer). My v7.16 suite had called the function, never clicked a real plate on a reopened answer — `_test717` does (a real reload state, a real click, both widths). **(2)** Eric: "i think notes should be searched first yes" (asked after the v7.16 diagnosis: texts and emails were 3 of every 4 rows the Wizard read first — 72 texts, 36 emails, 31 notes in the newest 140 on 09-25). `buildAskContext`: his own notes and records first (`ASK_MINE_N` = 100 newest), then the newest texts and emails (`ASK_CHAT_N` = 40) — `askChatter(e)` = texted / mail / "Text from"; inside a date span his notes lead too; `askSweep` breaks a tie in score toward his notes; the RECENT LOG heading says the order. Measured on his real data (read-only): 105–125k characters per question. **His model question, answered 2026-09-25, on his go:** the Wizard runs `claude-sonnet-4-5` ($3/$15 per million tokens, max_tokens 1200); `claude-sonnet-5` is $2/$10 but its tokenizer counts ~30% more tokens and it thinks by default (adaptive — max_tokens must rise or answers truncate; `effort` medium ≈ Sonnet 4.6 at high) — about the same cost per question (~$0.10) with better reasoning and more literal rule-following; `claude-opus-5-5` ($4/$20, always thinks, default effort medium) ≈ twice the cost. `tests/_test717.js` (15 checks).
- Before that: **v7.16** (pushed 2026-09-25): ERIC'S LIST OF 2026-09-25 — seven asks in one sitting. **(1) The pocket list is its own window** ("The pocket list needs to be separated from the grinder 5 steps. It's in the same bigger window"): `#pocketCard` is a `.card` of its own right ABOVE `#qnCard` (was inside it since v6.75), still just above ①; `body.crew-mode #pocketCard { order: 3 }` keeps it above the grinder on a crew phone's flex-ordered page; `_test612/673/677` re-pinned (plates → pocket card → grinder card). **(2) Everything Eric sends reaches Phil** ("I don't think everything that I send to Phil looks like it's going into his drop box"). Real data, read-only: every note since 08-14 WAS in `Crew/Phil/shared.json`, but `publishSharedNotes` sent the newest 120 unlocked notes FOR THE WHOLE CREW (older ones fell off as new ones came), a crew phone kept only 100, and only the newest 40 notes carried their picture — 45 of Phil's 68 photo notes arrived without one, and 44 of those copies were ALREADY in his photos folder. Now each man gets HIS newest `CREW_NOTES_MAX` = 300 (counted after `visFor`; `checkSharedNotes` keeps as many); `crewPhotosHave(name)` lists `Crew/<name>/photos` once a session and every note points at a copy that is there; NEW copies are capped at `CREW_PH_COPY_MAX` = 25 a sweep; a copy Dropbox refuses is no longer counted as done (`dbxRpc` returns the error object, it never throws — only a conflict = already there; "too many" = next sweep). Rehearsed: 174 notes back to 07-30 (was 118), 97 with a picture (was 23). An answered question (`askDone`) now joins his FROM ERIC log marked `📢 ANSWERED` / `📢 NO ANSWER NEEDED` (row `ask`); an open one still rides only in `asks`. **(3) ⤵ Flush it on Phil's 📢 ERIC NEEDS AN ANSWER card** ("Under the 'Eric needs an answer,' there can be a 'flush it' or 'log it' button"; the one open ask that day was a heads-up sent with ⚠ — a container move, not a question): under the answer box, `⤵ Flush it — no answer needed · it goes in your log` (`crewAskFlush`): a note in HIS log (`⤵ Seen — no answer needed: <q>`, `askRef` + `askFlush`), off his card; Eric's `checkCrewLogs` closes the ask (`askDone`, `askFlush = name` — it leaves every crew phone) with NO Sort card and nothing on FROM THE CREW (`crewFeedTake` skips `askFlush`); his strip reads `✓ SEEN by Phil — no answer needed`. What puts a note on that card: ⚠ Needs attention in ⑤ UNLOCK IT on Eric's phone (= the old ❓ NEEDS AN ANSWER); leave ⚠ off and it goes to FROM ERIC only. **(4) ✕ "No longer relevant" on every Wizard line** ("I also need a button that says 'No longer relevant' or 'Delete'. Sometimes things will get on the list that I don't need on there"): a second plate beside ✎ — ✕ over `Delete` on the phone, `✕ No longer relevant` on a PC (`.wl-gone`, both his words); a tap KEEPS it with his fixes (v7.02's way — `_wizPend` item `{gone: true, t: WIZ_GONE_T}`): the line is struck through + `✕ OFF THE LIST — not saved yet`, the plate turns to `↩ Keep it` (`wizLineGone` toggles); saved (`wizPendSave` → `wizGoneSave`) it goes on `prefs.wizGone` (100 kept) — handed to the Wizard on every question as `LINES ERIC TOOK OFF HIS LISTS` — and onto the kept answer (`wizIsGone` draws it struck, no plate); nothing in his log is deleted; only entries the LINE names by number get a mark (`wizLineIds`). **(5) The Wizard's answers** ("The wizard's not coming up with very good answers" — a read-only diagnosis of 46 kept answers and his 14 fixes): a fix was written onto EVERY record the answer cited (56 entries carried fixes that were not theirs — the Chafin "no longer relevant" sat on the container move) → `applyCorrection` now marks only records the line or the fix names by number, or the answer's ONE record (`prefs.wizFixes` carries every fix either way); the words of ASKING were searched ("bring up a list of what I need to talk to Logan about this week" pulled July records for bring/list/talk/week) → `ASK_STOP` gained them; "the newest 140" was the newest by the order things were ADDED (an Aug 8 crew note sat at the top) → `live` is sorted by date; a date span that did not fit said "all of them are included" → it says how many fit; a pocket leftover went out twice (POCKET NOT DONE + BOARD) → `boardCtx` skips them; a crew member's note logged into his book (✓ Log it, v7.15's flush — "✅ FINISHED" ones too) was an OPEN board line → `boardOn` leaves out `crewKey` / crew `who` notes unless he touched them; the prompt now says a summary lists open items ONCE, leaves out what is done or taken off, cites only ids shown. **Parked for his go:** a newer model (every question goes to `claude-sonnet-4-5`, max_tokens 1200), "last week" = the calendar week (Mon–Sun) vs the last 7 days, texts/emails fill 77% of the newest 140 rows (his own notes crowded out), the Build List and QuickBooks paid lines never reach the Wizard, cleaning the 56 misplaced old fixes, heads-ups never expiring. **(6) Phil's tags are his own** ("he had all my tags on step 4. Tag it … There's no reason he should have any personal tags from me"): `crew.json` carried Eric's WHOLE list (family names, "Cooking"…) and a crew phone merged it in on joining (`daylog-crew-tags`), at boot and on every `refreshCrewCfg`. Now a crew phone's `knownTags()` = his own + the tags on notes Eric SENT him (`crewPushedTags`, from `crewShared`) + 🔒 Personal (the lock); nothing merges Eric's list; `crewTagScrub` sweeps the handed-over tags out ONCE (`prefs.crewTagScrub716`) unless he used one on his own notes; a fresh crew phone starts with no tags (Eric's starter set was his own people). Eric's side: `crewCfgBody(name)` is the one shape of crew.json and its `tags` = `crewTagsFor(name)` (tags on notes pushed to that man, never Personal); `crewCfgRefreshOnce` (from `publishSharedNotes`) writes every crew.json ONCE, quietly (`prefs.crewCfg716`, set only when every folder took it) — so the Aug-13 copy with his list leaves their Dropbox, and their job lists come up to date. **(7) A photo that is not paper reads as nothing** ("Phil did a photo entry. And the wizard tried to read it like a receipt and comes up with gibberish … if it's not a receipt, it gets just blank"): with no Claude key a picture is read by Tesseract in the browser, and a jobsite shot came back "NS RY — NR 19 N\ | — J" — drafted as the note's words, kept on the entry (`ocr`), and made the photo's FILE NAME (Eric's own log held 261 such readings, many scrambled). `ocrClean(data)`: kept only when at least `OCR_MIN_SURE` = 8 real words (3+ letters or 2+ digits) are ones Tesseract was `OCR_SURE` = 80%+ sure of, and then ONLY the sure words, line by line. Calibrated on his real pictures: jobsite and progress shots scored 25–31% overall with ZERO sure real words; a bank letter, invoices, insurance papers and receipts had 38–123. Through the app's own `ocrRead` on nine real photos: the three scene photos blank; the six papers kept, clean (a store receipt that read "A Bp a VOT STR te io" now reads its store lines, phone number, date, time and SALE). Old readings already on entries are left as they are. **Answered, parked:** "Does Phil have a running log like mine? I think the 'from Eric' and 'from Phil' could be folded into the running log. And just have a filter for that." — yes (📜 RUNNING LOG under FROM ERIC on his page); folding both cards into the running logs as a filter plate offered, on his go. **(8) ⬆ Take 30 back off** ("The bottom of the running log says 'A Next 30,' but it needs a button to take 30 back off"): `rlLess(30)` beside ⬇ Next 30 — back 30 at a time, `⬆ Back to the first 12` at the end, never under 12, counted from what is on the page (`window._rlTotal`, so it works after Show all too); the pager (`.rl-pager`) now stays while more than 12 show — it vanished once everything was showing, leaving no way back. `tests/_test716.js` (41 checks: Eric's phone, a 1280px PC, then a real reload as Phil); `_test702` re-pinned (a saved line fix no longer lands on records the line does not name).
- Before that: **v7.15** (pushed 2026-09-25): ⤵ THE FROM PHIL CARD FLUSHES LIKE THE POCKET. Eric: "From phil window needs to have a flush like the pocket list does and it goes into grinder like pocket list". The card's head wears the pocket's own plate (`#cfFlushBtn`, `.pk-tiny.pk-flush`, before the how-many wheel): `⤵ Flush N`, or `⤵ Flush these N` while the search / person / job wheels narrow it; it takes EVERY note the card lists then, past the how-many cap (`_cfShownKeys`, set in `renderCrewFeed`), then clears the search and the wheels. Each row's `✓ Log it` became `⤵ Flush — into your log` / `⤵ Flush — off the card` (`.cf-flush`, `crewFeedFlushOne`; `crewFeedLog` stays as its alias) — the plate says beforehand what the flush will do (`cfWillLog`). **What a flush does** (`crewFeedFlushKeys`, the 🧹 sweep's way — `_revBulk`, the toast swapped, one word at the end: `⤵ N off the card — n into your running log, k already handled ✓`): a NOTE whose Sort card waits goes into the running log through the same door as always (`reviewAct(key,'log')` → `Phil: <words>`, `who`, the note's own day); a ✅ finished note goes in as `Phil: ✅ FINISHED: <words>` (`cfCardFor` → the same door; a leading "Phil:" of his own is dropped, `cfWords`), and one that ANSWERS a to-do also checks that to-do off, as ✓ on its card always did — neither kind's words had ever reached the log before (a Done that closes no to-do never gets a Sort card; one that answers a to-do only ticked it off); anything already in his log — the card's key on the entry (`e.crewKey`, stamped by `reviewAct` from v7.15) or, for an older entry, the same name + the exact words + the same day (`cfInLog`) — just closes its card and is never logged twice (handled-ness lives per device in `pendDone`, so his PC re-cards notes his phone already logged); a note skipped before just leaves. A flushed note LEAVES THE CARD for good: its key goes into `prefs.crewFeedGone` (synced, newest 800; `crewFeedRows` filters on it). **Also:** a crew note with no job, or a job not on Eric's list, now files under `—` (`reviewAct` — it fell to `jobs[0]`, "Internal / Admin" today, the silent default v6.81 took out everywhere else). **Rehearsed read-only on his real data** (the mirror behind a fake Dropbox, nothing written): Phil's 10 notes — 6 already in the log (their 6 Sort cards of Aug 8 keep coming back) close without a second copy; 4 ✅ notes go in dated Aug 8, two under Ashman and two under — (two of them Phil's answers to two of Eric's to-dos, which had never reached the log). `tests/_test715.js` (15 checks); `_test713` re-pinned (the row's ⤵ Flush; the row leaves the card, so 4 not 5).
- Before that: **v7.14** (pushed 2026-09-24): FOUR OF ERIC'S ASKS. (1) "When I click Summary, just under Needs You is the Pocket List, which I like. But instead, I want the pocket not done, just under the needs you." — `SUM_SECS` = ⚠ NEEDS YOU · 🎒 POCKET — not done (`pocket`) · 🎒 POCKET LIST (`get`) · PEOPLE · …; the page still opens on the first of the two that holds anything, which is now the leftovers when there are any. `_test676`/`_test707`/`_test678` re-pinned to the order. (2) "Phil and crew don't need to see what they look at on the project portal. And not the copy link either." — the office fold (`renderPortalFold`) is 📐 Plans · 📖 Journal · 📋 Build List · 👁 View as this client (two by two); on Eric's portal 📋 Copy link and 📊 What they look at now wear `.only-me` (the dashed only-you edge) with the rest of his-alone plates. `_test709` re-pinned. (3) "On the journals, we have: work notes · hours on site · photo count · money moved. I don't think we need any of that. We only need the draft it for me because it already knows where to pull it from." — the four chips and their hint line are gone from `openJournal`; `journalDraft` no longer reads `prefs.journalPrefs` (left in prefs, unread; `jrnFilter` stays a function): it pulls ⭐ the stamped notes, the week's work notes and the picked-photos line, and NEVER an hours tally, a photo count or raw money (his most-journaled jobs had those off anyway; raw money on a homeowner page would be an unmarked cost). The button reads `✍ Draft it for me — from this week's notes and picked photos (the Wizard polishes it)`. (4) "At the bottom of the journal editing page, it tells the week, says 'Edited,' and has an edit button and a take back button, but they don't really fit on there very well. Make sure all the take back buttons are in a line and all the edit buttons are in a line." — `renderJrnReleased` rows are `.jrn-rel-row`: a grid `minmax(0,1fr) 76px 108px` (the week's words wrap in their own column; `.jrn-rel-ed` / `.jrn-rel-back` full-width in fixed columns); the armed take-back reads `⚠ SURE? Tap again` (`.armed`, err edge) in the same column. `tests/_test714.js` (15 checks: the order and open-on, the portal edges, the draft with every old pick on, the two straight columns measured at 390px).
- Before that: **v7.13** (pushed 2026-09-24): 👷 FROM THE CREW ON ERIC'S PAGE; FOUR PLATES ON THE UNLOCKER. Eric: "The log that Phil has below the Grinder button that says 'From Eric' and it's got a little searchable area is that on mine as well? I need to be able to see incoming things from Phil, or just his running log that he's unlocked for me or everyone." It was not — a crew note reached him only as a Sort card (👥 PEOPLE) and only `✓ Log it` put it in his running log. **The card** (`#crewFeedCard`, right after `#qnCard` in the DOM = under the grinder, above the running log; `renderCrewFeed`, called at the end of `checkCrewLogs` and from `renderAll`; hidden on a crew phone and in the 👁 preview): `crewFeedTake(name, folder, entries)` keeps, per crew folder, every `Note`/`Done` that is not `mine`/`sample`/`personal`, has words, and was unlocked FOR ERIC OR FOR EVERYONE (`cfForMe`: no `vis` — an old note, which went to Eric — `'crew'`, `'Eric'`, or an array holding `'Eric'`; a note for another crew member alone is left out, as are clock/hours/miles); rows newest first: `👷 Phil · time · job · 📨 for you | 🔓 for everyone` (+ `✅ finished`, + `⚠ NEEDS YOUR ATTENTION` for `route:'Eric', urg:'now'`), the words in full (600), `📷 Open the picture` (`cfPhotoPath`: the crew phone's first path segment is its mount → Eric's `/Clore DayLog/Crew/<name>`), and `✓ Log it — into your running log` while its Sort card (`crew:<name>:<id>:<day>`) still waits (`crewFeedLog` → `reviewAct(key,'log')`). A search box (`crewFeedSearch` keeps the cursor through the redraw), a person wheel (2+ senders, `✓ ONLY Kevin`), a job wheel, `✕ clear`, a how-many wheel (`prefs.crewFeedN`) and a fold (`prefs.crewFeedFold`) that says the count in words; titled `FROM PHIL` with one sender, `FROM THE CREW` with more. Then, mid-build: "My unlock it button should say 'Just me', 'Just Phil', and 'All crew'. And then I don't think we need the buttons that send it to the specific person, but maybe just the exclamation point in a triangle, like the warning light, where it means this needs someone's attention. I think there'd be four buttons total on the unlocker for both me and Phil. They'll just say a little bit different things." **The unlocker** (`renderVisChips`): Eric's phone `🔒 Just me · 📨 Just Phil · 🔓 All crew · ⚠ Needs attention`; a crew phone `🔒 Just me · 📨 Just Eric · 🔓 All crew · ⚠ Needs attention` (was `🔒 Just my log · 📨 Eric · 🔓 All crew · 📨 <each mate>`); two by two on the phone, four across over 700px; the ➕ person plate and the rows/across wheels are gone; chips carry `data-v` (the personal lock disables every plate but `data-v=""`). "Just Phil" = `visBuddy()` = the office man on the roster, else the first crew name. The first three are ONE choice (`setVis` — v5.51's stacking of 🔓 crew + 📨 names left with the names; `sendTo` is no longer set from the grinder); `🔒 Just me` also clears ⚠. **⚠** (`toggleAttn`, `attnOn`; `toggleAsk` is an alias): on Eric's phone the old ❓ NEEDS AN ANSWER (`ask`); on a crew phone the old 🔔 Needs Eric's attention + 🔥 I need it NOW (`qnRoute='Eric'`, `qnUrg='now'` → the 🔥 hot card on Eric's page) — so `#crewRoute` ("Send it to" / "How soon") no longer shows on a crew phone (still drawn, hidden) and `#myReqStrip` (the SENT → ANSWERED lamps) moved out of it; tapped while 🔒 it picks the everyday person first (a note nobody sees cannot need anybody), so the ⚠ plate is never disabled (`lockGate` lost its ask-chip block). Step ⑤'s folded line reads `· 📨 just Phil · ⚠ needs attention`. The old "🔒 Just my log" words are gone (lock note, toast). The phone "ring" half of ⚠ still waits on the alerts fix (no `prefs.pushSecret`) — what works today is the pinned card. `tests/_test713.js` (22 checks: Eric's phone, then a real reload as Phil).
- Before that: **v7.12** (pushed 2026-09-24): 👷 A CREW MEMBER'S DROPBOX ON A PHONE NEVER SET UP AS CREW SAYS SO. Eric sent two screenshots from Phil's phone (09:53, on v7.10): the 🏠 Project portal read "No portal set up yet — ask Claude to build the client pages" and the 📇 Job card listed only "Test by Phil in setup". Both are OWNER-phone words — `CREW_NAME` was empty (the bottom bar showed Personal and Vault, which `body.crew-mode` hides): his phone was connected to his OWN Dropbox but never set up as a crew phone (⚙ Setup → 📱 This phone → 👷, `crewJoin`), so it ran as an owner's phone looking for `/Clore DayLog/App Data/Client Portal/index.json` in his Dropbox. His crew folder (`Crew/Phil/App Data/entries.json`) meanwhile holds Eric's job list and August entries — a DIFFERENT install of his (on iPhone the Home Screen app and Safari keep separate storage) that is in crew mode. **The fix:** when the owner branch of `renderPortalList` finds no portal it still prints the old words and then `crewDbxFind()` looks at the Dropbox TOP for a folder holding `crew.json` (exactly what `crewJoin` looks for; ONCE a session, cached in `_crewDbx` — `undefined` not looked · `null` none · `{name}`; a refused listing is not an answer and is looked again) and, when there is one, the box becomes `crewDbxHtml(c)`: `👷 This Dropbox is Phil's — a crew member's`, why in words, and ONE button `👷 Set this phone up as Phil's` → `crewJoin('crewJoinHere')` (it now takes the id of the box to speak in); the 📇 Job card window shows the same block at its top when the answer is known. Eric's Dropbox always has the portal, so his phones never look (asserted: zero top listings). `crewJoin`'s no-Dropbox words now say `connect Dropbox (⚙ Setup → Dropbox)` (they said "above"; the Dropbox section sits BELOW 📱 This phone). `tests/_test712.js` (12 checks, including a real reload into crew mode). **Steps given to Eric for Phil:** in the Boiler Room he actually opens (the Home Screen icon): ⚙ Setup → 📱 This phone → 👷 This phone belongs to a crew member — set it up (or, on v7.12, the button in the portal); if it says no crew folder, dropbox.com → Shared → add "Phil" (and "Client Portal"), then tap it again.
- Before that: **v7.11** (pushed 2026-09-24): 📅 A WAITING CARD SITS WHERE IT CAME IN. Eric: "on the running log a grinder 'pay spenard builers supply' has been stuck at the top row for a while". It was not a grinder note: the running log folds Review cards in as `⏳ waiting:` rows (v6.10) and every card that is not crew/client/text/question wears the ⚙ glyph (`pendKind`); this one was a 💳 BILL card (`billMaybeSuggest`, off a statement photo) and `pendWhen` placed a card at `payload.ts || payload.d || payload.due` — a bill's only date is its DUE date (10/10, in the future), so it sorted above everything until the bill came due. The same held for a 🔁 renew card (its expiry, Dec 22 — past the first 40 cards, so never drawn) and the 👷 crew cards (a `day`, no time → "now" on every draw → the folded `⏳ 8 crew messages waiting` row sat second from the top). **The fix:** `pendStamp()` (beside `savePendingSoon`) gives every card `p.at` = the moment it CAME IN, once, and it is saved with the pile — called in `checkPending` right after the load (and `savePendingSoon()` when it stamped anything) and at the top of `savePendingSoon` (a card made on this phone is stamped as it is saved); an OLD 💳 bill with no stamp takes the time of the photo it was read off — the log entry whose `ai`, whitespace-squeezed and cut at 240, equals the card's `payload.what` word for word (never a guess) — anything else takes the moment this app first holds it. `pendWhen` = the first of `pay.ts` · `pay.d` · `pay.day` · `p.at` (a bare `YYYY-MM-DD` read as local noon via `pendTime`), NEVER later than now; a due date is never a placement. `pendWords` says `💳 bill — Pay …` for a bill. Checked read-only against his real pile and log (mirror, nothing written): the Spenard card stamped 2026-09-14 (its statement photo, entry 3301), the top of the running log is his real latest notes. `tests/_test711.js` (13 checks). pending.json has ONE writer (`savePendingSoon`), so every save carries the stamps; the first load of v7.11 writes the pile once (every card newly stamped), then never again for that reason. **Found the same day, told to Eric, nothing changed:** he emailed himself his Claude API key (to copy it onto a new iPad Air) and the email pipe logged it — the key sits in an email entry, `prefs.mailIgnored`, `master-log.csv` (an email's SUBJECT goes into the CSV, and the subject was the key) and `/Texts/Mail Ignored/`; an OLDER key sits in a File Cabinet entry of 2026-07-26 (and every daily backup since). Advised: make a new key at console.anthropic.com on each device and delete the old ones there. NEVER print a key in a tool output or a reply — show fingerprints only. Offered, not built: the mail sorter locking an email that carries a key or password instead of logging it in plain words. **Phil's text of the same morning** ("Version 7.10 in effect. Job card works if I add it, no project portal?"): his crew file says office and he synced at 01:42, so the likely cause is the shared Client Portal folder not added to HIS Dropbox — v7.09 deliberately shows only `👁 Open the client's page` until `findPortalRoot` finds it; steps given to Eric.
- Before that: **v7.10** (pushed 2026-09-24): 💵 THE GRINDER'S BUILD LIST PICK IS A COST LINE; SIX JOURNAL PHOTOS THEN A FOLD. Eric: "When I enter something into the grinder and I click 'Build List' I want all the construction categories on there, well organized, so I can just click … once I click the build list in the category I want 20% added automatically in it goes directly to the homeowner's upcoming cost page. I don't want it to stop and get sorted again." **The WHERE window** (`blModalOpen`): the job's own Build List categories first (`📋 On this Build List now`), then EVERY construction category of the estimates board by build phase (`EST_PHASES` → `.bl-ph-h` headings, `.bl-cat` chips; overhead categories Tools / Fuel / Office/Admin left out; a category already on the board is not listed twice), in one scroller; the hint says a construction category + a dollar amount = their Upcoming too. **SEND** (`saveNoteFrom`): `_bl` carries `cat = isBuildCat(room)` (a name in `EST_PHASES`, never an overhead one) and `amt = grindAmtOf(e)` (the photo's 💵 read via `estBillParse`, else the first `$n` in his words; a minus is never a cost); after `grindToBuildList` the note gets `e.category = room` and rides `estQuickUpcoming(e.id, null, amt)` — the SAME one-tap road as the log's 🔮 → Upcoming (`estQuickUpcoming` gained a third argument, an amount override): a pend under that category on `estimates-<code>.json` with `base`, `e.budg = 'sent'`, the client page's `upcoming` rebuilt with the job's markup folded in (its board's `mk`, 20 when none), never named, the vendor never on their page. A ROOM pick (Kitchen) with a $ keeps the money OFF their page and says why (no guessing which cost line). A note that goes to their page has no ↩ Undo (a page is "someone", like a routed note); the toast reads `📋 Build List → Framing · 💵 → their Upcoming ✓` and estQuickUpcoming's own `🔮 On their page under UPCOMING — Framing, $1,488 ✓` follows. A photo whose read lands AFTER send is not sent (the log's 🔮 → Upcoming is there for it). **The journal window** (`renderJrnPhotos`): the first `JRN_PH_SHOW` = 6 tiles show, the rest are `hidden` (`.jrn-more`) behind ONE full-width plate `▸ +N more photos — tap to see them all` / `▾ Showing all N — tap to tuck the rest away` (`jrnPhMore`, `_jrnPhAll`, reset on every `openJournal`); picked-and-unsent photos were already sorted first (v6.60), so this week's picks are never behind the fold. Eric: "just have the first six photos showing and then the rest folded … The hundreds of photos could add up, and I have to scroll way down." `tests/_test710.js`. **Also asked 2026-09-24, PARKED on his own word ("Don't do it yet"):** "I feel like the build list and the estimates page could be combined … start comparing and thinking about how it could be joined but still kept very simple." First thoughts given (memory `build-list-direction`): one window per job with two views of the same rows — BY ROOM (today's Build List) and BY COST LINE (today's estimate categories, each unfolding to the rows linked to it with their money summed, plus the bids / bills / books figure) — the money-only lines as rows with no room; v7.10's pick already makes a construction category a Build List category, which is the first brick.
- Before that: **v7.09** (pushed 2026-09-24): 🏢 PHIL'S PHONE, ERIC'S WAY. Eric, after the 09-23 crew-phone audit (memory `phil-crew-phone-gaps`): "Start with the job cards. He should be able to see everything in the job card except the thing on the bottom where I can choose whether it's private or crew … he should be able to edit any of the ones that are open to the crew. Next is project portal … all the jobs. At the bottom where I can give a job a client page, he should not be able to see that, so put a dotted line around that, right? The selection at the top where you choose what the client sees, Phil does not need to see that. He should not have the Remove, Rename, or Rotate codes. I don't want him to have the Project Tracker, Receipts, or estimates but he should be able to click on Plans, Build List, and Journal." **One folder, two names** (`PORTAL_CANON`, `canonPath`, `localPath`, `swapPrefix`, beside `portalRoot`): the Client Portal folder is `/Clore DayLog/App Data/Client Portal` on Eric's phone and `/<whatever he accepted the share as>` on Phil's — a path written into a shared file (a Build List photo `it.phs`, a pocket paper `pkt.docs[].path`, a plan set `cur.file`) is STORED the way Eric's phone sees it (`canonPath` on the upload's `path_display`) and OPENED by this phone's own name (`localPath` at the top of `showPhoto`, `matOpenDoc`, `matConvoImg`, `matThumb`, `matWhatIs`, `matPhotoRead`); a FROM ERIC note's picture (`/Clore DayLog/Crew/Phil/photos/…`) is turned into `DBX_ROOT/photos/…` the same way. On Eric's phone both are no-ops. **The portal on an office phone** (`renderPortalFold`): the fold is Plans · Journal · Build List · Copy link · What they look at · View as — no Estimates / Receipts / Project Tracker (`openEstimates` and `openRcptReview` now turn EVERY crew phone away in words: "the money stays on his phone"), no Rotate / Rename / Remove, no 🎛 switches, no ➕ new page; it shows ONLY when the share was actually found (`off = amOffice() && !!offRoot` — before, `portalRoot()` fell back to the phone's own folder and a board saved there was a board nobody saw); with the flag but no share the foot says why; 🔄 Look again re-hunts the share; the journal meters read the real dates on a crew phone too (`refreshJrnAges` runs from the crew branch and repaints). **Plans:** `openPlanRack` no longer refuses crew (portalGuard still stops field crew); `pkStamp` uploads under `portalRoot()/Plans/<code>/<slot>/` — INSIDE the shared folder (it was `/Clore DayLog/Plans`, Eric's alone) — and stores the canonical path. **Build List:** `openMaterials` no longer refuses crew (its files were always under `portalRoot()`). **Job cards on an office phone:** `canEdit = !CREW_NAME || amOffice()` — the reading face + hand-out ticks + ✎ Edit, the form, add/drop person and code, all as Eric's, but the 👷/🔒 line is Eric's alone on both faces (`cardLock` returns on crew); `cardMake` uses `allCards()` (his copy of Eric's cards); `cardCommit(c)` = Eric's phone stamps `c.updated` + saves + publishes, a crew phone `cardEditPush(c)`: the card lands in `crewCards` at once, in `daylog-card-edits` (localStorage), and in `DBX_ROOT/App Data/card-edits.json` (`{ by, at, edits: { <cardKey>: { card, at, by } } }` — read-modify-write, his own folder); `cardEditsOverlay()` keeps his edits on top of `shared.json`'s cards until Eric's copy carries them (`updated >= at`) or the card is gone; Eric's `checkCrewLogs` reads `card-edits.json` from OFFICE crew folders only (`isOffice(f.name)`) → `cardEditsTake(name, j)`: an edit NEWER than the card and never a locked card — addr / people / codes / notes sanitized and capped, `c.updated = e.at`, `c.editedBy`, save + publish + a toast, a redraw if the card is on screen and not mid-edit. Eric editing after Phil wins (his save stamps `updated` now). **Eric's own portal** (`renderPortalList`): with office crew on the roster, Estimates, Receipts, Project Tracker, Rotate / Rename / Remove, the 🎛 block (`.pf-visbox`) and ➕ Give a job a client page wear `.only-me` (a 2.5px dashed `--mine` cyan outline, the client-preview colour) and a line at the top says `┄ A bright dashed edge means only you see it — Phil does not`; no office crew = no dashes. `tests/_test709.js` (32 checks: Eric's phone, then a real reload as Phil with the folders mounted under other names, then the field face). **Data, same night, create-first through the Dropbox connector:** the five plan sets moved from `/Clore DayLog/Plans/<code>/…` into `/Clore DayLog/App Data/Client Portal/Plans/<code>/<slot>/` (Ashman's R1 got its missing `.pdf` back in the move) and the three rack files (`plans-ashman-bb75cd`, `-gilbreath-224e77`, `-scrithfield-7c4e19`) rewritten with the new paths, old copies in `App Data/Backups/`. **Also in v7.09, Eric mid-build:** "On the build list, there's a box that says 'Reply: Go straight onto their page.' I want that reply box gone. When I open a build list item on the bottom … Pick Needed · Picked · Ordered · Arrived · Edit · Delete. Let's keep the Edit and Delete, and get rid of the rest because we already have that in the new buttons on top." — `matConvoHtml` no longer draws the ✍ Reply box or the 💬 Reply plate (the client's remarks still read there; `matReplySend` stays a function), and the opened row no longer draws `matLampRowMgr` at all (the lights are the P·O·R·S·I boxes, ⇄ is on the row, ✎ / ✕ on the name line since v7.08). Then: "When I click on a build list item … I want the edit and delete buttons to always be visible. Rather than having it fold I don't think anything needs to fold because this will be really simple, especially with the edit window that opens for all details." — **NOTHING FOLDS on the Build List now:** a row is its name line and its lights line (`matMgrItem` lost `open` and every block under it — the steps with their menu, the client's remarks/picture, the pick hint, the pocket chip); a tap on the name (`.mat-nm`, `matItRowTap`) opens the edit window, a hold still does; `_matItOpen`/`matItToggle` stay as no-op functions for the old suites. The edit window took what the fold had: a ☐/☑ plate on every step (`matKidOk`) at the front of the ☑ Steps rows, and the homeowner's own picture (`it.cph`) first in 📷 Photos marked 🏠 with 🔮 What is it (`matWhatIs`); the client's remarks were already in the 🏠 fold. `_test708` +5; `_test691` re-pinned (no row to open or fold); `_test637` re-pinned (the office fold no longer carries the Project Tracker — 2 doors, not 3). **Still on his go:** alerts (no `prefs.pushSecret` anywhere; a crew phone's push sub is saved where `pushlib` never looks), the grinder's 📋 Build List chip and the log's 🔮/💰 links on an office phone, the Wizard key on Phil's phone, a Dropbox `rev` check on the board's auto-save.
- Before that: **v7.08** (pushed 2026-09-24): ✎ THE BUILD LIST ROW — EDIT AND DELETE ALWAYS THERE, THE NOTE EDITS IN PLACE, PHOTO ICONS; THE WIZARD READS A PHOTO. Eric: "when i click on the item and it open up the buttons across the bottom some of which are edit and delete. i want those buttons always open but keep the click and hold for edit as well. the words next to the light up buttons, i want to be able to click and edit without having to click the edit button and pull up the window. but keep both … add a photo or two to each item but keep them small and make it clickable … drag and drop and wizard can read it and pull sku number and price and add markup etc. lets keep that in the edit window … build list just has photo icons to click on." **The row:** `matQuickHtml(it)` ends every row's name line with small plates — a 📷 per photo (`.mat-q-ph`, three then `+n`; a tap = `showPhoto`, the row does not open), ✎ (`matEditOpen`) and ✕ (`matDelItem`, two taps, `⚠ SURE?` + `.armed`); holding the name still opens the edit window. The opened row's bottom strip (`matLampRowMgr`) lost its ✎ edit / ✕ delete, the trail lost `📷N`, the opened row lost its `📷 1` chips. **The note in place:** `matSelHtml(it)` — a tap on the words next to the lights (`.mat-seltxt`) turns them into a writing box right there (`_matSelEdit`; `_matSelDraft` keeps what he types through a redraw); Enter or a tap away keeps it (`matSelDone` → `matSet(id,'sel')`), Esc lets it go; the install date / money / estimate line ride after it, outside the box; it is the same note as the edit window's. **The edit window:** `matEdPhotosHtml` is its own open section right above 💵 Money — 64px thumbnails (`matThumb`, Dropbox `w128h128`, kept for the session) you tap to look at, 🧙 read and ✕ under each, a drop box (or tap to pick; `matDrop-`/`matPh-` ids kept) and what the Wizard read (`matReadCardHtml`); the old fold is 📁 Pocket alone. 💵 Money gains a SKU / model box (`it.sku`) and "with this job's N% markup" (`matMkNow`/`matMkLoad` read `mk` off `estimates-<code>.json`, 20 when there is none; `fmtMoney` — cents). **The Wizard:** `matAddPhotos` keeps a long file name's ending (the v7.06 plan-rack bug, again), saves itself (`scheduleMatSave` — the old toast said 💾 Save), and with the Claude key reads the FIRST new photo (`matPhotoRead(id, pi, file)`: the file via `shrinkForAi`, or later from the photo's `w1024h768` thumbnail; `aiCall(…, 'photo')` = the quick brain; `MAT_READ_SYS` asks for JSON only; `matReadParse`) — the SKU / model / item #, the price and the product name go into EMPTY boxes only (`filled` says which; nothing he typed is ever written over), `it.phRead` keeps the reading, and 💵 Money opens; no key = the photo still lands and the box says what the key would add. **The wall:** the client cut strips `sku` and `phRead` too (templates were already a whitelist). `tests/_test708.js` (24 checks, made-up products and prices); `_test691` re-pinned (✎ on the name line; the window reads Name · Note · What kind · Its lights · 📷 Photos · 💵 Money · 🏷 · 🏠 · ☑ · 📁 Pocket). **Same day, data — Eric's Word "Finish Out Action Checklist" (9/23, the list he sent Phil) into the Josten–Weiser Build List, by hand through the Dropbox connector, create-first:** ADD-ONLY into `materials-office-josten-842d53.json` — 3 new rows (KITCHEN Disposal and sink trim · MASTER BATHROOM Shower layout — curb or curbless · GARAGE / LOFT Garage lighting — check it is complete), 55 steps (`kids`, the ☐ actions) on 36 rows, 34 notes EXTENDED (`sel` + ` · 9/23: …`, the old words first), 7 lights moved picked → ordered (kitchen sink and faucet, master bath sinks, both shower heads, front door, and GENERAL / VERIFY "Bath hardware set" — which IS the list's towel-bar kit; caught before writing, not added twice), `odate` only where his own log dated the order, the replacement toilet (read off the Word file's picture) appended to the toilet's note, a history line per change. Proved add-only by a script (all 134 rows still there, no field lost, no note re-worded, no light moved back); the `.new` compared byte-for-byte through the mirror, the live file re-checked unchanged since his 08:05 save, the old copy → `App Data/Backups/materials-office-josten-842d53-2026-09-24-before-923-list.json`, the `.new` moved live, then opened in the real app (14 categories, 137 rows, 55 steps, no errors); the homeowner's file untouched (no 🏠 row changed). **Hazard noted, not fixed (offered):** `matAutoSave` writes the whole in-memory board with no version check, so a board left OPEN on another screen since before a change made elsewhere (a by-hand merge, Phil once he has the board) saves over it — a Dropbox `rev` check on save would catch it.
- Before that: **v7.07** (pushed 2026-09-23): 🎒 THE POCKET LIST UP TOP, AND ITS OWN PLATE ON THE RUNNING LOG. Eric: "When I click Summary, I want the pocket list to be on the top of the page, just underneath [NEEDS] YOU … Also, on the running log, one of the top search filters I want is pocket list. We can take the crew button off the running log search." **The summary:** `SUM_SECS` order is now ⚠ NEEDS YOU · 🎒 POCKET LIST (key `get`, was TO GET — the list today/tomorrow + what got done) · 🎒 POCKET — not done (`pocket`, the leftovers with their four plates) · PEOPLE · MONEY · PHOTOS · WIZARD · SCHEDULE · SENT TO LOGAN · THE RECORD; the page opens ON the pocket (the first of `get`/`pocket` holding anything, else the first heading holding anything, as before) and NEEDS YOU keeps its count on its heading. The list reads today's first, then tomorrow's (`pocketByDay()`, as on the pocket card), and a done row wears its ✓ once. **The running log:** `kindOf` gives a pocket note (`e.pocket` set, or a `pocket` tag) its own kind `pocket` (🎒), ahead of crew — so ⚙ Grinder is the grinder alone. The plates read ✳ All · ⚙ Grinder · 🎒 Pocket list / 🧙 Questions · 📱 Texts · 📧 Emails / 📷 Pictures · 🏠 Client · 💬 Feedback; the 👷 Crew plate is off (crew traffic is still under ✳ All, the person wheel picks one man, and a phone that had Crew lit or 🚫 lets it go on the next draw). Under 🎒 the items still ON the list lead (`ON THE LIST — …`, `today`/`tomorrow` in the time column; the search box narrows them, a hard filter hides them — they are not book entries), then every pocket note newest first, one 🎒 each (a note's own leading 🎒 is dropped); a leftover he cleared leads with what became of it — `back on the list — …` / `not needed — …` (`POCKET_WORD`), the word FIRST because a phone cuts the end of a long row. A tap on a live row = `rlPocketGo()`: the search window shuts, the pocket card scrolls into view lit (`.pk-flash`). Under ✳ All the live items are not in the feed. **Answered, not built** — "Pocket list acts just like the grinder, correct? Meaning it's all sorted, and I can look it up.": mostly — a checked-off, flushed or left-over item is a note on the log (tagged pocket; the Wizard reads it; the 🎒 plate finds it), but an item still on the list is not on the log yet, and pocket notes file under no job (GENERAL) — the Board's ⇄ puts one on a job. A job pick on the pocket offered, on his go. `tests/_test707.js` (18 checks); `_test676` re-pinned (the heading order); `_test678` / `_test682` test names only (TO GET → POCKET LIST).
- Before that: **v7.06** (pushed 2026-09-23): 📐 THE PLAN RACK — BIG DROP BOXES, FOUR STANDARD SLOTS. Eric: "On the plan rack, make the drag-and-drop areas bigger and have it always have whole project, trusses, windows, and cabinets. Categories as a default." **The slots:** `PK_SEED` is four — Whole project · Trusses · Windows · Cabinets. `pkHeal(d)` runs on every open and puts the four FIRST, in that order (found by key, else by the same name — a slot he once made by hand called "cabinets" IS Cabinets), his own slots after them in the order he made them; every set, note and history rides along untouched and NOTHING is written on open (the next real change saves it). `pkStd(si)` = slots 0–3: an empty standard slot wears no ✕; a standard slot holding a set has a ✕ that EMPTIES it on two taps (`⚠ SURE? Empties it — files stay`) and the slot stays; his own slots come off as before. ➕ slot refuses a name already on the rack. **The drop boxes** (`.pk-drop`, CSS beside `.fc-sec.rm-drop`): 3px dashed, a big ⬆, 17px words; 120px tall on the phone (was 62), 180px on the PC (was a 45px strip). The WHOLE slot card takes the drop (`pkDragOver` / `pkDragLeave` / `pkDrop`; a card with a set waiting for its note does not), and its box lights only for a FILE — a solid edge, a tinted plate, and the words turn to `⬇ LET GO — it goes in as Trusses R1`. On the PC the slots sit two across (`.pk-grid`) inside a 1100px column (`.pk-wrap`), so all four are on the screen at once — a file being dragged cannot scroll the page to the one below. Two files dropped at once = the first goes in, and a toast says so. The box is `role=button`; Enter opens the picker. The "what changed" box under a dropped set is a real writing box now (44px; it was a bare thin input). **Found on the way and fixed:** `pkStamp` cut a long file name at 60 characters and lost its ending — Ashman's R1 went up as `…1117 layou` with no `.pdf`, so `matOpenDoc` handed it over as octet-stream (v5.85b's "unrecognizable file that won't open", back again). The ending is now kept (only the name in front of it is shortened; a PDF with no ending gets `.pdf`), and `matOpenDoc` tells a paper by its first bytes (`%PDF-`, JPEG, PNG) when its name has no ending — so Ashman's set opens with no change to the data. The real racks on 2026-09-23 (local mirror, read-only): Ashman and Gilbreath held the standard three; Scritchfield held whole, windows, trusses + his own Kitchen (it now opens whole, trusses, windows, cabinets, kitchen). `tests/_test706.js` (28 checks: the phone and a 1280px PC, real DragEvents carrying a File, old racks healed, zero writes on open).
- Before that: **v7.05** (pushed 2026-09-22): 🧮 A TRACKER JOB CAN NAME ITS QUICKBOOKS JOB. Eric: "if carricks fourplex numbers are in there make sure thats taken out." One QuickBooks customer can carry more than one job — Carrick's customer also carries an older, separate job — and matching on the customer alone had been adding that job into the duplex in three places: his homeowner page (invoiced and paid, plus the Interior finish phase bar, because the July phase sort had it inside), the books receipt row, and the 📊 Project Tracker. `trackerFresh` now matches a tracker entry whose `customer` reads "Customer:Job" on the WHOLE name (`mine(r, cust)`), so only that job's invoices count; a bare customer name still counts every job under it, exactly as before; and a job-level entry also holds its top-level customer in the one-customer auto-join (`taken`), so a figure that matches the whole customer cannot join as a job of its own. `tests/_test705.js` (6 checks, made-up names). **Data fixed by hand the same night, create-first through the Dropbox connector** (each `.new` compared byte-for-byte through the local mirror, the live file re-checked against what it was built from, the old copy moved to `Backups/` or `Archive/` under a `before-4plex-fix` name, then the `.new` moved live): `Client Portal/carrick-2bcf5c.json` (invoiced, paid and the Interior finish phase bar; open and every other key untouched — the live client-portal function was read back afterwards), `qb-refresh.json` (Carrick's row and its why), `qb-refresh-state.json` (a CARRY-THIS note for the Tuesday chain: match that page by customer:job, never the whole customer), `project-tracker.html` (Carrick's entry names the job; the older job's invoice, its share of the phase bar and of the tax gap are out, and phases + tosort + gap still equal invoiced). **Found on the way:** in the Sales by Product/Service Detail the markup lines sit under the BLANK product/service heading ("Total for --"), not under Utility Connections — a parser that carries the last heading forward mislabels them (the books run's notes already say so; the costs were never affected here because markup lines are dropped by their memo). The Tuesday chain ran the same morning off the 09-18 exports; invoices written 09-21 ride into the 09-29 run.
- Before that: **v7.04** (pushed 2026-09-21): 🔗 AN ENTRY NUMBER IN THE WIZARD'S ANSWER OPENS THE ENTRY. Eric: "on the summary it has the entry number which i really like but i think i should be able to click on that and see the entry." The Wizard cites its records in words — `entry 3667`, `(entry 3667, 3666)`, `entries 12 and 15`, `Entry #14`, `(entry 2720, repeated 2716)` (seen in his kept answers; the prompt only asks for the final `SOURCES:` line, the inline numbers are the model's own habit). `wizLinkEntries(html)` runs over each answer line after `mdToHtml` (in `wizLinesHtml`, so the card, the full screen and a redraw all get it): every number that follows the word entry/entries — singly or in a list — AND is a record in `entries` becomes `<a class="wl-ent">` (underlined + heavy + brass, never the colour alone); a bare `#1330` is an invoice and stays plain, a number that is no longer in the log stays plain. `wizEntryOpen(id)` draws THE ENTRY into `#wizEntHost` (a child of `body`, `.we-sheet` z 330 — over the full-screen answer at 320, which stays where it was; at the TOP of the screen): `ENTRY n` · the day and time · kind · job · who · the amount / hours / miles · 📧 / 💬 · `🔒 personal — it stays on this phone`; the WHOLE of the words (`pre-wrap`); what was read off its photo and his corrections (`e.ai`); its tags; `📷 See the photo(s)` (`openEntryPhoto`); closes by ✕, `‹ Back to the answer`, or the dim edge (`wizEntryClose`). The link stops its click, so on a PC it opens the entry and NOT the line's fix box (v7.03's click-anywhere). **Found on the way and fixed (stacking):** a photo opened from the full-screen answer (`📷 See the photo it read`) had been opening UNDER it — the lightbox was z 100, the answer 320 — and a toast (z 90) was never seen there either, so v7.02's "✎ Kept — 2 waiting" went unseen on the full screen: `#lightbox` is 340, `#busyBand` 350, `#toast` 360 (`!important`, the base rules sit later in the sheet). `tests/_test704.js` (12 checks, phone and a 1440px PC with a real mouse). **Also 2026-09-21 — Carrick's account summary redone on his word** (`boiler-room-tools/.carrick/summary.mjs`, not in this repo): "make sure my markup is built into any numbers such as the plumbers bill. and don't mention markup, its all built in to the clients eyes. also take the late fee off, everything below the total left to pay take off" and "crew labor was phil doing exterior paint, add that" — the page now follows the client-side hard rule (markup folded in, no vendor names), shows each open invoice at its ORIGINAL balance (proved from the late-fee arithmetic: each 1.5% fee = its invoice's balance before the fee), and stops at the total. QuickBooks itself still carries the late fees and the extra tax that posted with them — Logan has to take those off for the books to match the page.
- Before that: **v7.03** (pushed 2026-09-20): 🖥 ON THE PC THE PENCIL LEADS ITS LINE. Eric: "on the phone the edit buttons on the summary works great but on the pc its hard to follow across and click the right edit pencil as is far apart on the big screen." The Wizard's answer draws each line as words + a ✎ at the far right — fine at 390px, a screen-width apart on a monitor. CSS only, behind `@media (min-width: 701px)`: the ✎ is FIRST on the line (`.wl-line .wl-x { order: -1 }`, right where the eye starts), the answer keeps a readable width (`.wl-list` 880px, the save bar in the foot too), the line under the mouse lights as ONE band with its pencil (`.wl-line:hover` — tinted plate, the words at full strength, the pencil's edge brass and heavier), and the fix box and a kept fix sit under the WORDS (`margin-left: 56px`). And there is nothing to aim at: a click ANYWHERE on the line opens its fix (`wizLineTap(i)` on the row — it acts only at PC width; the pencil's own click stops there so it does not open-and-shut). The phone is untouched: the pencil at the right, a tap on the words does nothing. `tests/_test703.js` (9 checks: a 1440px window and a REAL mouse — the gap between pencil and words, the hover band, the click on the words, one click on the pencil; then the phone). **Also answered the same evening — "do a quick report on this app":** the numbers, from the repo and his Dropbox on 2026-09-20: first log entry 2026-07-21; first GitHub commit 2026-07-30 (v4.74); 252 commits on 33 days (151 in August), v4.74 → v7.03; `index.html` 21,097 lines / 1.44 MB (0.44 MB gzipped — less than one of his receipt photos), about 1.1 MB of it JavaScript in ~1,115 named functions; `c/index.html` 998 lines; function sources 930 lines; 77 test suites, ~13,000 lines, ~1,430 checks (0.94 MB — nearly the size of the app's own script); the whole site folder 7.7 MB, 3 MB of it the steampunk art.
- Before that: **v7.02** (pushed 2026-09-20): ✎ WORK THROUGH THE WHOLE ANSWER, SAVE AT THE END. Eric: "when i ask for a summary and i edit the line id like to save it but not have it re ask the wizard right away, i want to work through the whole summary and make multiple edits and then save at the end." v6.83 saved each line fix on the spot and asked the question again AT ONCE, so the second fix had to be made on a new answer and a summary with five wrong lines meant five waits. **Now a line fix is KEPT on the page:** `wizLineFix(i)` offers `✓ Keep this fix` (and says nothing goes to the Wizard until he saves at the end); `wizLineSave(i)` only adds it to `_wizPend` (`{i, line, t}`, with `_wizPendCtx` = the answer it belongs to: `key` = the kept row's `at`, `q`, `srcIds`, `keep`) — NOTHING is written and nothing is asked; the line wears `✎ KEPT — not saved yet: …` (`.wl-fix.pend`, a dashed edge AND the words); ✎ again on that line shows his words to change (it replaces, never doubles) or `🗑 Take it off` (`wizLineDrop`). **One bar** (`wizPendBarInner`) — in the FOOT of the full screen (`#wfPend` inside `.wf-foot`, so it is always in reach; the in-list copy is hidden there) and under the lines on the main-page card — reads `✎ n fixes kept — not saved yet` with `✓ SAVE n FIXES`, `🧙 Save and ask again` and `✕ Drop them` (two taps, `⚠ SURE?` four seconds). **`wizPendSave(again, leaving)`:** every kept fix lands where a fix always did — the records the answer came from (`ERIC CORRECTED (re "line"): fix`), the kept answer's `fix[]` with its line, `prefs.wizFixes` (the list the Wizard reads on every question) — through `applyCorrection(corr, line, quiet)` (the new `quiet` skips the per-fix log note) and then ONE note in the log for the lot (`✎ 3 corrections — re "<question>": "line" → fix · …`; a single fix still writes the old one-line `✎ Correction — re "line": fix`); the answer STAYS UP, the lines now read `✎ Eric: …`; with `again` the question goes back ONCE with every fix in. **Nothing typed is lost:** the kept fixes ride between the card and the full screen (`wizPendRefresh` redraws every `.wl-list` that has no box open), are mirrored on the phone (`daylog-wizpend`, read back by `wizPendLoad` — reopening that kept answer after a reload shows them), and whenever the answer is about to go away they are SAVED FIRST and he is told (`‹ Back` = `closeWizFull`, the ✕ on the card = `hideInlineAnswer`, and a DIFFERENT answer coming up — `wizLinesHtml` sees another key — in which case they go onto the answer they belong to, not the new one: `_lastAnswer` is swapped for the save and put back). The whole-answer `✎ Wrong? Correct it` is as it was (saved on the spot, its own note, never asked again). `tests/_test702.js` (17 checks); `_test683` re-pinned (the button's words; `wizPendSave(true)` after the keep does what the one button used to do per line).
- Before that: **v7.01** (pushed 2026-09-20): 🔔 THE HOURS REMINDER. Eric: "also i need some kind of reminder to look at payroll every week or two weeks, every week is better for the summary." Two halves. **(1) A count on the 👷 Crew portal plate** (`#scCrewN`, the Summary plate's corner badge; `chBadge()`): pay periods still to approve or to send + new time-clock files the email pipe has put in Dropbox; nothing when all is sent. It is refreshed from `chSaveSoon`, `chScan`, and once a session from the Dropbox pull (`pullRemote` → `chBoot()`: the badge, `chLoad()`, and — no more than once every SIX HOURS per device, `daylog-chscan-at` — a look in the QB Exports folder). **(2) A push to his own phone.** `fnsrc/hours-reminder.mjs` (bundled; `build-functions.mjs` ENTRIES now includes it) wakes every hour (`netlify.toml` `schedule = "0 * * * *"`), READS `App Data/hours-reminder.json` — which the app owns and the Crew hours window writes — and WRITES only its own `App Data/hours-reminder-state.json` (the day it last rang). The deciding is `fnsrc/hours-reminder-core.mjs` (no imports; the suite runs it in node): `due(settings, state, now)` says yes ONCE — `on === true`, his weekday, his Alaska hour (`alaskaParts` via Intl America/Anchorage, so summer and winter are both right), not already rung today, and with `every: 2` only in the week right AFTER a pay period ends (the Saturday on or before today = `anchor` + whole fortnights; `anchor` = the newest pay period's Saturday, written by the app) — and FAILS CLOSED: no file, OFF or a broken setting is a no. It rings `sendToAll` = Eric's own devices (`App Data/push-subs.json`; a crew phone's subscription lives in that man's folder). The words are fixed — `⏱ Crew hours` / `Time to look at the crew's hours for the week. Open Boiler Room, then Crew portal, then Crew hours.` — no name, no dollars, no number at all (asserted). **In the app** (the foot of the Crew hours window, `chRemHtml`): `🔔 REMINDER — a push to your phone` · `OFF · ✓ EVERY WEEK · EVERY 2 WEEKS` · `on <day> at <hour> Alaska time` (`chRemSet`, `chRemSave`, `CH_REM_PATH`). He asked for a reminder, so the FIRST time the window opens with no settings file it is turned ON — every week, Fridays at 8 am (the morning after Phil's Thursday recap lands) — and SAID in a toast; a file that is already there is read, never reset; offline nothing is assumed and nothing is written. Every 2 weeks with no pay period on the page falls back to weekly in words. It points to Setup → 🔔 Notifications for the phone's own switch. Never a crew phone (no door, no read, no write, no count — a real crew reload asserted). `tests/_test701.js` (18 checks). **Checking the timer without a Netlify login:** GET `/.netlify/functions/hours-reminder` → 403 = deployed AND on the timer. **Still on his go (told 2026-09-20):** Phil's weekly JOB RECAP has never been caught by v6.58's reader in his real data (see the v7.00 note) — rebuild `prefs.laborWeeks` from the archived `/Texts/email -job recap…` files, accept the `[TEST]` prefix; then the Friday reminder has the week's homeowner paragraph waiting in the 📖 Journal window.
- Before that: **v7.00** (pushed 2026-09-20): ⏱ CREW HOURS — BY WEEK, A SPLIT DAY, TWO KINDS OF NOTE. Eric, first evening with v6.99: "i really like the layout of the week calendar and how each box gets filled in. i also need more then just by job and by person, i also need by week, and i think if there is more than one job per day, the calendar box for the day could be split into 2 or more and hours are on each job and could have job when mouse hover and could even be two differnent shades or colors ? maybe just a slash mark to split the day into multiple jobs. also think i shold be able to click on the day or hours or in somewaay be able to see the categories worked as well and be able to put notes on it even if those notes are onlly for my record, make those sepereate from hour fixes notes i send to logan to fix payroll excel." **BY WEEK** is the first tab and the default (`_chView` = `daylog-chview` on this device, `chSetView`; three tabs BY WEEK · BY JOB · BY PERSON): the period's jobs lead it (`.ch-legend`), each wearing a LETTER (`chTags(P)`: A = the most hours, in `chJobs` order; the hours with no job wear `?`) with its hours and its OK box (`okBtn` — he can approve from here; the name is a button → `chJobOpen(i)` = BY JOB with that job open); then a calendar a week (`.ch-week[data-week]`): a row a person who has hours that week with ALL his jobs in it, the week's total, and an `everyone` row of the days. **The split day** (`wcell`, `chDayParts(P, pp, d, tags)`): one job = a plain box, the hours and the job's letter (`.ch-part.one`, the letter in the corner above the hours; four characters like 10.5 are set tighter so nothing is covered in a 29px box — asserted: no part overflows at 390px); two or more = the box SPLIT by a line (`.ch-c.split`), a part a job in letter order — its hours, its letter, one of two shades (`s0`/`s1` by the letter's place; the LETTER tells them apart, never the shade alone), the day's total under (`em`); `title` = the hover he asked for (`Ann · Mon 10/5 — A Oak House 4.0 h · B Pine Cabin 6.5 h`) and the same words in the `aria-label`. On a PC (`min-width: 701px`) the calendar keeps a readable width (800px) and a part's letter sits right beside its hours. The same letters ride on the BY JOB headings and the BY PERSON job lines. **THE DAY** — a tap on ANY day box in any view (`chNoteOpen(pi, d, focus)` → `chSheetDraw()` into `#chSheetHost`, a child of `body`: a fixed box inside a scrolling one is at the mercy of its parent — the Build List's edit window learned it — and it sits at the TOP of the screen so the phone's keyboard never covers the writing boxes; z 85, over the window, under the toast): who and when, the hours, and under each job the WORK TYPES he logged that day with their hours (and the time clock's own Notes cell when it has one); an EMPTY day opens too (`no hours this day`) — that is where a missing day gets its fix. **Two boxes that never mix:** `✎ A FIX FOR LOGAN` (`P.notes`, as v6.99: rides with the file in `chSendWords`; the hours are still never edited here) and `● MY NOTE` (`P.mine` — his record only). `chNoteSave()` keeps both from the one `✓ DONE — it is saved` plate, and a tap on the dim edge saves too (nothing typed is lost); an emptied box takes only that note off; `chNoteDel(id, key)`. The day's box wears ✎ in its top corner and ● in its bottom corner (+ the accent edge / an under-line, + the words `has a fix for Logan · has your own note`) in every view; the page lists them apart — `✎ FIXES FOR LOGAN — they ride with the file` and `● MY NOTES — only you see these, never sent` — with `➕ a fix for Logan` / `➕ a note for me` for the whole pay period. **THE WALL (asserted):** MY NOTE is in none of what goes to Logan (the words, the text file), not in the 📗 log note, not in the master log; it IS in his own `App Data/crew-hours.json` so his other devices show it; the store has one way in (`chStore`) and nothing outside the Crew Hours block touches it; a newer file for the same period keeps BOTH kinds of note. With a pay period on the page the drop box moved UNDER it (the calendar is what he came for; `📥 FOUND IN DROPBOX` still leads). `tests/_test700.js` (25 checks, its own .xlsx built in the page, made-up names); `_test699` re-pinned (it walks BY JOB / BY PERSON; the note lives in THE DAY window). Rehearsed on his real 9/12 file, phone and PC: eight split days read right (three jobs in one day on Wed 9/2). **Found while looking at the recap PDF he sent the same evening ("i can also get these week summaries from phils app easily. this could be used to grab for the client page week journal") — TOLD to him, NOT fixed, on his go:** that PDF is the Thursday recap EMAIL's twin, and v6.58 already reads the email (hours by job / work type / person, the homeowner paragraph offered in the 📖 Journal window as ⤵ Build on it) — but in his data it has NEVER caught: `prefs.laborWeeks` is empty. The Aug 30 email was logged as a plain email (judged by the cloud, 09-10, likely before the phone had v6.58); the Sep 6 one was read and archived to `/Texts/` yet left neither an entry nor a week (entries.json is last-writer-wins — most likely written over during the 09-14…09-18 catch-up); today's came as `[TEST] Job recap…` and `LABOR_RECAP_SUBJ` wants the subject to START with `Job recap`. All three emails are still in `/Texts/email -job recap, week of ….txt`. Offered: make `prefs.laborWeeks` a cache that REBUILDS from those archived files (so a lost week heals), accept the `[TEST]` prefix, and no PDF reader is needed — the email carries the same words.
- Before that: **v6.99** (pushed 2026-09-20): ⏱ CREW HOURS — THE TIME CLOCK'S PAY-PERIOD FILE, BY JOB, FOUR LIGHTS. Eric: "i want to make a place for me to upload an excel spreadsheet from phils time clock app and have it readable on my boiler room … arrange by job and week and just summarize the week so I can easily, quickly see hours and missing hours or days and approve and pass onto logan and want to have the cube lights for: need to gather and approve hours, approved, sent to logan, paid … not looking to rebuild boiler room time clock or replicate phils app." A mock-up drawn from his real file and four suggestions went back; his answers: "lets do every two weeks pay period" · "[Logan's portal address] is the email to get things to logan portal, lets make that how i send the excel sheet" · "i think it shoudbe on the crew portal button at the top." The block sits right after `crewBadgeTap` (`// ⏱ v6.99 — CREW HOURS`). **The file** is the time clock's own `Clore-PPE-<the Saturday the period ends>.xlsx` (+ a `-detail.csv` twin): a summary block (reg/OT hours a week — and pay columns, which are EMPTY today), then `Employee · Project · Service · Billable? · SUN…SAT · Total · SUN…SAT · Total · Notes`, a person per `Last, First (L1|L2|L3)` row with blank-name rows under him, `Total:` closing him, `WARNING:` lines at the foot; projects are QUICKBOOKS job names, services QB codes, `UNKNOWN PROJECT` / `XXX - UNKNOWN SERVICE` when the man picked none. The email pipe already saves it into `/Clore Project Tracker/QB Exports/`. **The door:** the FIRST plate in the 👷 Crew portal sheet (`crewViewMenu`, `#chDoor`, `chDoorWord()` = `1 to approve · 1 to send · 📥 new file`) → `openCrewHours()` (the `#revModal` full window, `renderCrewHours`). **Getting it in:** `chScan()` lists QB Exports, takes the newest copy of each period (`CH_FILE_RE`), and OFFERS what is new or newer by `content_hash` (`📥 FOUND IN DROPBOX … tap to bring it in`, ✕ = never offer that one again, `skip`) — his tap, never by itself; or the drop box (`chDrop`). `chXlsxRows` = `matZipEntry` (v6.89) + shared strings + sheet 1; `chCsvRows`; `chParse` walks it by HEADING NAMES, works the fourteen dates out from the period's end (title line, else file name, else the dates row; a period that does not end on a Saturday, a missing week, a non-PPE file are refused in words). **THE WAGE WALL:** only the hour columns are read by name (`Reg/OT Hrs Wk1/2`, `Total OT Hrs`, `QOT Hrs`) — whatever sits right of them (gross pay, taxes, cost an hour) is never looked at, so it cannot be kept (asserted with a file that HAS pay figures). **Kept:** the ORIGINAL bytes in `App Data/Time Clock/<name>` (a changed file is kept BESIDE the first, never over it) and the parsed period + his marks in `App Data/crew-hours.json` (`chStore`, the newest eight mirrored in `daylog-crewhours`; `chSaveNow` is read-modify-write so periods only in the file survive; `gone` keeps a removed one removed). **The page:** `PAY PERIOD ENDING SAT m/d` · the four lights (`CH_LIGHTS`, `.ch-cube`: icon + word + ✓, `aria-pressed`, the state in a sentence under them) · `⚠ NEEDS A LOOK` (`chFlags` — facts off the file, nothing guessed: hours with no job, no work type, a day over 12 h, a Mon–Thu nobody has hours, the file's own no-pay-grade warning, `🔁 changed since you OK'd it`; an empty Friday is NOT a flag — the crew works four tens and the day boxes show every hole) · BY JOB | BY PERSON. BY JOB (`chJobs`): a fold a job (the QB prefix and `[number]` off the name for reading, the full name in the title, NO JOB ON IT last), inside it each week that has hours — a row a person with seven day boxes (`.ch-c`, a number or a dot) and the week's total, the work types under. BY PERSON: his two weeks job by job and what the clock's own `Total OT Hrs` says (it is MORE than the two weeks' OT added — a week over forty counts). **The lights are the PAY PERIOD's** (his call): he OKs a JOB with one tap (`chOk`, `✓ OK every job — n left` with Undo, the no-job hours `⚠ OK AS IS?`); ② APPROVED lights BY ITSELF when every job is OK'd (`chApCheck`) and goes out if one is reopened; ③ 📗 TO LOGAN = `chSend()`; ④ 💵 PAID = his tap (Undo). A light comes off only from the far end and only on a second tap (`chArmed`, `⚠ SURE?` four seconds); once sent the OK boxes are locked in words. **Read, never edited** (his question — "or should we make it so when i change things on it on the app it changes the excel"; answered no: the time clock stays the one true record, two versions of the hours is how payroll goes wrong): a tap on a day box opens `✎ A note for Logan — Bo · Tue 9/29` (`chNoteOpen/Save`, the box wears ✎); notes ride to Logan; the fix is made in the time clock, and a NEWER FILE for the same period whose hours differ (`chSig`, `chDiff`) drops it back to ①, clears ONLY the changed jobs' OKs, keeps his notes, says exactly what moved (`Ann · Oak House · Tue 9/29: 10.0 → 8.0 h`) and writes into the history that it HAD been sent. **📗 Send** (`chSend`): the phone's send sheet must ride his tap (the rule `toBookkeeper` learned), so the original bytes are fetched BEFORE he taps (`chWarm`, `chDownloadBuf`); two files go — the Excel exactly as it came (same name, same bytes, asserted) and `… - approved by Eric.txt` (`chSendWords`: approved + unchanged, each person's two weeks and the clock's overtime, his notes, what is worth a look); a web page cannot fill the To line when a file rides along, so Logan's address is COPIED for one paste; no send sheet (a PC browser) → both files download and `mailto:` opens with address, subject and words, and because only he knows whether it went, `Did it go? ✓ Yes — mark it SENT`. Sent = ③ + a `📗 Crew hours → Logan` note under Internal / Admin tagged Bookkeeper (`chRef`), so it shows under SENT TO LOGAN. **Logan's address is typed ONCE into `prefs.loganMail` and is NEVER in this repo** — the site is public and the address files whatever it is sent straight into his portal (asserted: the address is not in `index.html`). **Walls:** Eric's phone only — every door returns on `CREW_NAME` and the preview (a real crew reload asserted: no door, no read, no write); nothing here reaches a client page, a push, the Wizard or a crew file. `tests/_test699.js` (36 checks — builds its own time-clock .xlsx in the page, a real zip with shared strings, deflated and stored; every name and hour made up); `_test692` re-pinned (its source check counted three Dropbox calls that wait on a 429 — `chDownloadBuf` is a fourth and waits the same way). Rehearsed on his two real files (read-only): both read to the time clock's own totals. **Not built, on his go:** approved job-weeks → the receipts window's 👷 CREW LABOR / the client's ESTIMATED UPCOMING COSTS priced exactly (the file has person × job × work type — no blended tier as in v6.58); QB job names → app jobs by a one-time exact pick; 💵 pre-lit from the QuickBooks payroll rows for his tap; a one-tap robot send through a Zap from his own Gmail (no pasting, the same on a PC); a row on the Summary page when a period waits; tap-to-learn a person's normal days so a missing one is flagged; the Wizard knowing the hours.
- Before that: **v6.98** (pushed 2026-09-20): ⇄ ON EVERY BUILD LIST ROW, ALL THE TIME. Eric: "how do i swith from checklist to item to pick and order" — the ⇄ plate (v6.91) left the row the moment one of its lights was lit and hid in the open row's chips and the ✎ edit window, so he could not find it; offered on every row, and he said "yes make it show on every row all the time". `matRowBtnsHtml(it)` now ALWAYS draws `.mat-flip` right after `⚠ 1st` — the same place on every row (`⇄ list` on a thing to buy, `⇄ buy` on a checklist row). **One tap when nothing is lost** (a tag or a schedule is nothing to lose — `matBuyToggle` carries `tg`, `sch`, `schd` across). **A row with PROGRESS the flip would re-map arms first:** `matFlipRisky(it)` = a thing to buy past `pick` or installed, a checklist row in work or done → `matFlipTap(id)` arms the plate (`_matFlipArm`, `_matFlipT`: `⚠ sure?` in words, a solid orange edge, the label says "tap again to flip it", four seconds, then it lets go by itself with nothing changed — a stray thumb cannot turn an ordered faucet into a to-do); the second tap flips it and the light is carried across as best it fits (ordered → in work, installed → done). **A 🏠 homeowner row wears the plate too** — dimmed (`.is-off`), a 🏠 on it, `aria-disabled` — and a tap says in words why it stays something to buy ("take it off their page first (✎ edit → 🏠 The homeowner)"); nothing changes. All seven plates of a thing to buy still sit on ONE line at 390px (asserted). `▸ ? how this works` says it the new way. `tests/_test698.js` (11 checks); `_test691` re-pinned (the plate no longer leaves a lit row). **Not built, on his go (unchanged):** the grinder's photo riding onto the row, a row from the BOARD tab's ⇄ wheel, picking 🛒 something-to-buy right in the WHERE window, a per-category "these are all checklist items" flip.
- Before that: **v6.97** (pushed 2026-09-19): THE GRINDER → THE BUILD LIST. Eric: "how can i put something into the grinder and have it land on the build list?" There was no door (a row came only from the board itself or 📋 Paste a list); the door was offered and he said "go". **The chip:** `qnBLChipHtml()` draws `📋 Build List` in ④ TAG IT beside ⚠ Heads-up and 🧾 Receipt (never on a crew phone — the boards live in Eric's Dropbox); `qnBLToggle()` wants the job in ① first ("▲ Pick the job first"), refuses a 🔒 personal note, finds the client by `blClientIdx(job)` = the portal client whose `portalAppJob(c)` IS that job (no client page → it says so, nothing guessed), reads the board and opens **the WHERE window** in the receipt window's own modal (`blModalOpen` in `#catModal`: that job's categories as same-size plates with their row counts, `#blNewRoom` for a new one, `✕ Not for the Build List`); `qnBLPick(name)` lights the chip `📋 KITCHEN — tap to change`, step ④ counts as done and its folded line reads `· 📋 Build List → KITCHEN`. The wheel turning under a pick lets the pick go in words (`qnBLJobChanged`, also re-checked at SEND); lit with nothing picked, SEND opens the window instead of saving. **SEND** (`saveNoteFrom`): the note is filed exactly as before, then `grindToBuildList(e, job, room)` → `blPutRow`: a ✅ checklist row (`s:'todo'`, not `buy`, not `hm`; ⇄ on the row makes it something to buy) — the FIRST LINE is the name (80), the rest the note (`sel`, joined with ` · `, 300; one long line = the name is its start and the whole of it is the note), `eid` = the entry, `src:'grinder'`, `gat`; the id minted the way `matNextId` does; the board's history gets `⚙ from the grinder → KITCHEN: …`; the entry gets `matRef {code, id, room}`. If that board is OPEN on this screen the row lands in `_matD` and it saves itself; otherwise the office file is read, the row added and ONLY `materials-office-<code>.json` written (`full:true`) — never the homeowner's file. Rows go on ONE AT A TIME (`_blLine`, a promise chain): each is a read-modify-write and two in the same breath would cover each other (asserted). The working band says `📋 PUTTING IT ON THE BUILD LIST…` → `📋 ✓ ON THE BUILD LIST — KITCHEN`; the saved toast reads `Note saved · Oak House · 📋 Build List → KITCHEN ✓` and its ↩ Undo takes the row off the board too (`grindBuildListUndo`, through the same line). If the board cannot be written the NOTE is still saved and he is told the row did not make it. **Seen afterwards:** the board row's trail says `⚙ from the grinder`; the log row wears `📋 Build List → KITCHEN` and the tap opens that board (`openBuildListOf(code)`). **The wall:** the client cut now also strips `eid`, `src`, `gat` (with `sel`, costs, `ec`, `first`). **The refactor under it:** the board's READ came out of `openMaterials` word for word as `matBoardRead(c)` (same two files, same merge of the homeowner's picks and remarks, same stamps; what it learned rides back as `_log` and `openMaterials` writes it to the history as before) — so the grinder can add a row without the board being open. `tests/_test697.js` (21 checks); 689/691 and the grinder suites unchanged and green. **Not built, on his go:** the grinder's photo riding onto the row, a row from the BOARD tab's ⇄ wheel, picking 🛒 something-to-buy right in the window.
- Before that: **v6.96** (pushed 2026-09-19): ┄ IN THE CLIENT PREVIEW, WHAT ONLY HE SEES WEARS A BRIGHT DASHED EDGE. Eric: "anytime im in the client preview i want anything they cant see to have a dashed border, bright, even the buttons that are for me." The owner bars (v6.61) were brass — the page's OWN colour — so his buttons read like part of the homeowner's page, and a hidden card was dimmed rim and all. `c/index.html` only: a new token `--mine` (a bright cyan on the dark page, a strong blue on the light one — a colour the page never uses; 3:1 or better against the page in both themes, asserted) and ONE sign for "only you see this": `2.5px dashed var(--mine)` on the OWNER VIEW line, on every `.own-bar button` and `.own-inline` (✓ THEY SEE THIS / ○ HIDDEN, ✎ Edit, ➕ Add photos, the tile switches, the per-board switches), a `3px dashed` rim on everything `.own-off` (a hidden card, tile or option board, the empty-budget stand-in) — the rim now stays at FULL brightness and only what is inside dims (`.own-off > *:not(.own-bar):not(.own-tag) { opacity: .5 }`; it used to dim the rim too); the tile's `○ HIDDEN FROM THEM` strip wears the edge as well. A thing they DO see keeps its plain edge, so the sign means one thing. The line at the top says it in words ("A bright dashed edge, like this one, means ONLY YOU see it") — never the colour alone. The homeowner's own page draws none of it (asserted: not one dashed `--mine` edge anywhere). `tests/_test696.js` (10 checks: the real page in a frame behind a tiny HTTP server, both themes, the Options window, and the plain homeowner page). **Asked the same day, ANSWERED and PARKED for his go — "how can i put something into the grinder and have it land on the build list?":** there is no door today (the Build List is filled on the board itself, by 📋 Paste a list, or from the master). Offered: a `📋 Build List` chip in the grinder's ④ TAG IT that lights when the job in ① has a client page → a wheel of that job's categories (+ a new one) → on SEND the note is logged as always AND lands as a row in that category (a checklist item by default, ⇄ flips it; the first line is the name, the rest the note), the row and the log entry carrying each other's id. Build on his go.
- Before that: **v6.95** (pushed 2026-09-19): THE FOLD STAYS UNDER HIS MOUSE; A WHEEL THAT HOLDS A FILTER IS LIT. Eric: "whenever i click on a drop down menu make sure when the card opens it doesnt change in relation to where my mouse is. it makes it jump and i have to move my mouse to get to the drop down again" and "on the running log where i can select a person in a drop down list, when someone is selected make sure its highlighted to show that filter is on." ("Drop down" is his word for a FOLD — the summary, sort and board headings, a Build List category; "drop down list" is a real `<select>`.) **The jump:** most folds are one-open-at-a-time — opening the fourth heading shuts the second, what was between collapses, and the heading he clicked slides out from under the pointer. **The fix is ONE listener for the whole app** (`foldAnchor`, an IIFE right after `modalOpenSync`): a capture-phase click on `[aria-expanded]` (every fold header carries it) or `[onclick^="gFold("]` / `[onclick^="brdOpen("]` notes the heading's place on the screen; after the handlers have redrawn (0 ms, and again at 120 ms for a late one) it finds the same heading — the same node, else its id, else the element with the SAME `onclick` words (the nth of them) — and scrolls the window it lives in (`scrollerOf`: the nearest `overflow-y: auto|scroll` ancestor, else the page) by exactly the difference. **When the window cannot scroll that far** (he is at its top and a card above shut), the rest is made up with blank room at the top of the card (`padding-top` on the scroller's first child — `#revBox` for the review windows), which MELTS as he scrolls down (a capture-phase `scroll` listener takes the pad and the scroll down together, so nothing on screen shifts) and is cleared whenever a window opens or closes (`foldPadReset()` from `modalOpenSync`). It never touches a plain button, so a control that scrolls on purpose still does. Families are untouched — no per-fold code, no state changes. **The lit wheel:** the person wheel under the running log's kind plates (`#rlWhoSel`) wears `.sel-on` when `prefs.rlWho` is set — a 2.5px brass edge, a tinted plate, heavy words — and says it: the picked line reads `✓ ONLY Phil` (options now carry explicit `value`s), the first line turns into `✕ everyone — clear this filter`, the `aria-label` says `person filter ON — only Phil`. The search window's wheels and boxes do the same from `renderAskRecent` (`lfJob/lfType/lfWho/lfCat/lfTag/lfFrom/lfTo/logSearch` → `.sel-on`, and the field's label gains ` ✓ ON`) — brightness + glyph + word, never colour alone. `tests/_test695.js` (14 checks, a 1280×800 window and REAL mouse clicks: a tall card open above and the clicked heading still under the pointer; shut again on the same spot; the top-of-window case with the blank room; the melt; the reset on close; the board; the wheels). **Hertz, DONE 2026-09-19 on Eric's "yes"** (the QuickBooks job under the Hertz customer IS the "Hertz Laundry/pantry" page): by hand through the Dropbox connector, create-first each time (`.new` → compared byte-for-byte through the local mirror → the live file re-checked against what it was built from → old copy moved to `Backups/`/`Archive/` → `.new` moved live): `Client Portal/hertzlaunpan-365733.json` (ONLY invoiced / paid / open / updated — from the one ledger invoice, paid in full; his journal week and every other key byte-identical; `phases` left empty — the phase label map is not ours to guess), `qb-refresh.json` (a tenth row for Hertz, `ranAt` moved so the banner returns, the done "drop the exports" item off `needsEric`), `qb-refresh-state.json` notes (Hertz = that customer, CARRY IT ON EVERY RUN; the receipt must list every code in `index.json`; where the 09-18 exports now sit and that they are deliberately not in `seen`). Checked through the app's own window on the real files: `📚 BOOKS UPDATED · 9 of 10 client budgets match the books · 1 not in QuickBooks yet — every page is current`. **Next:** the tags switch + 🧹 tidy list, the two extra email sorts, Phil's time-clock note, the museum page.
- Before that: **v6.94** (pushed 2026-09-19): THE CLIENT BUDGETS WINDOW CHECKS THE PAGES ITSELF. Eric, with a picture of 📚 Client budgets reading "⚠ 1 of 9 client budgets refreshed · 8 did not" and eight rows of "✕ NOT UPDATED — unchanged since the 09-15 rebuild — not rewritten": "i think theres still problems with budgets updating? check through the process start to finish and see." **Start to finish, read-only (`scratchpad/qb-endtoend.mjs`):** the newest Invoice List in `QB Exports` (All Dates, 230 invoices) = `qb-ledger.json` ROW FOR ROW; eight client pages tie to the ledger to the cent on invoiced, paid and open (the customer found by an exact lifetime tie, never a name guess); the receipt's numbers = the pages'. So the budgets WERE right and the WINDOW was wrong: since v6.18 it has counted a page as good only when the run REWROTE it (`updated === true`), and both the 09-15 chain and the 09-18 by-hand run rightly leave a page alone when nothing moved in QuickBooks — so "checked, agrees, left alone" read ✕ NOT UPDATED and the banner said ⚠ BOOKS PART-UPDATED every week. **And it hid the one real miss:** the portal has TEN pages, the receipt lists NINE — `hertzlaunpan-365733` was made after the run's client list, no run has ever been told about it, its page shows 0 / 0 / 0 with `show.money` on, while QuickBooks holds a Hertz customer with one invoice from July, paid in full, under a differently named job. A page that is not on the receipt could not show in a window that only reads the receipt. (Gilbreath: the name appears in NONE of the three exports — nothing has been billed; its zeros are right.) **The fix (app):** `qbFreshVerify(force)` reads `Client Portal/index.json` and every `<code>.json` (reads only — asserted: zero writes) and `qbFreshRows()` gives each row a STATE from the FIGURES, to the cent, not from the flag: `updated` (rewritten, and it matches) · `current` (left alone, and it matches — `✓ CURRENT — nothing moved in QuickBooks; the page matches the books`) · `mismatch` (`⚠ DOES NOT MATCH THE BOOKS — their page: … · the books: …`, whatever the flag said) · `nobooks` (the run gave no figures: `○ NOTHING IN QUICKBOOKS YET — <why>`, said but not a fault) · `unlisted` (a page in the index that is not on the receipt: `⚠ NOT ON THE BOOKS RUN'S LIST … (their page shows …). Tell Claude which QuickBooks customer it is.`) · `noread` · `unchecked` (the pages could not be read: the run's words stand, softened to `NOT REWRITTEN`, and the tally says they could not be checked — never a guess either way). What needs him sorts to the TOP; the tally and the banner count what MATCHES THE BOOKS (`8 of 10 client budgets match the books · 1 not in QuickBooks yet · 1 needs a look`; clean = `📚 BOOKS UPDATED … every page is current`, else `⚠ BOOKS — A PAGE NEEDS A LOOK`); the pages are read once per run while the banner is up, again when he opens the window, never after he clears it. Rehearsed on the real receipt and pages (`scratchpad/dbg-qbf.js`): 7 CURRENT + Josten UPDATED, Gilbreath ○, Hertz ⚠. `tests/_test694.js` (16 checks, made-up clients). **For the books session (the receipt contract, unchanged in shape):** keep writing `updated: false` + the figures + a `why` for a page you checked and left alone — the app now proves it against the page; list EVERY code in `Client Portal/index.json`, and a page you cannot place gets a row with NO figures and the reason. **Open, Eric's word needed (no guess-matching):** is the QuickBooks Hertz job the same job as the "Hertz Laundry/pantry" page? On his yes: the page's invoiced/paid/open from the ledger (create-first, only the books-owned keys), a receipt row, a line in `qb-refresh-state.json` notes so Tuesday's chain carries it. **Parked:** the homeowner page's lamp reads `CURRENT AS OF <updated>` and `updated` only moves when the page is rewritten, so a page that was verified yesterday can read a date weeks old — a separate `checked` date written by the run (and shown as "books checked …") would fix it; his go.
- Before that: **v6.93** (pushed 2026-09-18): "SWEEP NOW" SAYS WHAT IT DOES. Eric: "i'd say just change 'sweep now' to different wording, i thought that meant send all text to the grinder like the 'flush' word we used. it means 'go check for texts'?" It does — nothing is flushed or sent anywhere — so every word HE reads about it says CHECK (the function names stay: `sweepNow`, `sweepNote`, `sweepStatusHtml`, `.sweep-line`). The line under the text counter: `📥 Checked for new texts <when>: n in the Inbox · n texts and n emails brought into your log · n more on the next check · Check for texts now`; never run: `📥 Has not checked for new texts yet on this phone — it checks with every sync`; failed: `⚠ The check for new texts FAILED <when>: <why>`; another device busy: `⏳ <when>: another of your devices is bringing the texts in — this one waits its turn`; the Setup → 📧 Email senders plate: `📥 Check for new texts & emails now`; the messages: `📥 Checking for new texts and emails…` → `📥 n texts and n emails brought in · n still to come` / `⚠ The check failed: …` / `📥 Already checking for texts — watch the line under the text counter`; the band: `⚠ THE CHECK FOR TEXTS STOPPED — see the line under the text counter` (`CATCHING UP ON THE INBOX — n LEFT` and `✓ INBOX CAUGHT UP` stay — he was told to watch for them). He calls the meter "the text counter" — use his word. `tests/_test693.js` (the word "sweep" appears nowhere he reads); `_test686`/`_test690` re-pinned. **Lesson:** a word that is OUR name for a mechanism ("sweep") is not a word for a button — he mapped it onto the nearest word he knew ("flush") and read the opposite of what it does; name a button by what happens for HIM. **Answered the same evening:** what a "suite" is (one test file = a robot running one feature's checklist on a phone-size screen; all of them run before every release). **His call, still open:** while he is CLOCKED IN the app checks for texts only when it opens or he taps Check for texts now (`autoPull` skips a clocked-in phone) — offered, not decided.
- Before that: **v6.92** (pushed 2026-09-18): WHEN DROPBOX SAYS SLOW DOWN — the first real catch-up stalled, and why. Eric: "i did the qb updater, check again?" (it took: the three 09-18 exports are in `/Clore Project Tracker/QB Exports/` as `… (5)/(6)/(2).xlsx`, sha-identical to his Downloads copies; `UPDATE-NOW.txt` was not pressed and does not need to be — the by-hand run already stands, Tuesday's chain will find the files). Checking the Inbox beside it showed v6.90's catch-up had started at 21:10 and STOPPED: about forty of 09-18's texts on the log (two rounds, dated by arrival as built), but only 13 files out of the Inbox, the lease written once and never handed back, 295 files still waiting. **Why:** a sweep fired its twenty `files/move_v2` calls AT ONCE (`archiveText` was fire-and-forget), and Dropbox refuses writes that pile into one folder together (`too_many_write_operations`, a 429) — a sweep of two or three files never showed it; then a listing or a read got the same pushback, `dbxRpc` handed the error object up, the sweep reported `err`, and the chain's rule "an error ends it" ended it. `dbxRpc` also never threw on an error object, so `archiveText` counted a refused move as done. **The fix:** (1) `dbxSlowDown(r, i)` + a bounded loop in ALL THREE Dropbox calls (`dbxRpc`, `dbxDownload`, `dbxUpload`): a 429 waits what it was told (`Retry-After`, else `error.retry_after`, else 2·4·6 s, never over 15 s; `window._dbxBackoffMs` for tests) and asks again, three times, then hands the answer up as before. (2) **One move at a time:** `archiveText` queues onto `_arcLine` (a promise chain) → `archiveTextNow` (an error object = NOT moved, except `not_found` = another device already moved it); `arcDrain()` is the line's tail and the catch-up awaits it (a minute at most) before it lists the Inbox again. (3) **A pushback is not the end:** `checkInboxTexts(round, tries)` — an `err` that reads too_many/429/rate does the round over after `window._sweepRetryGap || 10000` ms with the lease renewed and the band reading `📥 DROPBOX SAID SLOW DOWN — trying again in a moment…`, three times, then stops in words (band + the ⚠ FAILED line) and hands the lease back; the gap between rounds is 2.5 s. Files already logged whose moves were refused are `waiting` files — their moves are retried (forty a sweep) and they can never log twice (`pendDone`, and `textAlreadyLogged` on another device: the same `textTs`). Rehearsed on the real Inbox (read-only) against a fake Dropbox that refuses any two moves in the air at once: 15 rounds, 0 refused, 137 texts + 67 emails onto 09-14 … 09-18, caught up. `tests/_test692.js` (11 checks: the three calls against a stubbed `fetch` that answers 429, never two moves in flight, the chain waits for its moves, a refused move heals next sweep, a pushback mid-chain is done over, three in a row stops in words). **Known and left alone:** `autoPull` does nothing while he is CLOCKED IN (`if (clockedInAt) return;` — a pull replaces `entries` wholesale), so during a shift the Inbox is swept only when the app opens or he taps Sweep now; sweeping without pulling would push from a stale device more often (entries.json is last-writer-wins) — a real trade-off, his call, not decided here. **Next:** what the line under the text meter says after the catch-up; the tags switch + 🧹 tidy list, the two extra email sorts, Phil's time-clock note, the museum page.
- Before that: **v6.91** (pushed 2026-09-18): THE BUILD LIST, ROUND TWO — checklist lights, an icon in every light, ⚠ FIRST, the edit window, the phone. Eric, after dragging his Word tracker onto the board ("I really liked that I was able to click and drag the list, and it auto-populated"): "I like the succession of lights … But I also want something that will flip that … to just a checklist … 'Check all vents and airflow' — that's just testing items in a new house … Make sure it's on somebody's list, and then scheduled, and then done? … a little icon in each of the checkboxes that light up … the icon where the check mark is now could change depending on what light is lit up … 'test winch' … says 'pick needed,' I don't need a pick … an icon of a triangle with an exclamation point … that goes to the top of its category, meaning this needs to be looked at or done before anything else in that category continues … as soon as I click Edit, it shifts, it changes sizes, it brings up a whole bunch of stuff … Let's figure out a better layout for the edit … on the phone everything's pretty crammed and disorganized. It looks really good on the computer." The block sits above `matWord` (`// 📋 v6.91 — THE BUILD LIST, ROUND TWO`). **The lights:** `MAT_BOXES` rows are `[letter, word, icon]` — 👆 P · 🛒 O · 📦 R · 📅 S · 🔧 I; a row that is not something to buy wears `MAT_TASK_BOXES` — 👷 A on a list · 📅 S scheduled · ✅ D done (`matBoxes(it)`, `matBoxOn`, `matBoxTap`). A light = the icon over its letter; LIT = the brass plate + the icon in full + a ✓ in the corner (`.mb-ck`) + `aria-pressed` and the word in the label — never colour alone; unlit = hollow, the icon greyed. **"On a list" IS the tags:** a tag has always been somebody's list (the 🏷 sort at the top, v5.80), so 👷 lights when the row has a tag and a tap opens `matWhoPickHtml` under the row — the trades, his crew names (`matWhoBank`), ➕ a name (`matWhoAdd` → `prefs.matTags`); the row then reads `on the Phil list +1`. ✅ refuses while a step under the row is open (the lamp's old rule). A checklist row never reads "pick needed": `matRowWord` says `to do — on nobody's list yet` / `on the … list` / `scheduled <date>` / `in work` / `done`; `matLampGlyph` puts the icon of the furthest lit light on the row's round lamp (strip order — I, S, R, O, P). `matRoomCounts` counts them the same way (`✅ 1/3 done · 2 on a list · 1 scheduled`, no leading dot) and `👷 on nobody's list` joined the working lists (`MAT_STAGE_F`). **⇄ the flip:** `matBuyToggle` (it now keeps `sch`/`schd` across the flip) is one tap on the row WHILE NO LIGHT IS LIT (`.mat-flip`, `⇄ list` / `⇄ buy` — the moment a pasted "test winch" still says "pick needed"); after that it lives in the open row's chips and as two KIND plates in the edit window; a 🏠 row never offers it. **⚠ FIRST:** `it.first` (`matFirstToggle`, the `⚠ 1st` plate on every row, a plate in the edit window); `matIsFirst` = flagged AND not done; `renderMatMgr` sorts a category's rows first-on-top (a stable sort — the rest keep their places), the row wears `.mi-first` + the words `⚠ FIRST — the rest of this category waits on this`, the heading counts `⚠ n first`; office-only — the client cut strips it (`const { pkt, sel, est, act, ec, first, ...rest } = x`). **✎ The edit window:** `matEditOpen(id, selectName)` / `matEditClose` / `matEdFold(k)` (`_matEdOpen`); `matEditSheetHtml(it)` is drawn into `#matSheetHost` — OUTSIDE the scrolling board (a fixed window inside a scrolling one is at the mercy of its parent), z 85: over the board (80), under the toast (90) and the lightbox (100); `renderMatMgr` fills it and keeps its scroll through every redraw; the row behind never turns into a form (`.mi-editing` outline only). Always the same order: NAME · NOTE (`sel`, a real writing box — "decide whether the railing is needed" is editable right there) · WHAT KIND OF ROW · ITS LIGHTS (+ the date, + whose list on a checklist row) · ⚠ FIRST · then folds that say what is in them — 💵 Money · 🏷 Whose list · 🏠 The homeowner · ☑ Steps under it · 📷 Photos · 📁 Pocket (bodies `hidden`, so every id exists folded) · a pinned foot `✕ Delete` (two taps) / `✓ DONE — it is saved`. The old inline `matMgrEditor` and `matEditorPlusHtml` are gone (their chunks were lifted into the sheet verbatim by `scratchpad/apply-691.js`); ✎ edit, hold-a-name and ➕ item all open the window (➕ with the name selected); `matClose` clears it. **The phone:** the category toolbar is no longer floated over the first row (`.mat-rm-head` flex; on ≤700px only `➕ item` and `⋯` show — `_matRmTools`, the rest behind `.rm-more`; a SURE?-armed delete forces it open), counts in plain case on their own line, the title row is flex (`.mat-head`), the instructions and the legend fold behind `▸ ? how this works` (`_matHelp`, `#matHelpBox`), rows get a hairline between them, the strip starts at the left edge and all seven plates (5 lights + ⚠ + ⇄, 37×42) sit on ONE line at 390px, the note runs full width under them. **Also in v6.91 — the summary's 🎒 POCKET — not done** (Eric: "the buttons are huge compared to the text … make it a little more visually appealing"): four small plates in ONE row per leftover (`.pk-mini`, an icon over its word — ↩ Today · ➡ Tomorrow · ✓ Done · ✕ Not needed, 40px tall, `aria-label`s) beside the small print (`.pk-leftrow`/`.pk-meta`), the words 15px; the 🧹 all-at-once plates are as wide as their words. **And the board's ✓** (Eric: "so I know that it's getting saved somewhere"): the label reads `✓ Done — leaves the board · stays in your log, marked done` and the toast `✓ Done — off the board · kept in your log, marked done — <words>` (a to-do: `the to-do is checked off`) — a board line always WAS a log entry; the ✓ only marks it. `tests/_test691.js` (22 checks); `_test689` and `_test687` re-pinned to the new markup. **Gotcha kept:** a quoted Bash heredoc still ate the backslashes in a regex-bearing node script (`<\/b>` → `</b>`) — write those with the Write tool. **Not built, on his go:** a marked-up cost and Scheduled/Installed lamps on the homeowner's page, a receipt ticking a row (his tap), the talk-through walkthrough, a per-category "these are all checklist items" flip, sending a checklist row to a crew phone's board. **Next:** the tags switch + 🧹 tidy list, the two extra email sorts, Phil's time-clock note, the museum page.
- Before that: **v6.90** (pushed 2026-09-18): THE INBOX LISTING COMES IN PAGES — why the texts stopped on 09-14. Eric sent the v6.86 status line from his PC: "📥 Inbox sweep just now: 0 in the Inbox · 0 texts and 0 emails filed" — while the Dropbox folder held 336 files (200 texts, 109 emails, 27 attachments), the oldest unfiled from 09-14, the newest minutes old. **The cause:** Dropbox answers `files/list_folder` a PAGE at a time, and a page may hold few entries or NONE AT ALL while `has_more` is true (a folder thousands of files have passed through pages over what has left it first). Every listing in the app read page one and stopped — no `has_more`, no `list_folder/continue` anywhere. On 09-14 page one of `/Inbox` came back empty; the sweep read that as an empty Inbox, and `mail-judged.json` shows the cloud watcher (the same one-page read, a different token) judged its last file on 09-15 — two independent readers going blind together is what pointed at the listing, not the phone. Reproduced first (`scratchpad/dbg-690.js`: the live v6.89 code reads `seen: 0` with files on page two). **The fix:** `dbxList(arg, maxPages = 120, maxMs = 25000)` beside `dbxRpc` — first call `{ limit: 2000, ...arg }`, then `files/list_folder/continue` with ONLY the cursor until `has_more` is false; page one's shape with everything in it (`pages` added); a refused first call is still Dropbox's own error object; a page that fails part-way leaves `partial` (the reason) on what was gathered. ALL TEN listings go through it (Inbox, Summaries, Crew, the three root scans, the crew dir, QB Exports, Backups, the File Cabinet's recursive folder list) — `_test690` asserts `dbxRpc('files/list_folder'` appears exactly once in the file. **The cloud half:** `fnsrc/mail-watch.mjs` `listAll()` (forty pages or six seconds, inside the run's budget), bundled — **run the bundler FROM THE REPO folder** (`node ../boiler-room-tools/build-functions.mjs mail-watch`): run from the tools folder, esbuild relabels every module path and the bundle diff is hundreds of lines of noise. A file the Shortcut NAMED as a text is marked `skip:'text'` without a download and no longer uses up the eight-a-run; emails are taken newest first; an important email found more than a day late is written `push:'late'` and does not ring (the result gains `pages`, a count). **Catching up** (the sweep files twenty a round): `checkInboxTexts(round)` is now the one-at-a-time door (`_sweepBusy`) around `inboxSweepOnce()` (the old body); when a round leaves NEW files behind and handled something (`_sweepLast.did`), the next follows by itself 1.5 s later — until clear, a round that handles nothing, or forty rounds — and the working band reads `📥 CATCHING UP ON THE INBOX — n LEFT…` then `📥 ✓ INBOX CAUGHT UP`. **🔑 One device catches up:** entries.json is last-writer-wins, so his PC and phone filing the same backlog for minutes would write over each other; the device that starts a chain takes a lease (`App Data/sweep-lease.json`: a random `daylog-device` id + `until`, ninety seconds, renewed every round, handed back at the end); a device that finds someone else's live lease skips its sweep and the line says `⏳ another of your devices is catching up … this one waits its turn`; no file / unreadable / expired = sweep as always. **Four holes the backlog would have hit:** (1) `files.slice(0, 20)` was taken BEFORE handled and carded files were set aside, so twenty stuck files at the front (a hot text waits in the Inbox until he answers its card) starved everything behind them — now `waiting` files ride along (forty a sweep: late photos, archive retries) and the twenty are NEW files, newest first; the email candidates likewise. (2) A read that FAILED (`dbxDownload` → null) was logged as "(photo only — no words)" and the file archived, the words gone — now it is skipped and counted (`missed`, said on the line). (3) An email waiting behind a pile of texts was downloaded again every sweep — `_txtCache` first. (4) **Texts are dated by ARRIVAL** (`autoLogText` sets `ts` from `server_modified`, as emails have been since v6.41) — a sweep runs minutes after a text lands so nobody saw the difference, but 200 texts from five days were about to be filed as "today" and break the meter's record, its average and the days they belong to. A hot text found more than twelve hours late still pins its card but does not ring. The status line gains `· ⚠ n could not be read this time` and `· ⚠ the listing was cut short (why)`. **Dress rehearsal** (`scratchpad/dbg-inbox2.js`: the real mirror Inbox, read-only, a paging fake Dropbox, every write a no-op, Chromium AND WebKit): 16 rounds, 173 texts and 73 emails filed onto 09-14 … 09-18, 5 hot cards, 1 locked personal, no rings, nothing left but the cards' own files and the 27 attachments. `tests/_test690.js` (the app half with a paging fake Dropbox, the watcher half in node); `_test644` re-pinned (the sweep's body is `inboxSweepOnce`). **Also answered 2026-09-18:** Eric liked dragging his Word tracker onto the Build List and asked whether it "goes to Claude" — no: `matDocxText` + `matParseList` run in his browser, no AI, nothing leaves the PC but the saved board. **Finding, parked:** the email Zap saves every ATTACHMENT (sub invoices, estimates, a bond letter, insurance certs, signature images, Phil's recap PDFs) into `/Clore Project Tracker/QB Exports/` and some into `/Inbox`; the app never shows them — tying an attachment to its email entry is on his go. **Still to do by Eric:** drop the three 09-18 QuickBooks exports into Setup → 💼 QuickBooks updater (the by-hand run used his Downloads; the exports folder still holds the 09-14 files). **Next:** the tags switch + 🧹 tidy list, the two extra email sorts, Phil's time-clock note, the museum page.
- Before that: **v6.89** (pushed 2026-09-18): THE BUILD LIST, ERIC'S FINISH-OUT TRACKER. Eric walked the Josten/Weiser house talking into his phone and built a Word tracker by hand — rooms, an item a row, what was picked ON the row, a strip of boxes that fills in (Picked · Ordered · Received) and a by-trade page: "It should probably have 'installed' or 'scheduled' or something on there too, probably a couple more checkboxes for the total cost of each item. This is what I'm trying to accomplish on the build list. I think the build list and the estimate sheets could eventually be combined." Feedback given (LINK the two boards, do not merge them), four steps offered, then: "Yes, build it all." The block sits above `matWord` (`// 📋 v6.89 — THE BUILD LIST`). **(1) The row.** A row is 🛒 something to BUY (`matIsBuy(it)` = `it.hm || it.buy`, chain `MAT_BUY_CHAIN` pick → picked → ordered → arrived) or 🔨 WORK (`MAT_TASK_CHAIN`); `matBuyToggle` flips an office row and maps the lamp; `matHmToggle` no longer remaps a `buy` row. Under the name rides `.mat-line2`: a five-box strip (`matStripHtml`, `MAT_BOXES` P O R S I — each box a ☒/☐ glyph + its letter + an aria label in words, 34px), then the selection (`it.sel`, else `it.pick`), `📅 install <schd>`, the money (`matCostBits`) and the estimate line. `matBoxTap`: P/O/R walk the ONE state the homeowner's page also reads (`it.s`; un-ticking a box un-ticks what follows and clears Installed; Ordered stamps `odate`), S = `it.sch` (+ optional `it.schd`), I = `it.ins` (the day) and pulls Received along. **Done changed meaning:** `matItemDone` = INSTALLED for a thing to buy (it was "arrived"), `done` for work — the row word reads `installed`. Category headings say it in words (`matRoomCounts`: `🛒 9/15 picked · 2 ordered · 1 received · 0 installed · 🔨 3/5 done · est $ · paid $`), a legend line carries the whole-house money, and four working lists filter the board (`MAT_STAGE_F`/`_matStageF`: ○ to pick · 🛒 picked, not ordered · 📦 ordered, not here · 🔧 here, not installed), AND-ed with the trade tag filter. **(2) Cost.** `it.est` / `it.act` (`matSetNum`), shown per row with `⚠ over by` / `✓ under by` in words, summed per category and for the house. **(3) 📋 Paste a list** (`matPasteOpen` → `#matPasteSec`, `matParseList`, `matPastePreview`, `matPasteRoomFlip`, `matPasteAdd`): TABLE mode (a tab anywhere, or two lines with ` | `): a line on its own names the room, a heading row maps the columns (Item · Selection/Notes · Picked · Ordered · Received · Scheduled · Installed · Estimate · Actual · Estimate line; no heading = that order), ☒ ☑ ✓ x yes tick a box; PLAIN mode: CAPITALS or a trailing colon name the room, `name - what was picked` splits, a leading ☒ = picked. A room exists only once an item lands in it, so a title, a legend line and a by-trade summary page fall away; nothing is guessed (no trades, no prices; an estimate line only by its EXACT name). A preview (counts, a ✓ add / ○ skip plate per category) comes first; adding makes `buy` rows, updates a same-name row in the same category instead of doubling it, and never walks a row backwards. **The Word file itself** drops onto the box or comes through 📄 Pick a Word file: `matZipEntry` (central directory, stored or `DecompressionStream('deflate-raw')`) + `matDocxText` (paragraphs → lines, table rows → tab-separated) feed the same parser; `.txt/.tsv/.csv` too. **(4) The estimates link.** `it.ec` = an estimate line (`matEcList`: `EST_DEFAULT_CATS` + what the board already uses); `openEstimates` loads `materials-office-<code>.json` into `_estMatRoll` (`estMatRoll`) and each category row's `.est-sub` gains `📋 n on the build list · picked · ordered · received · installed · est · paid` (`estMatRollBit`) — and a line with build-list items no longer folds away as empty. **THE WALL:** `sel`, `est`, `act`, `ec` are office-side — `matSave`'s client cut strips them (`const { pkt, sel, est, act, ec, ...rest } = x`), because his selection notes name suppliers and a cost on their page must be a marked-up number, which this is not; `pick` stays the client-facing word. Templates and the master keep `buy` and `ec`, never a selection or a cost. `tests/_test689.js` (every name and figure made up; builds its own .docx in the page, stored and deflated). **Not built, on his go:** a marked-up cost per item on the homeowner's page, Scheduled/Installed lamps on their page, a receipt ticking a build-list row (by his tap), the talk-through walkthrough mode. **Next:** the tags switch + 🧹 tidy list, the two extra email sorts, Phil's time-clock note, the museum page; still open: what the Inbox-sweep status line says on his phone.
- Before that: **v6.88** (pushed 2026-09-18): THE PROJECT TRACKER REFRESHES ITSELF FROM THE LEDGER. Eric: "Check and see why the project tracker isn't updating. Update all the QuickBooks off of these spreadsheet reports. Like we normally would." **Why it never moved:** the weekly books run (the Tuesday chain, in a chat session) rewrites `qb-ledger.json`, the client pages, `profit-ticker.html`, `qb-costs.json` and its receipt — `App Data/project-tracker.html` was built once on 2026-07-28 and has never been on that list, so 📊 Project Tracker showed July money while everything else moved. **The fix is app-side:** `openMoneyPage('tracker')` → `trackerFresh(html)`: `trackerJobsBlock` lifts the page's `const JOBS = {…}`, each job's `invoiced / paid / open / lastDate / invoices[]` is rebuilt from the ledger rows whose top-level customer (`n` before the `:`) equals the `customer` NAMED ON THE PAGE (exact, never a guess); the July identity `phases + draws + tosort + gap = invoiced` is kept true by ONE added `tosort` line, `TRK_SINCE` ("Billed since the phase sort — not in the phase bars yet"); a receipt client (`qb-refresh.json`) with no job on the page joins only when its lifetime figure ties to exactly ONE ledger customer to the cent (Scritchfield does); the banner and the "Updated …" string are rewritten to say the money is as of `ledger.asOf` and the phase bars are still the page's own sort. Left alone: a page whose "imported <date>" is on or after `ledger.asOf` (the run rebuilt it), no ledger, a broken ledger, no JOBS block, and the ticker always. `tests/_test688.js`. **The by-hand books run of 2026-09-18 (this session, Dropbox connector):** three exports from Eric's Downloads, all ALL DATES, Sales detail ACCRUAL; each parse tied to its own sheet TOTAL, and the parser first REPRODUCED the 09-15 chain's Transaction List sum, its 2026 cash in/out/net, last-60 and 2025 net to the cent and 136 of 137 `qb-costs` qids from the 09-14 exports. Against the 09-15 ledger: four new invoices (one Josten, already paid; three C&C Properties, open), nothing changed, nothing gone. WRITTEN create-first (`.new` → compared byte-for-byte through the local mirror → old moved to Archive/Backups → `.new` moved live): `qb-ledger.json` (rows now also carry `d` = invoice date, which the tracker refresh reads), `Client Portal/josten-842d53.json` (ONLY invoiced/paid/open), `qb-refresh.json`, `qb-refresh-state.json` (`lastInvoiceNum` 1333), the heartbeat. NOT touched: `qb-costs.json` (14 new lines wait for the chain), `profit-ticker.html`, `project-tracker.html`, every `phases` block. **The contract earned its keep:** Eric released a journal week while the run was under way — the verify step caught that the live page no longer matched, the stale `.new` went to Archive, the page was read again and rebuilt with his week intact. ALWAYS re-compare the live file against what you built from, immediately before the swap. **Findings worth keeping:** `phases` on client pages are a July snapshot that no run maintains (Josten's bars add up to well under half of what is invoiced; Scritchfield has none) — the chain "preserves them verbatim", so the phase bars everywhere are stale and need a real weekly rebuild from the Sales detail (the label map lives with whoever built the July tracker); the Dropbox connector in Claude Code is a UUID-named MCP (`who_am_i` = Eric) whose `create_file` takes TEXT only — no xlsx upload, so exports still go in through Setup → QuickBooks updater; the local mirror stays read-only and is the byte-exact way to VERIFY an upload (size + sha). **Eric's go of 2026-09-18, still to build:** the Build List — selection on the row, Scheduled + Installed, room counts, cost per item, 📋 paste-a-list (and .docx drop), estimate-line link and roll-up (see the memory note `build-list-direction`). **Next after that:** the tags switch + 🧹 tidy list, the two extra email sorts, Phil's time-clock note, the museum page; still open: what the Inbox-sweep status line says on his phone.
- Before that: **v6.87** (pushed 2026-09-18): CLEAR THE POCKET LEFTOVERS. Eric, with 18 rows under 🎒 POCKET — not done and one button each: "i need a way to clear these: add them back onto todays list, tomorrows, or check them as done or no longer relevant." Every row under the summary's POCKET heading now wears four plates (`.pk-acts`, two across on a phone, four on a PC): `↩ Today` / `➡ Tomorrow` (`pocketBack(id, 0|1)` — the item goes onto that day's pocket with the board's priority cubes riding along as `it.p`, the note turns `pocket:'back'`), `✓ Done` (`pocketLeftDone` → `pocket:'done'`, the note reads `✓ words` like any pocket ✓, `pocketAt` stamped) and `✕ Not needed` (`pocketLeftDrop` → the new state `pocket:'dropped'`, the note's words untouched — the log keeps the record); done and dropped carry `toastUndo`. **On top, when two or more wait:** `🧹 All n at once: ✓ All done · ✕ None needed` (`pocketBulkHtml`/`pocketLeftBulk`, two taps — the first arms `⚠ SURE? tap again — all n` for 4 s (`_pkLeftArm` — the plan rack already owns `_pkArm`; a duplicate `let` kills the whole script, and `scratchpad/syntax-check.mjs` runs `node --check` on the app block to catch that before the suites do), one Undo brings them all back). **A full pocket refuses in words** (`The pocket is full (8/8) — finish one or make it bigger with +`): the old `pocketBack` called `pocketOverflow()`, which pushed the OLDEST item out as a brand-new leftover that landed straight back under this heading. **The list no longer ages out of TODAY:** `pocketLeftList()` = every flushed/overflow/swept note of the last 14 days that is not personal (`sumHidden`) and not ✓'d on the board, whatever the TODAY / THIS WEEK span says. **The board follows:** `boardOn` checks `['done','back','dropped'].includes(e.pocket)` BEFORE the touched-line rule, so a leftover he had set cubes on leaves the board when it is cleared here (before, a touched line stayed until its own ✓ — a duplicate of the pocket item once it went back). **The Wizard** hears `DROPPED — he said it is no longer needed` on the row instead of NOT DONE; a leftover checked off joins POCKET DONE. `tests/_test687.js`; `_test678` re-pinned (➡ Tomorrow, `.pk-acts` count). **Still open from 2026-09-16:** Eric has not yet sent what the new Inbox-sweep status line says (59 texts + 45 emails were stuck in `/Inbox` since 09-14). **Next:** the tags switch + 🧹 tidy list (on his go), the two extra email sorts, Phil's time-clock note, the museum page.
- Before that: **v6.86** (pushed 2026-09-16): ERIC'S LIST OF 2026-09-16 + ③ THE PHOTO. Eric: "take the photo/file select button and make it step 3 and move tag it to step 4" · "the phone version still has 2 summary buttons at the top" · "when i click on [search] it opens the search boxes it pushes the search tags down … it should either come up in a window or be easily separated out" · "when i click on receipts it should close all other tags, currently still has other things in there like a wizard and photo and grinder" · "the new budget button on the bottom should say personal budget" · "the text counter at the bottom i dont think is working it says 0 texts today" · "on the pick the job, make shop/admin to say internal/admin". **The grinder** is six steps: ① job · ② SAY IT (words only now) · ③ ADD A PHOTO OR FILE (`.cam-btn--wide`, the same `#qnPhoto` input, `#qnPhotoInfo` under it; DONE reads `📷 n attached`) · ④ TAG IT · ⑤ UNLOCK IT · ⑥ SEND IT; `updateStepFlow`: `next = words||photos ? 6 : job ? 2 : 1`, folds are ④/⑤ (`gFold(4)`/`gFold(5)`); `_test624/630/642` re-pinned. **One summary button everywhere:** the v6.85 PC-only rule is global — `body:not(.crew-mode) #scRow .sc-btn:nth-child(4)` hidden, three plates, the header SUMMARY (`#hbSumN` count) is the one door on the phone too (`_test685` re-pinned). **The search window** `#rlWin` (z 70): `rlSearchShow(true)` MOVES `#rlSearchWrap`, `#askRecent` and `#logDbxHits` into `#rlWinBody` (the log renders wherever `#askRecent` lives) and leaves a `#rlWinNote` line in `#runLogCard`; closing moves them back in order and keeps the search — the plate reads `🔍 Search ✓ ON` (`window._rlFiltersOn`, set in `renderAskRecent`) until Clear filters ✕ (`clearLogFilters` resets the plate). **Receipts ON** also clears `prefs.rlFilter/rlWho/rlHide`, and the kind-plate row collapses to ONE `🧾 ✓ RECEIPTS ONLY — every other filter is off · tap to show everything` plate while it holds. **The plate** reads `Personal Budget` / `Personal` (italic), the page `💰 PERSONAL BUDGET — yours, not the business`. **Rename a job:** `renameJob(from, to)` moves the log, to-dos, `curJob`/`qnJobPick`/`prefs.lastJob`/`prefs.jobRecent`, `prefs.jobCats` (by `jobCatKey`), `prefs.cards` (by `cardKey`), `prefs.recapJobs`, `todoFilter`, `daylog-cardjob`/`_cardJob`; Setup → Jobs pills wear ✎ (`jobRenameStart/Done`, `#jobRenBox`, Enter saves, Esc cancels, a taken name is refused); the one-time switch in `pullRemote` (`prefs.jobRen686`, never `CREW_NAME`) renames `Shop / Admin` → `Internal / Admin`; `laborRecapTake` files under `/^(shop|internal)/i`. Crew phones get the new name when Eric next runs Setup → Crew → set up crew folders (`publishCrewFolders` writes `crew.json`). **The Inbox sweep speaks:** `sweepNote()` keeps `{at, seen, texts, mails, left, err}` (`window._sweepLast` + `daylog-sweep-last`); `checkInboxTexts` records no-Dropbox, a refused listing (`Inbox listing: <error_summary>` — before, an error object read as an EMPTY Inbox), the counts at the end, and any thrown error in its catch (which used to be silent); `sweepStatusHtml()` rides under the text meter (`.sweep-line`, even on a quiet morning) and in Setup → 📧 Email senders (`#sweepStatusSet`, a `📥 Sweep the Inbox now` plate, `sweepNow()`). **The live finding (2026-09-16):** the mirror shows 59 TEXT + 45 Email files in `/Inbox` untouched since 2026-09-14 11:06 AKDT (the last texted entry), while `entries.json`, `master-log.csv` and `wizard-log.json` kept being written; replaying the real Inbox through the sweep in Chromium AND WebKit (Playwright, `scratchpad/dbg-inbox.js`, Dropbox writes stubbed) filed all 40 cleanly — the code path works, so the phone's sweep is failing at the Dropbox listing or being refused; v6.86's status line will say which. `tests/_test686.js`. **Parked (Eric, 2026-09-16):** the summary tab "looks an awful lot like just a better organized [log] … we may not need both eventually" — leave both for now; a PC/phone mode switch in Setup (answered: the layout already follows the screen width at 700px; a switch adds little). **Next:** the tags switch + 🧹 tidy list (on his go), the two extra email sorts, Phil's time-clock note, the museum page.
- Before that: **v6.85** (pushed 2026-09-15): TWO THEMES, FIVE CALM TONES; ONE SUMMARY BUTTON ON THE PC. Eric: "In the appearance and theme I'd like you to get rid of everything but Steamworks and Calm Brass. Then use the calm brass THEME as its the best, but make a few different color schemes with different background colors. Make them very neutral and easy on the eyes." and "Looks like we have two summary buttons at the top. Let's keep the one at the very top between the Instant and the Setup buttons, at least on the PC version." **Themes:** `SKIN_NAMES` is six picks — `calm` Calm Brass, `calm-slate` (cool grey), `calm-graphite` (plain grey), `calm-moss` (green-grey), `calm-stone` (lighter warm grey), `steam` Steamworks. A Calm tone is the SAME skin: `skinPaint(k)` sets `data-skin="calm"` + `data-tone="<tone>"` on the root, and `:root[data-skin="calm"][data-tone="…"]` overrides only the canvas (`--page`, `--surface-1`, the new `--raised` chip plate and `--field` writing-box tokens, the three text levels, grid/baseline/border); brass, green, orange and coral and every calm layout rule stay. `skinKey(s)` maps anything else — Clore Gold `''`, Command Center, Steel, High-Vis, Blueprint, Winter Night, Daylight (their CSS blocks and chips are gone, the base `:root` tokens stay as fallbacks) — to Calm Brass, so an old pref or an empty one lands there; `applySkin`/`setSkin`/boot all go through it; the ◐ Light / dark button is gone (`toggleLightDark` stays as a function). Each chip carries a `.skin-dot` of its page colour AND its name. Crew phones see the same six. **The summary buttons:** on a PC (`min-width: 701px`) the 📊 Summary plate under the clock is hidden and `#scRow` is three across; the header SUMMARY button (between the Wizard lamp — INSTANT/HOURLY — and SETUP) now wears the waiting count too (`#hbSumN`, written beside `#scSumN` in `renderPendBanner`); the phone keeps both plates. `tests/_test685.js` computes real WCAG ratios for every tone (text 7:1+, dim text and brass 4.5:1+, a near-grey canvas, no two alike). **Next:** the two extra email sorts, Phil's time-clock note, the budget page as Eric grows it, the tags switch and 🧹 tidy list (offered 2026-09-15, on his go), the museum page.
- Before that: **v6.84** (pushed 2026-09-15): THE FILE CABINET TO THE MUSEUM; 💰 PERSONAL BUDGET IN ITS PLACE; THE POCKET: ✎ AND ⤵ PER ITEM. Eric: "send the file cabinet button and pages to the boiler room museum. dont throw it away but its no longer needed for now. replace that buttons place with 'personal budget' and we are going to work on a budget page for tracking personal expenses and finding ways to save money and plan for debt pay offs … eric loves graphs and charts and numbers, make something simple but informative." `body.parked-630` hides `.cap-btn[data-panel="file"]` (the panel, `openPanel('file')`, `fcSuggestFolder` and every filing road stay — someday = take it off the list); a `data-panel="budget"` plate sits in its spot (hidden on crew phones); `#panel-budget` → `renderBudget()` from `openPanel`. **Data:** `prefs.pb = { inc[], bills[], debts[], goals[], spend[], caps{}, extra, method }` (`pb()` heals the shape) — his own Dropbox prefs; never a log entry, never in `buildAskContext`, never in a crew or client file, and **never in this file: the site is public, so none of his figures may ever be baked into the code** (`_test684` scans the budget block for any figure to the cent or in the thousands with a comma; the test file is public too, so its own numbers are made up) — they enter by form or paste into `prefs.pb`. **The page:** THIS MONTH tiles (IN · BILLS · SPENT · ✓ LEFT/⚠ SHORT in words) + a labelled SVG bar chart (`pbBars`: the number on every bar) + WHERE IT WENT by category ($ and %, or against the cap); SPEND quick add (amount · category `PB_CATS` + his cap names · note, ✕ with Undo); SPENDING CAPS (`prefs.pb.caps` — a ceiling per category, the "ways to save": the bar reads spent of cap with ✓ left / ⚠ over in words and a dashed line at the cap; the month card says what is left if every cap holds); BILLS (due day, autopay, monthly total); DEBTS with THE PAY-OFF PLAN (`pbPlan(debts, extra, method)`: minimums every month, interest first, the extra and every freed minimum onto the focus debt — SNOWBALL smallest-first or AVALANCHE highest-rate-first; tiles DEBT-FREE IN n mo + month, INTEREST PAID, +$100 A MONTH saves $, PAID IN ALL; a bar per debt with its pay-off month; unpayable minimums say so); SAVINGS GOALS (progress track, $ of $ · %, "there by <month>" from the monthly put-in); INCOME (per month / 2 weeks / week / year → monthly). Forms per list (`pbEdit`/`pbSave`/`pbDel`) and a **📋 Paste lines** box per list (`pbPasteOpen`/`pbParseLine`/`pbPasteSave`: one item per line — the name, then the numbers; the pace, due day, autopay, APR %, minimum, saved, the monthly put-in and a bill category are read off the words; `pbCatMatch` maps dining/fuel/grocer words onto his categories; a repeated name updates rather than doubles). **The pocket** (Eric: "keep the size very small … maybe even slightly smaller. I do want an edit button and a flush button, and maybe instead of the X we just have a flush button for each individual thing"): a row is ✓ · ✎ · ⤵ — `pocketEditStart/Save` edits the words in place, `pocketFlushOne(id)` sends that one item to the log as a `🎒 Flushed` note (it lands on the board and the summary); the ✕ and ➡ Tomorrow buttons are gone (`pocketTomorrow`/`pocketDrop` stay as functions); 14px words, 34px plates. **The board opens closed** (Eric: "when I open it with all the drop-downs open, there is just tons and tons of information in front of me. I can't get to anything quickly"): `_brdShow` is the ONE open heading (`brdFold(j)` toggles it, `openReview` resets it), every heading reads its count and `· tap to open`; `_test679/680/682` open the heading under test first. **The budget chat** (his claude.ai summary of 2026-09-16, pasted 2026-09-15) lives only in the chat — not here, not in the code. `tests/_test684.js`; `_test673`, `_test674` and `_test683` re-pinned. **Next:** the two extra email sorts, Phil's time-clock note, the budget page as Eric grows it; the tags question (every text and email sender becomes a tag — `prefs.tags.unshift(from)` at the text and mail doors) — a sender switch and a 🧹 tidy list offered 2026-09-15, on his go.
- Before that: **v6.83** (pushed 2026-09-15): LINE BY LINE; THE PC BAR AT THE BOTTOM; BIGGER LINES IN THE VAULT. Eric: "In the vault, I want bigger lines between the different entries. yes to the line by line, also on the pc version id like the mileage and vault 4 buttons to be on the very bottom, theyre fine on the phone." **Line by line** (his 2026-09-13 spec): `wizLinesHtml(md)` draws the answer one locked line at a time (`.wl-line` > `.wl-t` greyed, not editable, + `.wl-x` ✎) in BOTH `showWizFull` and `showInstantAnswer`; `_lastAnswer.lines` = the split; `wizLineFix(i)` opens a box under that line with the locked words above it; `wizLineSave(i)` → `applyCorrection(fix, line)`: the record's `ai` gets `ERIC CORRECTED (re "<line>"): fix`, the log a `✎ Correction — re "<line>": fix` note (job `—`), the kept answer `fix[]` gets `{at, t, line}` (`wizKeepFix(corr, at, line)`), and **`prefs.wizFixes`** (last 50 `{at, q, line, t}`) — the durable list `buildAskContext` now hands the Wizard as `CORRECTIONS ERIC MADE` on every question (the `ai`-field note rode as `photoDesc` cut at 300 chars, so a fix did not always reach the next answer); then it closes the full screen and `askInstant(q)` again. A reopened answer (`wizReopen`) shows its earlier fixes under their lines (`.wl-fix`). The sort page's WIZARD heading opens with `✎ LAST FIXES` (`.rev-wizfix`, 5 rows, not counted as waiting). The whole-answer `✎ Wrong? Correct it` stays. **The PC bar:** `@media (min-width: 701px)` fixes `.capture` to the bottom, centred `min(100%, 1060px)`, body padding 108px (the phone rule was already fixed-bottom). **The Vault:** `.vault-row` 18px padding, a 3px brass-tinted rule between entries. `tests/_test683.js`. **Next (v6.84, Eric's go given 2026-09-15):** the File Cabinet plate to the museum (hidden like the Note plate, the panel and code stay) with a 💰 PERSONAL BUDGET plate in its place — a page for personal expenses, ways to save, debt pay-offs; "eric loves graphs and charts and numbers, make something simple but informative". No personal-budget chat exists in this PC's Claude sessions (searched 2026-09-15) — his claude.ai chats are not reachable from here; build a starter and let him feed the numbers.
- Before that: **v6.82** (pushed 2026-09-15): THE POCKET AND THE GRINDER LAND IN THE SAME PLACE. Eric: "When I make a pocket item, it just goes into general notes. And then, when I check it off as done, it goes to the board. Is that correct? … if I'm adding things to the grinder or to the pocket list, I need it all to end up in the same place. When I ask for a summary or ask the wizard a question, the pocket checklist affects that." The truth before: an open pocket item lived on the main-page list only; ✓ made a log note (off the board); only a leftover (flushed/overflow/swept) became a board line. Now `boardLines()` adds every OPEN pocket item as a line (`k: 'p'`, under GENERAL, meta `🎒 pocket · today/tomorrow`, no lamps, no sub-lines, no crew plates): cubes ride on the item (`it.p`), ⬆⬇ place it (`it.bOrd`), ✎ edits `it.t`, ✓ = `pocketDone` (a ✓ note on the log, off the list, off the board; Undo puts the item back and drops the note), `⇄ Put it on a job…` = off the pocket + `addEntry('Note', t, job, { board })` (a grinder note on that job). The BOARD tab count includes them. The Wizard: POCKET LIST already carries the open items, so `boardCtx` skips `k: 'p'` (no second copy). `tests/_test682.js`. **Next:** line-by-line Wizard corrections (his item 4), the two extra email sorts, Phil's time-clock note.
- Before that: **v6.81** (pushed 2026-09-15): EVERY ✕ BLACK ON GOLD; NO SILENT JOB DEFAULT; THE WIZARD SEES THE POCKET AND THE BOARD. Eric: "There are still some X's that need to be black for closing windows. Also, check: I'm still getting the Hertz job tagged on stuff. I don't know if it's tagged or if it's always the default … Also, the pocket list: when I push Done or when I make a pocket list item, are those searchable by the Grinder? … I want to make sure that as I make a checklist and check them off, the wizard knows that those items are done." **(A)** `.x-plate` (+ `.x-small`): the lightbox ✕ Close, `.sv-close`, `.ask-inline__x`, `.lb-close`, the plan rack, the estimates board, the materials board (both) — gold, 3px edge, glow ring, ✕ `#111`; `.cp-x` black too. **(B) the default:** `renderAllJobChips` fell back to `prefs.lastJob` then `ordered[0]` (the first job on the list) whenever `curJob` was empty — that was the Hertz on everything. Now `curJob` stays `''` until he taps a plate (session only); `saveHours` refuses without a job ("▲ Pick the job first"); the bill card's `job` = the grinder wheel's pick, else the entry the photo landed on (`billEntryOf`), else none; `toBookkeeper`'s share word = the wheel's pick or `—`; a Wizard correction note and a pocket ✓ note file under `—`; the board add box defaults to GENERAL. **(C)** `buildAskContext`: each log row carries `pocket` (DONE / back / NOT DONE, in words) and `board` (`{p, done: 'DONE by Phil'|'open', sub}`); new blocks POCKET DONE (14 d), POCKET NOT DONE, BOARD — OPEN LINES (from `boardLines()`, never personal), BOARD — DONE THIS WEEK (who). Pocket notes are plain entries, so the running-log search already finds them. **(D) the Vault, 💳 CARDS** (Eric: "In the vault, I'd like to store credit card information too. Make a section or a tab for that. Same thing: be able to copy and paste."): two tabs (`.vault-tab`, `vaultTab`, `vaultShowTab`), one encrypted file — a card is `{ kind: 'card', site, num (digits only), exp, cvv, name, zip, notes }` in `vaultData`; `#vaultCardForm` (`vaultEditNewCard`/`vaultEditCard`/`vaultSaveCard`), `vaultCardRow` shows LAST FOUR ONLY until 👁 (the number never sits in the HTML — `vaultCopyField(id, field)` looks it up by id), 📋 Number/Exp/CVV/Name/Zip; the add button follows the tab (`vaultAddTap`). Never a log entry, never in the Wizard context. **(E)** `openPortalWin` draws a static `← Back to the main page` plate (`.x-plate`) under `#portalList`, in every state (Eric: "I keep pushing Build the page"). **(F) Personal first** (Eric: "Whenever there's a drop-down list and personal is on it … I want the personal to always be on top … let's take it out of the number 1 box and leave it just in the number 3 box"): `personalFirst(list)` wraps `recentJobsOrdered()` (clock/pt wheels, File Cabinet, the bill wheel), the hours/mileage/clock-out grids, `cardJobList`, the board's move wheel; the grinder's ① wheel EXCLUDES the Personal job (`isPersonalJob`) — the 🔒 Personal chip in ③ is the same lock (`isLocked`: flag, tag or job, v6.53). `tests/_test681.js` (24 checks); `_test667` "the wheel" = `qnJobPick`; `_test673` pocket ✓ under `—`. **Next:** line-by-line Wizard corrections (his item 4), the two extra email sorts, Phil's time-clock note.
- Before that: **v6.80** (pushed 2026-09-14): THE ✕ BLACK; THE BOARD, ROUND TWO. Eric: "i like the new gold X close window buttons but make the X black and then it's perfect. the board is looking good, next make it so i can edit the text and arrange the notes' order. also there should be more cube lights maybe? one saying that phil and the crew can see it and one that shows if its on the client page?" `.win-x` / `.panel-close` glyph `#111` in every skin (`--accent-ink` is white on the light skin). Board: the open line's words become a box IN the line (`.bd-inline`, id `brdEd-<k>-<id>`, Enter or ✎ Save the words); a ✎ hint after closed words; `⬆ Up` / `⬇ Down` (`brdShift(k, id, ±1)`) — the first move stamps every line in that heading with its place (`e.board.ord` / `t.bOrd`), `brdCmp` shows unplaced lines first (priority, newest) then placed ones in order, so a brand-new line lands at the top until he places it; `brdMove` to another heading clears the place; two lamps after the cubes (`.bd-lamps` → `.bd-lamp` 👷 lit when `e.vis` (`brdCrewWho`), 🏠 lit when `e.jrnSent`/`e.jrnDone`/`budg:'sent'` (`brdPageState`; stamped-not-released reads STAMPED, unlit), words in the title and in the open row: `👷 CREW: Phil · 🏠 ON THEIR PAGE — sent Sep 10`). `tests/_test680.js`; `_test678`'s ✕ colour check pinned to black. **Next:** line-by-line Wizard corrections (his item 4), the two extra email sorts, Phil's time-clock note.
- Before that: **v6.79** (pushed 2026-09-14): THE BOARD. Eric (his "style summary" spec, then "yes to all"): "anything added to the grinder should be able to just add thoughts to the grinder as i go and the summary list changes as i go and things get added under the right job or category … a super quick priority light … three cubes for each level of priority but keep those small and not flashing … arrange or edit the notes … a check box that phil or i can both touch … each category could have a sub category … each week the list will populate and get organized and i can move things and add priorities and add last minute things and alert phil." The THIRD tab of the summaries page (`_revTab = 'board'`, `renderBoard()`, `revTabsHtml` three tabs with the open-line count, `.rev-tabs` `repeat(3, minmax(0,1fr))`); the 📊 plate and the SUMMARY button now `openReview('last')` = the tab he used last (`lsSet daylog-revtab` in `revTab`, summary until he has been on the board). **A line** = a grinder Note from the last 14 days OR any entry he has touched until ✓ (`boardOn(e)`: never personal/sample, never texts, emails (unless ground in — the v6.71 hand-off calls `brdTouch(m)`), receipts, done/back pocket notes, completions/replies/answers/`boardRef` notes, non-Notes), plus every open top-level to-do (`todos`, pri → cubes), plus pocket leftovers (Notes with `pocket` flushed/over/swept, the stamp stripped on the line). `e.board = { p: 0-3, done: ISO|'', by, sub: [{t, done}], at }` lives ON the entry (syncs with entries.json). Headings = the jobs, the job on the clock first, GENERAL (`—`) last, only those with lines; fold per heading (`_brdFold`). **Per line:** `.bd-pri` (three `.bd-cube`s, filled = lit; tap `brdPri` cycles 0→3; the word rides in the label and in the open row) · the words (tap `brdOpen` → the row: `brdEditSave` rewrites `e.details`/`t.text` keeping a 🎒 stamp, `brdSubAdd`/`brdSubSave`/`brdSubDone` sub-lines under an entry, `brdMove` = the ⇄ wheel changes `e.job`/`t.job`, `brdCrew(id, name)` one plate per crew name) · `.bd-ck` ✓ (`brdDone`: entry → `board.done`+`by:Eric`, to-do → `toggleTodo(quiet)`; `toastUndo`). `brdAdd` (the box at the top) = a real `addEntry('Note', …, { board })`. **The crew half:** `publishSharedNotes` and the preview map spread `brdShareBits(e)` (`board: {p, sub}` for any line ever on the board, `boardDone: by`) onto the FROM ERIC row; `renderCrewShared` shows the cubes (`.bd-pri--ro`), the sub-lines, and `✓ Done — tell Eric` (`crewBoardDone(id)` → `addEntry('Note', '✓ DONE: …', job, { vis: 'Eric', boardRef })` + `daylog-board-done` on the phone) or `✓ DONE — <who>`; Eric's `checkCrewLogs` reads `boardRef` off the crew entries file, marks the line `done` by that name and republishes. `brdCrew` sets `sendTo`+`vis` for the man (tap again takes it back) and sends the generic push `📋 Board — Something new is on your board` (no names, no dollars). `tests/_test679.js`; `_test676` updated (three tabs, `openReview('last')`). **Next:** line-by-line Wizard corrections (his item 4), the two extra email sorts, Phil's time-clock note.
- Before that: **v6.78** (pushed 2026-09-14): ACCRUAL WRITTEN WHERE THE EXPORTS GET MADE; A BIGGER DROP BOX. Eric asked "accrual with the reports correct?" (yes — the Sales by Product/Service Detail must be ACCRUAL, the other two just ALL DATES; source = `QB Exports/HOW TO EXPORT - read me.txt`), then "put that in the quickbooks updater instructins next to sales by product. make the box for dragging qb exports to, bigger." Setup → 💼 QuickBooks updater (`#qbSec`): the hint now says report period ALL DATES on every report (a window cannot rebuild the ticker), and line 1 carries Accounting method: ACCRUAL, where to set it (Customize → General → Accounting method), the footer check ("Accrual Basis") and why (Cash Basis shows only PAID work, so open balances vanish); `#qbDropZone` is 160px+ tall, 3px dashed, 17px words, still the same drop/tap handlers. **The ticker question (2026-09-14):** `profit-ticker.html` stands on the 08-25 All Dates data because the 09-08 run got only Logan's new WINDOWED weekly reports and the ticker step only knows how to REBUILD from a full All Dates Transaction List; the client pages already move by DELTA (baseline ledger `App Data/qb-ledger.json` + windows on top). Eric asked whether the ticker could do the same — answer given: yes, the books run would need a cash ledger in Dropbox (bank-in/bank-out rows from the last All Dates Transaction List), weekly windows matched by date/number/name/amount, and the ticker rebuilt from that ledger every run; spec on his "go". **Also in v6.78, five eye-catch asks (Eric, 2026-09-14):** (1) every closer is bright and heavy — `.win-x` filled brass with a 3px edge and a glow ring, `.panel-close` the same, `.panel-back` ("← Back to dashboard") a 3px brass edge, tinted, 16px bold; (2) the job card's `.jc-head` words (ADDRESS, PEOPLE, CODES…) are brass, 16px, with a brass rule — "the first ones my eye sees"; (3) `showPhoto` puts the working band up (`📷 LOADING THE PHOTO…` → `📷 ✓ PHOTO` / `⚠ PHOTO DID NOT LOAD`), and the lightbox's original-photo link says `FETCHING THE ORIGINAL…`; (4) the summary gets a `🎒 POCKET — not done` heading after TO GET (`S.pocket`: notes with `pocket` flushed/over/swept, newest first; TO GET now lists the live pocket + done notes only) with one tap `↩ Pocket it for tomorrow` (`pocketBack(id)`: `pocketWords(e)` back onto the pocket for tomorrow, the note marked `pocket:'back'`); `_test676`'s heading order includes `pocket`; (5) `.rl-head .qn-div-label` (📜 RUNNING LOG) reads 17px so it beats the ALL plate under it. `tests/_test678.js` (18 checks). **Parked with a plan given 2026-09-14 — THE BOARD (Eric's "this is the style summary i was thinking"):** grinder thoughts land under the right job/category as he goes; the summary page shows every category with a small three-cube priority light (not flashing), a ✓ box he (later Phil) can touch, sub-lines under a line like the estimates page's sub-categories, move a line between headings, add last-minute things, alert Phil; weekly it repopulates. Steps proposed: ① a line per grinder note under its job/heading with the cube light and ✓, ② sub-lines + move, ③ Phil's ✓ and the alert. Build on his go. **Next:** line-by-line Wizard corrections (his item 4), then the two extra email sorts, then Phil's time-clock note.
- Before that: **v6.77** (pushed 2026-09-14): THE JOB CARD READS FIRST AND OPENS WHERE HE LEFT IT; THE POCKET LIST FRAMED. Eric: "on the job card make it so it starts empty or at least on the last one opened, its always opened on hertz. and rework the job card layout and font etc i like the size of the pick the job first window its great but after that gets confusing to the eye" and, mid-build, "make the pocket say pocket list and make it seperate from the pick the job 1". **Where it opens:** `openJobCards()` = `_cardJob || lsGet('daylog-cardjob')` — the last card opened on THIS phone (written in `openJobCard`), else ▲ Choose the job; never `curJob` (v6.65 used it, and the clock's running-shift job made every open Hertz). **Two faces:** `openJobCard(job, edit)` — read-only by default (`cardViewHtml`: address 18px + 🗺 Maps plate, one `.jc-person` plate per person with `.jc-tap` tel:/mailto: rows ≥44px, `.jc-code` big numbers, `.jc-notes`), then the hand-out ticks, then `✎ Edit this card` and the 👷/🔒 line; `_jcEdit` = the form (the old inputs, heads shortened, code rows a `.jc-coderow` grid so the ✕ is never clipped). ✎ Edit → `openJobCard(job, true)`; ✓ Save → `cardSave()` → `openJobCard(job, false)`; an EMPTY card opens on the form; the same card keeps its face across add/drop/tick re-renders. **Nothing typed is lost:** `cardStash()` (`cardRead()` + save + publish, no toast) runs when the wheel turns mid-edit and in `closeReview()` while `_jcEdit`. Crew phones read the same view. **The pocket:** `.pk-title` reads 🎒 POCKET LIST; `.pk-card` is its own framed box (2px border, 12px radius, surface-1, 14px gap before ①) still INSIDE `#qnCard` so the grinder stays first after the plates (`_test612`). `tests/_test677.js`; `_test665`'s "with a job set" check re-pinned to the last-opened truth. **Next:** line-by-line Wizard corrections (his item 4), then the two extra email sorts, then Phil's time-clock note.
- Before that: **v6.76** (pushed 2026-09-13): THE SUMMARIES PAGE. Eric: "build the summaries page and make a button for it up to the right of job cards at the top of main page. make it so the email sort is in the summaries page and the items to review page in it. and keep our categories for the summary page and then we will work on it from there." One window (`#revModal`), two tabs (`_revTab`, `revTabsHtml`, `revTab`): 📥 SORT (the v6.69 pile, count on the tab) and 📊 SUMMARY (`renderSummary`, `SUM_SECS`: NEEDS YOU · PEOPLE · MONEY · PHOTOS · WIZARD · SCHEDULE · TO GET · SENT TO LOGAN · THE RECORD; `_sumSpan` today/this week, `sumEntries()` never a personal entry; one open at a time, `_sumSec`). Sources: the log (emails, texts, receipts with `rcptLampHtml`, photos per job, Bookkeeper/shared/`budg:'sent'`, hours and miles per day), `todos` with `due` within 7 days, `mailWhy` deadline/schedule emails, the pocket list and its notes, `wizLog()`. Doors: a 4th plate on `#scRow` `📊 Summary` with the waiting count (`#scSumN`, set by `renderPendBanner`) and the SUMMARY header button, both `openReview('summary')`; `openReview()` with no tab = SORT. **Moved in, off the main page (CSS `display:none !important`, still rendered):** `#pendBanner` (its words feed tests and the plate) and `#mailAskCard` — `renderMailAsk` now wraps `mailAskHtml()`, whose HTML sits at the top of PEOPLE in BOTH tabs (`sum-mail card`). The old 📊 Summaries panel (July pages, the Ask box) stays behind "📄 Older summary pages". `tests/_test676.js`. **Four plates across:** `.sc-row` is `repeat(4, minmax(0, 1fr))` — v6.56's lesson again: with `1fr` the Summary plate pushed the page to 423px in the steam skin, and a page wider than the phone inflates `innerHeight`, which un-pinned every sticky footer (`_test642` 1 + 9b and `_test656` caught it); the plates read 12px with 4px padding, the count is a corner badge (`.sc-n`, absolute, dark on the plate) so it never widens the row, and a crew phone hides the 4th plate as it hides the Review banner. `_test638` reads the CREW gate off `mailAskHtml`; `_test648` re-pinned to the card being off the main page. **Next:** line-by-line Wizard corrections (his item 4), then the two extra email sorts, then Phil's time-clock note.
- Before that: **v6.75** (pushed 2026-09-13): THE POCKET, ERIC'S WAY. "make the pocket with tiny + and - so i can make it hold more or less things and any overflow goes to the grinder so it'll come up in summaries. and the flush button so it clears and sends all to the grinder. and put it just above the 1 pick the job window." `#pocketCard` now lives INSIDE `#qnCard` above `.g-step[data-step="1"]` (`_test612`'s one-screen thresholds moved to `top < 600 / bottom < 800`; the grinder card is still first after the plates); the cap is `prefs.pocketMax` (`pocketCap()`, 2–30, 8 default) shown as `n/N` between tiny − and + (`pocketCapSet`); over the cap the OLDEST leaves as a `🎒 Overflow — text` note tagged pocket (`pocket:'over'`, `pocketOverflow()`), the newest stay; `⤵ Flush` (`pocketFlush()`) sends every item as `🎒 Flushed — text` (`pocket:'flushed'`) and clears the list. `_test673` updated (+3 checks). **Next: v6.76 THE SUMMARIES PAGE** per Eric 2026-09-13: "build the summaries page and make a button for it up to the right of job cards at the top of main page. make it so the email sort is in the summaries page and the items to review page in it. and keep our categories for the summary page" — a 4th plate on `#scRow`, the page = the sort page + a SUMMARY tab (NEEDS YOU / PEOPLE / MONEY / PHOTOS / WIZARD / SCHEDULE / TO GET / SENT TO LOGAN / THE RECORD, today + this week), the 📧 card and the Review banner move in and off the main page.
- Before that: **v6.74** (pushed 2026-09-13): THE NOTE PLATE IS PARKED. Eric: "the note button between hours and vault, i think that can go away … put it in the historical app folder, like we dont need it anymore but could always come back to it someday." The grinder has been the note since v6.22; `body.parked-630` now hides `.cap-btn[data-panel="note"]` too and the bar is `repeat(5, …)` — Mileage · File Cabinet · Hours · Vault · Setup. The button, `#panel-note` and `openPanel('note')` all stay (someday = take the class off). `_test642` item 13 counts five; `tests/_test674.js`. **Next: v6.75 the summary page** — and per Eric 2026-09-13, it folds in the Review banner under the clock and the 📧 card on the main page ("itll cover both"): both leave the main page, the SUMMARY header button opens the sort/summary page.
- Before that: **v6.73** (pushed 2026-09-13): KEEP THE WIZARD + THE POCKET LIST (Eric's "1-4", the first two; 3 = the summary page and 4 = line-by-line corrections follow). **Keep the Wizard:** every instant answer → `wizKeep(q, a, srcIds, brain)` → `App Data/wizard-log.json` (newest first, 500 kept; `daylog-wizlog` mirrors the last 100 on the phone; `wizLogLoad()` after the pile loads merges the file with what the phone kept offline; `wizLogSave` debounced 1.5 s); `_lastAnswer.keep` = the row's `at`; `applyCorrection` → `wizKeepFix` attaches `{at, t}` under `fix[]` on THAT row. The sort page's WIZARD heading lists the last 7 days (`revWizRows`, 10 rows), `🧙 Open the answer` → `wizReopen(at)` → `showWizFull` with `_lastAnswer` set so ✎ Wrong? Correct it lands on the right answer; history never wins the first open (the fullest LIVE section does) and is not counted as "waiting". The no-key hourly path is not kept (its answers are already pages in `/Summaries`). **The pocket list:** `#pocketCard` right UNDER the grinder card, above the 📧 card and the running log — v6.12's rule that the writing box is first on the screen holds, and `_test612` enforces it (a first draft above the grinder failed it) (`pocketAdd/Done/Tomorrow/Drop/Sweep`, `renderPocket`, `prefs.pocket` = `[{id, t, day, ts}]` so it syncs), TODAY and TOMORROW only, `POCKET_MAX = 8` (the ninth is refused in words), ✓ Done → a `✓ text` Note on the wheel's job tagged `pocket` (`pocket:'done'`), ➡ Tomorrow → `day = tomorrow`, ✕ → gone with no note; `pocketSweep()` after the pile loads: `day < today` → a `🎒 Not done — text` Note tagged `pocket` (`pocket:'swept'`), off the list, one toast. `buildAskContext` hands the Wizard `POCKET LIST (…)`. `tests/_test673.js`. **Next: v6.74 the summary page** (second tab on the sort page; NEEDS YOU / PEOPLE / MONEY / PHOTOS / WIZARD + SCHEDULE / TO GET / SENT TO LOGAN / THE RECORD; today + this week; from the log, the receipts flow, the pocket notes and the kept answers; the 📊 Summaries button opens it with the old pages beneath), then **v6.75 line-by-line corrections**.
- Before that: **v6.72** (pushed 2026-09-13): NEEDS REVIEW. Eric: "do 4 but make it 'needs review' and it goes on the summary page." The ★ mail card gains `💬 Needs review — keep it under NEEDS YOU` (`reviewAct 'needs'`): the card STAYS in the queue with `payload.needs = true` + `needsTs`, `revSecOf` puts it under NEEDS YOU, its kind line reads `💬 NEEDS REVIEW — an email you flagged on <date>`, and its buttons become `✓ Reviewed — done` (`'log'`) / `📥 Send to grinder` / `✕ Not important`. Saved through pending.json like any card, so it survives a reload and will feed the summary tab (step 3) from the same queue. **Where the summary lives today (told to Eric 2026-09-13):** there is no live summary page yet — the sort page (Review) is the live half; 📊 Summaries (header button, `panel-summary`) lists the summary/answer pages the chat side wrote to Dropbox `/Summaries/` (last ones July 2026) and its Ask box writes `App Data/summary-request.txt` for the chat side; the Wizard's instant answers are kept nowhere (15-minute follow-up memory only) — step 2 keeps them, step 3 shows them. `tests/_test672.js`; `_test671` expects the fifth button.
- Before that: **v6.71** (pushed 2026-09-13): AN EMAIL INTO THE GRINDER. Eric: "on the email sort lets do 'important, send to grinder'. keep 'never from sender' and 'personal' which would still send to grinder but lock as personal." The ★ mail card's buttons are now exactly four: `📥 Important — send to grinder` (`reviewAct 'grind'`), `🔒 Personal — grinder, locked` (`'personal'`), `🚫 Never this sender`, `✕ Not important — it stays on the log`. `mailToGrinder(pay, personal)`: the email is already a log entry (v6.67), so the card hands THAT entry to the grinder — `askText` gets `Email from X: subj\nbody`, `qnSel` = the entry's tags (the sender lit), `_grindMail = e.id`; `saveNoteFrom` then UPDATES that entry (`Object.assign(m, nExtra)`, details, job) instead of `addEntry` — the 📧 chip, `mailAddr`, `mailSubj`, arrival date survive, no second copy, no undo toast (an undo would delete the email); the hand-off only applies while the box still starts with `Email from ` (a fresh note typed over it is a new note). Personal locks the record at the TAP (`personal:true` + `Personal` tag, `vis/sendTo/ask` dropped), puts the exact address on `prefs.mailPersonal` and republishes the rules so the watcher locks the next one unread. Grinding an emailed invoice with 🧾 + a category pulls the `$` into `ai` and it lands under MONEY as a receipt to approve. `tests/_test671.js`; `_test641`/`_test667` updated to the four buttons. **Suggested next sorts, offered 2026-09-13, not built:** 🧾 It's a bill (job + category on the card, straight to receipts), 📗 For Logan (Bookkeeper tag + route), 📅 Has a date (→ the pocket list / SCHEDULE), 💬 Needs a reply (a lamp until answered), 👷 For Phil (unlock for him), 📁 File it (File Cabinet folder).
- Before that: **v6.70** (pushed 2026-09-13): THE SUGGESTED TO-DOS ARE OUT. Eric, first look at the sort page: "take out the suggested to dos as well the ones that say 'nothing logged on carrick…' isn't useful." `RADAR_TODOS = false` switches off the three radars in `radarSweep` that only ever SUGGESTED a to-do (the v5.04 quiet-job nag `jobsilence:`, the v5.05 estimate chase `estchase:`, the v5.05 oil-change nudge `truck:`; the code stays); `checkPending` sweeps any of those still waiting out of the pile once, marks them done (pending.json is the app's to prune) and saves. Still carding because they are real events: a crew clock left running (`openshift:` in checkCrewLogs), a policy/permit about to expire (`renew:`), tomorrow's weather on weather-sensitive work (`weather:`). The 🧹 nag row simply has nothing to offer now. Live pile on 2026-09-13 before the sweep: 17 waiting = 5 jobsilence + 3 estchase + 1 text + 8 mail. `tests/_test670.js`.
- Before that: **v6.69** (pushed 2026-09-13): THE SORT PAGE (step 1 of three). Eric: "the review … feels complicated and long list … drop down each section … a 10 minute sort per day might get a lot done if its organized … but dont want it get too complex too quick." `renderReview` draws the SAME cards with the SAME buttons under five fixed headings (`REV_SECS`: ⚠ NEEDS YOU · 👥 PEOPLE · 💵 MONEY · 📷 PHOTOS · 🧙 WIZARD), `revSecOf(p)` (bill/qb → money; crew with a photo → photos, else people; mail → people; text → need if hot else people; everything else → need), one open at a time (`_revSec`, `revSecTap`), opening on the fullest (a tie → the earlier heading; `openReview` resets it), folded sections keep their cards in the DOM `hidden` (counts and searches still see the pile). MONEY also lists each job's receipts to approve (`revRcptRows` via `rcptCount(portalAppJob(c))`) with an Open button → `openRcptReview(i)` with `_rcptBack` so `closeRcptReview` comes straight back to the sort page on MONEY. WIZARD is empty until questions are kept. The 🧹 plate stays. `tests/_test669.js`. **Parked, Eric's go per step:** step 2 = the pocket list (today/tomorrow, eight items, leftovers sweep to the log as "pocket" notes) + keeping his Wizard questions (`App Data/ask-log.json`, then a pattern study); step 3 = the summary tab (same headings, what got done, by day: SCHEDULE / TO GET / SENT TO LOGAN / THE RECORD). Also parked: the line-by-line Wizard correction (each answer line locked, ✎ per line, fixes onto the source records + a last-50 fixes list, re-answer at once).
- Before that: **v6.68** (pushed 2026-09-12): HOMEOWNER PINGS (item 5 of Eric's "do 1-5"). The spec parked since v6.35, built as written: **opt-in on the client page** (`c/index.html` `bellHtml`/`bellTap`, a 🔔 PINGS card before the foot: Turn on → permission → `register('/sw.js')` → `pushManager.subscribe` with the app's `VAPID_PUBLIC` → POST `{c, push:{sub}}`; Turn off → POST `{c, push:{off}}` + unsubscribe; `localStorage clore-bell-<code>`; in Eric's preview (`pv=1`) it is words only so his phone never lands on a homeowner list; iPhone needs a Home Screen copy, so the page now carries `apple-mobile-web-app-capable` and a per-code manifest link `?c=<code>&manifest=1`). **Per-client subs walled off**: `client-portal` (source now under `fnsrc/client-portal.mjs`, read back from the bundle like pushlib in v6.27; it has no deps so the bundler COPIES it verbatim — `_test650` reads the shipped file by its own words, and an esbuild pass rewrites the quotes) writes `Client Portal/push-<code>.json` (endpoint + two keys + ts + ua, 20 phones max, code must exist, bad sub = 400) and serves the manifest; never `App Data/push-subs.json`. **Sending**: `fnsrc/client-push-core.mjs` (pure logic, no imports — the suite runs it in node) + `fnsrc/client-push.mjs` (POST `{c, kind}` with `x-push-secret`, web-push) + `fnsrc/client-push-drain.mjs` (netlify.toml `schedule = "0 17,18 * * *"` = 9am Alaska summer/winter, the other run holds). ONLY two kinds, `journal` and `budget`; fixed words (`WORDS`, no name, no dollars; the payload carries `url: /c/?c=<code>` so the tap opens THEIR page — `sw.js` reads `data.url`); 9am–5pm Alaska (`alaskaHour` via Intl America/Anchorage) else queued in `Client Portal/push-queue.json` (one per client+kind); one ping per client+kind per 30 min (`push-log.json`); dead subs (404/410) dropped. **App triggers** (`cpPush(code, kind)`, needs `prefs.pushSecret`): `journalRelease` after a NEW week lands (an ✎ edit does not ping); `estPublish` only when `budget`/`upcoming` actually changed. Bundler ENTRIES now include `client-push`, `client-push-drain`. `tests/_test668.js` (three halves: the core in node, the portal door in node, the app and the homeowner page in Playwright).
- Before that: **v6.67** (pushed 2026-09-12): CAPTURE EVERY EMAIL, THE DOUBLE CHECK, THE BILL CARD. Eric: "do 1-5 and explain 6." (1) ✓ Always files what was waiting as SEEN — `mailFileWaiting(list, seen)`; `mailSay`/`mailSayDom`/`mailListAdd` pass true. (2) CAPTURE EVERYTHING — `mailSort`: `const known = true`; every sender not on 🚫 NEVER / 🔒 PERSONAL is kept; the judge only says WHERE it lands (★ important = card + entry; maybe/ignore = entry); no "keep them or not?" question, no ✓ Seen chore; leftover `prefs.mailAsk` rows file as seen on the next sweep (`mailFileLegacyAsks()` in `checkInboxTexts`), rows with no words drop; the mail card's known line reads "✓ on your log". (3) THE DOUBLE CHECK on the 📧 card — `mailChecks()`/`mailCheckWhy()`: every skipped email of the last 7 days is re-read by plain word rules (`MAIL_CHECK_RE` money/date/due words, a dollar figure named whole, plus his job names exactly; `MAIL_BULK_RE` leaves out no-reply/newsletter/promo addresses) and listed with what it saw (`saw "$300"` · `the job name "Hertz"`), ↩ Rescue (`mailRescue`) or ✓ Fine (`mailCheckOk` → `r.ok`); with nothing flagged the card SAYS so. Header = this week's counts (`this week: N important · N maybe · N skipped`); ? MAYBE is read-only with 📣; ○ SKIPPED keeps the spot check. (4) THE BILL CARD — `kind:'bill'` (`PEND_KINDS.bill`), `billMaybeSuggest`: a PAID slip is never a bill (`BILL_PAID_RE` without `BILL_DUE_RE` → no card); the card shows the vendor (`billVendorOf` = the 🏪 line of the read), the amount to the cent, the date, what the read saw, 📷 Look at it when the photo's entry is on the log (`billEntryOf` by file name), and the job wheel (`revJobWheel`, wheeled in `reviewAct`) pre-lit to the job at photo time; buttons 📗 Logan pays it (Bookkeeper tag on the entry) / 🔁 Autopay (`prefs.billAuto[vendor]`, exact name, the radar skips it) / ✓ I'll pay it (a to-do with the date, on the wheel's job) / ✕ Not a bill; the push says only that a bill is waiting — the old one carried the amount and the vendor, a standing-rule break. `tests/_test667.js`; `_test638/640/641` updated to the new truth. **Item 5 (homeowner pings) is the next build; item 6 (Phil's time-clock file) explained in the 2026-09-12 reply.**
- Before that: **v6.66** (pushed 2026-09-11): CALM BRASS + THE ESTIMATES PAGE LAID OUT TO BE READ. Eric: "something still easy on the eyes in the dark but also easy to read … important things draw my eye … especially the estimates page … i quickly get overwheled" → three research agents (WCAG 2.2/APCA, Material + Apple dark mode, NN/g hierarchy; a read-only audit of the board) → proposal page https://claude.ai/code/artifact/00301cd4-66d3-453b-a64c-8f624e799608 → "build calm brass and estimates page layout". **The skin** `data-skin="calm"` (⚙ Appearance → 🕯 Calm Brass, `SKIN_NAMES.calm`, always dark): page #14110d, card #1e1a14, raised #2a2419, text #f4eee0 / #c8bea6 / #8e8570, brass #f2c66d (dark words on it), green #8fd694, orange `--warn` #ffa657, coral `--err` #ff8a70; chips are quiet raised plates (`.pick-chip` tonal, `.sel` = the only brass), ghosts outlined 44px, hints 13px. **The layout** (every skin; `renderEstimates`): `EST_PHASES` — a fixed table of the 55 categories in five build phases (site / shell / mech / finish / other; a custom category lands under OTHER COSTS), `estPhaseOf`; the board opens on **⚠ NEEDS YOU** (a category with a bill waiting or over its estimate) then the phases in build order; one `rowOf` per category — `.est-line` = name / `.est-num` (estimate, else what is on the way, else the books figure, else —) / the lamp (`.est-lamp`, same words `✓ ON PAGE` · `🏢 office` · `⚠ SURE?`), the detail on `.est-sub` (`✓ ON THEIR PAGE · bid $ · N bids, none picked · $ in · 📥 $ on the way · over $ · 🏠 they picked…`); EMPTY rows (no bid, bill, books money, not lit, not open) render `hidden` and fold into one `▸ +N empty — tap to show` line per phase (`estShowEmpty(id)`, `_estShowEmpty`, reset on open; `estAddCat` un-folds its phase); the two long hint paragraphs sit behind `▸ ? How this works` (`estHelp()`, `#estHelpBox`, `display:none` — the words stay in the DOM); the 💹 markup input moved INTO the 📥 bills strip (its value is printed on the header line and the strip's folded line). Calm-only CSS: rows are hairlines (`border-bottom: 1px`), the open row a box, name 16 / number 17 / detail 13, chips ≥36, ghosts ≥44. Steamworks and the rest keep their boxes; the v6.42 `.est-board .pick-chip` 34px rule stays for them. `tests/_test666.js` (computes real WCAG ratios from computed colours). **Parked:** the 💳 bill-radar card redesign, ✓ Always → seen, "capture every email, sort none" (spec in the 2026-09-11 reply).
- Before that: **v6.65** (pushed 2026-09-11): THE PROJECT PORTAL IN ITS OWN WINDOW, and three first-name defaults gone. Eric: "when i click on the project portal and it opens up in the setup menu, id like to separate that and have the project portal open to its own window and no longer in the setup." `#portalWin` is a `.rev-modal.mat-full` at z 70 — UNDER `#revModal` (80) and `#cliPrev` (120) on purpose, so 💰 Estimates / 🧾 Receipts / 📖 Journal / 📋 Build List / 👁 View as this client open on top of it and closing them lands him back on the portal, not the main page. `openPortalWin()` / `closePortalWin()`; `renderPortalList()` draws into `#portalList`, which now lives only inside that window (the Setup section `#portalSec` is gone; `jumpSetup('portalSec')` routes to the window; the 🏠 plate calls `openPortalWin()`). `modalOpenSync()`: `body.modal-open` follows whatever is still on screen (`.panel.open` or `.rev-modal.show`) — every revModal closer, `closePanels` and `openPanel`'s toggle use it, so a window closing over the portal no longer unlocks the page beneath. **Any test that calls `renderPortalList()` must `openPortalWin()` first** (`_test630/631/633` updated). Also in v6.65 — Eric: "when i clock in and come back and it says clock out its always on Hertz job … theres somewhere else the hertz tag is always lit up and selected … on #1 choose the job, take out (optional)": ⏱ `renderJobSelects` keeps the RUNNING shift's job on the wheel however the list is rebuilt (a reorder, a stray blank, a list that lost the job — then the job rides as its own option); 🚚 mileage lights no job when none is set (`mJobDefault`) and a trip with no job asks for one; 📇 job cards open on "▲ Choose the job" with no card drawn until he picks (never `list[0]`); the grinder wheel reads CHOOSE JOB. `tests/_test665.js`. **Parked, specs in the 2026-09-11 reply:** the 💳 bill-radar "Pay this bill" card redesign (`billMaybeSuggest` — a receipt with a date is not a bill he pays; Logan/autopay pay most), the Calm Brass theme (proposal page published), ✓ Always on the 📧 card marking that email seen (`mailSay` → `mailFileWaiting`).
- Before that: **v6.64** (pushed 2026-09-11): THE UPCOMING COSTS HEAL THEMSELVES. Eric, on Josten: "i have a receipt that says 'total due to phil: $15,600' and it says 'sent on their page' but then i go to client view and estimated upcoming costs and i dont see it on there." The board had it (Interior Paint, pend $15,600, eid 2842, sent 9/6) — the page's `upcoming` block was GONE. Proof: `App Data/Backups/josten-842d53-2026-09-08.json` (the page before the 9/8 books run) still carries `upcoming` (Interior Paint 18,720 / Garage Doors 3,948 / Electrical 210.58); the page the books run wrote has no `upcoming` key at all — the DELTA RUN rewrote the page from QuickBooks and dropped the app's keys. Fix, two doors: `estUpcomingStale()` compares the page's `upcoming` with what the board says (as the page prints it); `openEstimates` republishes when they disagree (after `estAutoClear`, the same `scheduleEstSave(true)` road, toast "🩹 Their page had lost the upcoming costs — putting them back"); `openRcptReview` does the same through `estPublish(c, estCut())` ONLY — the board file is never written from the receipts window (`estCut()` is the client cut lifted out of `estSave`, byte-for-byte). A page that already agrees is left alone (asserted: zero writes); hide switches, `boardsOff`, journal and books numbers survive the heal. The **Client page rewrite contract** now sits under Hard rules for the books session. Also in v6.64: with NO approved estimate the owner view draws a dim BUDGET placeholder "○ NOT ON THEIR PAGE YET … ✎ Edit — open the estimates board" (`data-own-empty`), nothing for the homeowner. `tests/_test664.js`; `_test661.js` reloads the frame by blanking `cpFrame.src` first (the same src twice is not a reload). 39/39 suites.
- Before that: **v6.63** (pushed 2026-09-11): A HIDDEN TILE LOOKS HIDDEN ON ERIC'S SCREEN TOO. Eric: "when i have 'options' or 'build list' hidden and i still see it above, do they still see it?" They never did — but his screen showed the tile bright. `photoTile`/`askTile` ids; `ownerDecorate` dims a switched-off tile (`.own-off`) and stamps "○ HIDDEN FROM THEM" on it (`.own-tag`). `_test661.js` (+1).
- Before that: **v6.62** (pushed 2026-09-11): THE WORKING BAND. Eric: "id like something to let me know when its thinking … when i click ask the wizard or uploading photos." `#busyBand` (first child of body, `position: fixed; top: 0; z-index: 200` — above every window and the client preview, `role=status aria-live=polite`), brass shimmer while working, ✓ for 1.1s when done, steady under reduced motion. `busyShow(word)` / `busyDone(word)` / `busyIs(re)`; whoever is working writes the words, last words win. Wired: `setWizLamp('thinking')` → "🧙 THE WIZARD IS THINKING…" + the ASK plate reads THINKING… and is disabled (`wizBtnBusy`), any other lamp state → "🧙 ✓ DONE" / "✓ SENT — the answer rides the hourly check"; `attachEntryPhotos` (grinder + journal ➕ Add photos) → "📷 UPLOADING PHOTO i OF n…" + the grinder plate wears "⏳ i OF n UP" (`grindBusy`, `pointer-events: none`) → "📷 ✓ n PHOTOS UP" or "⚠ k OF n UP — check the connection"; the File Cabinet → "🗄 FILING i OF n…" → "✓ FILED"; materials photos likewise. `tests/_test662.js`.
- Before that: **v6.61** (pushed 2026-09-11): THE OWNER VIEW. Eric: "I want my project portal to basically look exactly like the client page, and it will also control the client page … I have my edit buttons there and my add photos, and I can hide windows from them." Not a second page: the REAL `c/index.html`, opened by `clientPreviewOpen` with `&pv=1&owner=1` on Eric's phone (never crew). In the page `OWNER = PV && owner=1 && window.parent !== window` — a homeowner pasting `&owner=1` into a browser gets the plain page. `ownerDecorate()` puts a bar on every card (`OWN_CARDS`: journal, money, budget, upcoming, phases — ids `journalCard`/`moneyCard`/`phasesCard` added) and a switch row under the four tiles (`OWN_TILES`: photos, ask, boards, mat): `✓ THEY SEE THIS — tap to hide` / `○ HIDDEN FROM THEM — tap to show`, `✎ Edit`, `➕ Add photos`; hidden cards still draw for Eric, dimmed (`.own-off`). Per-board hide inside the Options list (`page.boardsOff[]`, filtered in `boardsLoad` for the homeowner). Every tap is `ownerMsg()` = `postMessage` to the app — **the page gains no write**. App side: one `message` listener that accepts only `ev.source === cpFrame.contentWindow`, never `CREW_NAME`, card names sanitized against `OWN_KEYS`; hide/show → `pageShowSet` writes `pg.show[key] = true/false` (money and phases are opt-IN — their gate is `d.show.money` truthy — everything else opt-OUT via `sh()`; true/false serves both), boards → `pageBoardSet`, edit/photos → close the preview and open the door that owns the card (`openJournal`/`openEstimates`/`openRcptReview`/`openMaterials`/`jumpSetup('setBoards')`/`openReview`/`openMoneyPage('tracker')`), then `cpReload()`. Budget and upcoming now answer to `sh('budget')`/`sh('upcoming')`. `tests/_test661.js` runs a tiny HTTP server so the app page and the client page share ONE store like Dropbox in production (a file:// fetch never becomes a network request, so this is the only way to test the frame for real).
- Before that: **v6.60** (pushed 2026-09-11): JOURNAL PHOTOS, THE TWO CAPS. Eric (Josten's journal): "the number of photos in the preview is limited … the photos I added are not showing up." (1) `jrnPhotoList` stopped at 24 in array order — now picked-and-unsent first, then newest first, cap 48. (2) `c/index.html` drew a week's first 8 photos and silently dropped the rest (Josten's 9/4 week has 11) — 8 show, the rest sit behind ONE button "📷 +N more photos — tap to see them all" (`jrMore`), `display:none` + `loading="lazy"` so they are not fetched until tapped. `tests/_test660.js` (drives the real client page through a routed `/client-portal` fetch). **Eric's next idea, feedback given 2026-09-11, NOT built:** make the project portal LOOK LIKE the client page and CONTROL it — his edit buttons and add-photos in place, and per-card hide switches (e.g. hide Josten's two option boards from the homeowner). Build on his go.
- Before that: **v6.59** (pushed 2026-09-10): two of Eric's asks. 📎 The Wizard's answer screen lists, in small print, the full Dropbox paths of the files its `files/search_v2` name-search turned up (`wizFilesHtml`; tap = copy; folders skipped; the search itself has run since v5.x, he just never saw the list) — "so if something messes up or its taking too long i can just find the file in dropbox myself." 🎡 The one-tap "recent" job plates on PICK THE JOB (v6.42 item 12) are OFF — the wheel alone, full width — behind `window.QN_JOB_CHIPS`; the code stays. `_test642` item 12 updated, `tests/_test659.js`.
- Before that: **v6.58** (pushed 2026-09-10): CREW LABOR FROM PHIL'S WEEKLY RECAP. Eric's time-clock app (he paid Phil to build it) emails "Job recap, week of …" from `pdkneela@gmail.com` every Thursday; the email pipe already lands it in /Inbox. `mailSort` recognises it by SENDER + SUBJECT (`laborRecapIs`, before the judge — never AI), `laborRecapParse` reads hours per job / work type / person + Phil's homeowner paragraph + the "still missing" list (trap: the pipe glues a job header onto the previous crew line — the header is found by its `· N h` tail), kept in `prefs.laborWeeks[week]`, filed as an email entry under Shop / Admin with `labor: week`. Pricing = `LABOR_RATES` (Eric's printed Labor Hour Rate Schedule, 15 rows × 3 tiers, NO markup — "these numbers are billable rates") × `LABOR_CAT_MAP` (recap work type → schedule row, a fixed readable table; unknown → Misc Labor and the page SAYS so) × tier per person (`LABOR_TIER_DEFAULT`: Canaan/Milo 1, Nathan 2, Phil/Aaron/Eric 3; `prefs.crewTier` overrides; unknown name flagged). The recap has hours by work type and by person but not the cross, so the tier is BLENDED from the week's crew mix on the job (`laborPrice`). The 👷 CREW LABOR section in the job's receipts window (`laborBlockHtml`) shows each week priced line by line with the crew (office only), `✓ SEND WEEK → THEIR PAGE` (`laborSend`: a `kind:'labor'` pend under a synthetic `Labor` category, `inc:true`, `base` = the page's `invoiced` at send) → client page `upcoming` line "Labor — week of Aug 30 (62 h)"; `estAutoClear` clears a labor week when `invoiced` has grown by it (its own pot); `↩ Take back` reopens it. Recap job names bridge to app jobs by exact name or the ONE job that starts with it, else a one-time pick (`prefs.recapJobs`). The journal window offers the crew's paragraph (`renderJrnRecap`, ⤵ Build on it). **Parked:** Phil's app writing a weekly JSON (`Time Clock/<week>.json`, per-person per-day `days[]`, names from `crew.json`) instead of the email — spec given to Eric 2026-09-10; wages (crew pay rates) to be added later. `tests/_test658.js`.
- Before that: **v6.57** (pushed 2026-09-09): NAME A NUMBER, AGAIN. Eric: "wheres the option for me to add a name to the phone numbers from texts?" It existed since v5.26 (✎ name in the 📬 texts digest; the name box in the 💬 thread window since v6.14) but v6.24's `tagLooksJunky` stopped bare numbers becoming tags, and every door was keyed on `(e.tags||[])[0]` — so a text from an unnamed number filed under "?", wore no ✎, had no 💬 thread link. `textSender(e)` (who → tag → the `Text from X:` prefix, through `senderName`) is now the ONE way to ask who sent a text: the digest, `personOf`, `threadWith`, the log's thread link (which now reads `💬 thread ↩ · ✎ name` for a raw number), the incoming-text dedupe `textFP`, and `aliasSender`'s retro-fix (tagless texts get the name as their tag). Email sites still key on the tag on purpose — emails always carry one. `tests/_test657.js`. **v6.56** (2026-09-09, built in a claude.ai/code web session, handed over as a `git format-patch` — the right handoff; ask any other session for "a git patch against current main"): the running-log filter plates are a 3-across equal-width grid (`.rl-filters`, `minmax(0,1fr)` not `1fr`; steam plate border 7px so QUESTIONS fits), Receipts/Search equal width in `.rl-head`; `tests/_test656.js`. **Windows gotcha, per-machine:** the repo's blobs are CRLF; `git am` with `core.autocrlf=true` rewrote line endings and broke `_test643`'s byte-identical checks — this PC's clone now has `core.autocrlf=false` (nothing committed), which makes checkouts and patches land exactly as stored.
- Before that: **v6.47** (pushed 2026-09-07): THE WATCHER ACTUALLY RUNS. Confirmed end to end at 13:22 AKDT: `{"ok":true,"seen":10,"judged":8,"important":0}` then a second run judging the rest — 9 texts `skip:text`, 1 email `important / why=deadline / by=cloud / push=sent`. **v6.47 the fix:** `Dropbox-API-Arg` is an HTTP HEADER and can only carry Latin-1; iOS writes Shortcuts filenames as `TEXT -Aug 27, 2026 at 4_50<U+202F>PM.txt` with a NARROW NO-BREAK SPACE before AM/PM, so the very first download threw `Cannot convert argument to a ByteString ... value of 8239` and the run died before writing anything. index.html has had `const hsafe = o => JSON.stringify(o).replace(/[\u007f-\uffff]/g, ...)` since the beginning — it is now lifted verbatim by `gen-mailwatch.js` alongside MAIL_SYS and used at BOTH `Dropbox-API-Arg` sites. **v6.46 (same day, the step that found it):** the default export passes a real logger (`console.log`) and prints the result; `run()` names a missing `DBX_REFRESH_TOKEN`/`DBX_APP_KEY` up front, catches a refused token, and logs a failed /Inbox listing. Only counts and fixed words can reach the log — `{ok, why, seen, judged, important, pushed}` — asserted by a test. **My v6.46 env diagnosis was WRONG and the log corrected it:** the Netlify env page lists ONLY `ANTHROPIC_API_KEY`, but the guard never tripped, so `DBX_REFRESH_TOKEN`/`DBX_APP_KEY`/VAPID **are** set — that page does not show everything. Do not conclude an env var is missing from that list alone. **Diagnosing this function without a Netlify login:** GET `/.netlify/functions/mail-watch` → 403 = deployed AND scheduled (404 = absent, 405/200 = deployed but unscheduled). With a login: Logs & metrics → Functions → mail-watch has a **Run now** button and a live log. `tests/_test643.js` — 39 green (was 34): `hsafe` byte-diff, every `Dropbox-API-Arg` routed through it, an Inbox of U+202F filenames read with every header Latin-1 clean (mkHarness now records `state.headers` — a stubbed fetch cannot throw for us), plus the missing-env message and log safety. **Verified by reintroducing the bug: 2 of the 3 fail without hsafe.** Every pre-existing case now passes `ENV` (Anthropic + both Dropbox vars); the missing-env case uses `NO_DBX`. 23/23 suites.
- Before that: **v6.45** (pushed 2026-09-07): SORT THE PILE. Eric had 97 waiting in Review and asked to get it to zero. The pending.json contract (app owns removal) means it clears ON HIS TAP in the app: a `🧹` plate (`REV_BULK` table → `renderRevBulk()`) at the top of Review when 6+ are waiting and not CREW; one row per kind with 2+ items — `qb` (verb `false` = ✕ Not job cost), `nag` (`todo` ids starting `jobsilence:` → ✕ No), `text` (`pile`), `crew` (`log`) — each button is that kind's own card button pressed N times, nothing more. Two-tap ⚠ SURE? arm (`_revBulkArm`, 4s self-disarm). `_revBulk` flag makes `reviewAct` skip its per-item renders/save and the sweep swaps `window.toast` for a no-op (`toast` is a function declaration, so the rebinding takes) — one render, one save, one summary toast at the end. Emails, clientask, question, match and plain todos are NEVER swept. **Durable QB skip:** any ✕ on a `qb` card (single or bulk) writes `prefs.qbSkipped[qid] = day` and `qbVerifySweep`'s `taken` set includes it — before, the skip lived only in the phone's last-300 `pendDone` localStorage ring, so the 301st skip or a reinstall re-carded every line from qb-costs.json. Gotcha met: `tagLooksJunky` (v6.24) drops bare phone numbers as tags, so a piled text carries its sender only in the words ("Text from +1208…"). **The real pile on 2026-09-07:** qb 61 (Jul 21–Sep 5, 51K, all no-job, acct 1111 OPEX - R5609), jobsilence 18, hot texts 9 (Aug 28–Sep 4), crew 8 (Phil, Aug 8 — incl. "haven't been paid for Carrick's"), mail 1 (Phil's crew-time email). **Local Dropbox is readable without the MCP:** `C:/Users/miloc/Dropbox/Clore DayLog/App Data/` mirrors the live files (pending.json, mail-rules.json, entries.json…) — read, never write there. `mail-rules.json` landed 12:57 that day, so the v6.44 handshake worked; `mail-judged.json` had not appeared yet (key or first run still pending). `tests/_test645.js` — 24 green; 23/23 suites.
- Before that: **v6.44** (pushed 2026-09-07): THE HANDSHAKE. v6.43 shipped a watcher that FAILS CLOSED with no `App Data/mail-rules.json`, and the only writer of that file was `renderMailSenders()` — i.e. Eric happening to open ⚙ → 📧 Email senders. The cloud half could have idled forever and never said why. `checkInboxTexts()` now calls `publishMailRules()` right after `readMailJudged()`, so every app open hands the cloud his lists; `_mailRulesLast` still means one upload a session and only on a real change, and the `dbx.refreshToken`/`CREW_NAME` guards are untouched. `tests/_test644.js` — 21 green, and the handshake is asserted BOTH ways: every `rules.X` the watcher reaches for is regexed out of `fnsrc/mail-watch.mjs` and must be present in what the phone actually wrote, so a new field on either side fails the suite. Also asserts the rules file carries lists and nothing else (no entries/bodies/wages), and that a CREW phone never publishes his lists (real reload with `daylog-crew-name` set, not a stubbed flag). 22/22 suites. **Verifying a scheduled function is deployed, with no Netlify login:** GET `/.netlify/functions/<name>` — **403** = deployed AND on the timer (Netlify refuses HTTP invocation of scheduled functions), 404 = not deployed, 405/200 = deployed but NOT scheduled. `mail-watch` reads 403.
- Before that: **v6.43** (pushed 2026-09-07): THE MAIL WATCHER. `fnsrc/mail-watch.mjs` (bundled to `netlify/functions/`; `build-functions.mjs` ENTRIES now includes it) on `schedule = "*/15 * * * *"` in netlify.toml. **Needs `ANTHROPIC_API_KEY` in the Netlify env — Eric pastes it; until then the function idles by design and the phone sorts alone.** FAILS CLOSED on: no key, no `App Data/mail-rules.json`, or `rules.sort === false`. Reads /Inbox/*.txt + mail-rules.json (phone-owned) + its own `App Data/mail-judged.json` + push-subs.json; **writes mail-judged.json ONLY** — never pending.json, never entries.json, never moves an Inbox file. 🚫 NEVER and 🔒 PERSONAL (names AND addresses) are matched off the FROM line BEFORE the body is parsed, so those words never reach the classifier from the cloud either. Push: `pushText(n, why)` takes an integer and an enum — no sender/subject/gist can reach it — plus `pushSafe()` refusing $ / @ / 3+ digit runs; quiet hours 8pm-7am Alaska write `push: "held"` and ride the first run after. Cap 8 files/run (10s budget), verdicts pruned after 7 days. **`MAIL_SYS`, `mailClean`, `mailVerdictParse`, `mailParse`, `mailListed` are LIFTED VERBATIM from index.html** by `scratchpad/gen-mailwatch.js` and diffed byte-for-byte by `_test643` — regenerate, never hand-edit the copies in fnsrc. Phone side: `readMailJudged()` at sweep start; a cloud verdict is used as-is (`by: cloud` → "⚙ judged while your phone was closed"), costs no aiCall and does NOT consume `MAIL_JUDGE_CAP`; a missing/corrupt file just means the phone judges for itself; Eric's lists always beat the cloud. `tests/_test643.js` — 34 green; 21/21 suites. NOTE: `netlify/functions/notify.mjs` and `plan-reminder.mjs` show pre-existing bundle DRIFT (newer web-push in the toolchain); left untouched on purpose — do not rebundle them without a reason.
- Before that: **v6.42** (pushed 2026-09-07): THE PUNCH LIST, items 1-13, from a 5-reviewer UX pass Eric picked off a plain-English page. (1) the steam skin laid out 428px wide on a 390px phone — the `.qn-div` running-log header never wrapped; it does now, and _test642 asserts `scrollWidth === clientWidth` in BOTH skins. (2) same-minute clock in/out read 24.00 hrs (`mins <= 0` hit the overnight guard); now `< 0` with a 1-minute floor, both in `coCalc` and `hCalc`. (3) tap targets: `.est-board .pick-chip` 24→34, `.est-board .btn-ghost` 28→36, `.pf-small` 30→40, `.pf-vis` 32→40, `.jc-x` 30→44, the log filter chips 28→38; and the grinder tag chips WRAP (`white-space: normal; overflow-wrap: anywhere`) instead of clipping to "Home Depo". (4) `fcSuggestFolder` falls back to Receipts for any `EST_DEFAULT_CATS` name — CAT_FOLDER only knew the 22 old File Cabinet names, so all 55 estimate categories filed to Inbox. (5) `updateStepFlow`: `next = hasWords ? 5 : (jobPicked ? 2 : 1)` + a `.blank` state on ① reading "○ BLANK — files as unfiled". **This REVERSES the letter of v6.30** (which sent him back to ① once he had written) while keeping v6.30's stated intent, quoted in its own comment; job-first still holds when nothing is said. `_test630` updated with the reasoning. (6) `c/index.html` `#matItemFS` z-index 70→160: the homeowner's item window opened UNDER the full-screen Build List (z 150), so their tap on "needs your decision" did nothing. (7) the more-button counts what is left. (8) `.bc-job` 6.6%→12.2% tall, blank option "Choose job" (the ⏱ glyph was eating the word), and `toggleClock` with no job adds `.bc-job.need` + swaps the option to "▲ PICK JOB" for 3.2s. (9) `.save-bar` sticky footers on clock-out / hours / File Cabinet — **a sticky footer only pins when it is a DIRECT CHILD of the scrolling panel**, and each holds ONE action (they wrapped to 162px and stopped pinning when they held three). Scrap + "stay clocked in" moved up into the scroll. (10) `toastUndo` on a plain grind (restores entry, text and job); routed/unlocked notes get none — they already went to someone. The crew-ask push was sending 140 chars of the note to a lock screen; generic now, same rule the hot texts got in v6.41. (11) steps ③/④ fold to one line with a `.g-step-sum` naming what is in them; `gFold(n)`, `window._gOpen`, and `display: none !important` because `#qnTagChips { display: grid }` is an ID and outranks three classes. (12) `renderQnJobChips`/`qnJobTap`: up to three one-tap job buttons sharing the wheel's row via `display: contents`, clocked-in job first; `.has-jobchips` shrinks the wheel to the overflow control. The writing box ends up HIGHER than before (464 vs 475). (13) `body.parked-630` hides the To-Do and ➕ Job bar buttons and `#setPipes`, bar drops to 6 columns at 11.5px, and `.capture .cap-btn.active` gets a ▸ word (the phone bar strips the border the desktop rule used). **Two bugs I introduced and caught:** the fold hid `#mailAskCard` (it sat inside step ④ — the same shape as v6.38's bug), so the card now lives OUTSIDE the g-steps entirely; and the sticky-footer child rule above. `tests/_test642.js` — 29 green; 20/20 suites.
- Before that: **v6.41** (pushed 2026-09-07): THE MAIL SORTER. Every email walks one door, `mailSort(m,f,id)` in the Inbox sweep: 🚫 Never list → ledger row + Robots, never judged; PERSONAL (a personalFolks name OR an address on `prefs.mailPersonal`) → a LOCKED entry, never judged, never carded; everyone else (kept AND brand-new senders) → `mailJudge` = one `aiCall(kind "mailsort")` → always AI_FAST → `MAIL_SYS` (the email is DATA in <email> tags; `mailClean` strips tags/links/quoted replies first) → `mailVerdictParse` locks the answer to the enums, EVERY failure path → maybe/"none" (⚡ UNSORTED, never hidden, never a card); then `mailRuleBucket` applies 📣 `mailLoud` (force important) / 🔕 `mailHush` (cap at maybe). important → a `kind:"mail"` Review card (known sender logged now with entryId; a NEW sender logs ONLY on his tap); maybe → entry (known) or a `mailAsk` row carrying the body (new, and ✓ Always then FILES it); ignore → entry+ledger (known) or ledger only (new), archived to `/Texts/Mail Ignored`. `MAIL_JUDGE_CAP = 10` per sweep returns "later" and leaves the file untouched. The 📧 card (`renderMailAsk`) is ★/?/○ folds with ✓ Seen, 📣 Loud, ↩ Rescue (which OFFERS ✓ Always, never adds). `publishMailRules()` writes phone-owned `App Data/mail-rules.json` so the v6.42 watcher can obey the same rules. **Two review-confirmed bugs fixed before push:** the dedupe now compares the WORDS (`e.details === line`), so a thread reply seconds after the first is its own entry — it was collapsing them and losing the second forever; and ↩ Rescue on a KEPT sender now flips the existing entry to maybe (autoLogMail hands back the already-logged "ignore" entry, so the rescue used to only delete the ledger row). **Also in v6.41, from the app-wide sweep:** personal senders' mail is LOCKED (v6.12 never did) + a one-time sweep of old ones (`mailLockPersonalOnce`); `jrnStamp`/`journalDraft` refuse emails; `csvString` writes an email's subject only (never the body) and master-log.csv excludes `personal`; the hot-TEXT push no longer carries the sender name or 140 chars of body; emails are dated by arrival (`ts`); sender names `push` (not `unshift`) onto `prefs.tags`; `checkInboxTexts` awaits `window._pendReady` (`checkPending`'s promise — a freshly pushed card could be wiped, hot texts too); the Wizard pool excludes `personal` and carries mail fields; log gets a 📧 chip + address search + "— no job yet"; `#setMail` hidden on crew phones. `tests/_test641.js` — 33 green; 19/19 suites. **NEXT (spec ready, needs Eric's go):** v6.42 the cloud watcher — `fnsrc/mail-watch.mjs`, schedule */15, `ANTHROPIC_API_KEY` in Netlify env, reads mail-rules.json (fail closed if missing), writes `App Data/mail-judged.json` (cloud-owned, phone reads it and skips its own call), ONE generic push per run for new IMPORTANT only, quiet hours 8pm-7am Alaska. Byte-identical `MAIL_SYS` + `mailVerdictParse` in both files, guarded by a diff test.
- **A plain-English punch list for Eric is published** at https://claude.ai/code/artifact/03898d1e-0ee9-4f83-a48c-54f19cd82ce1 — 17 numbered items (1-7 read as bugs and are quick; 8-13 usability; 14-17 need his go) + 4 decisions. Built from a 5-reviewer UX pass (97 notes) and a 5-reader email touchpoint sweep (89 touchpoints); both raw outputs and a Word copy are in Dropbox `/Clore DayLog/Notes/`. Eric could not read the first, engineer-worded version — keep his docs in his words.
- Before that: **v6.40** (pushed 2026-09-06 night): the log's Type wheel has 📧 Emails (`e.mail` entries) and the search box reads sender/subject/body; Setup → 📧 Email senders (`setMail`, `renderMailSenders`/`mailListAdd`/`mailListDel`) edits `prefs.mailOk` / `prefs.mailNo` — one list or the other, never both. Gotcha for future edits: a node replace-script fed through the Bash tool STRIPPED backslashes inside regex literals (`w` → `w`); write regexes with the Edit tool. **In flight / next:** judgment-based email triage (sub invoices, client questions → Important / Maybe / Ignored piles, push for Important, spot-check of ignored) — three designs were drafted and judged 2026-09-06; spec to Eric, build on his go. Also an app-wide sweep of every place an email note sits/sorts/searches/tags/leaks, with recommendations.
- Before that: **v6.39** (pushed 2026-09-06 night): the bookkeeper plate wears the grinder's pipe frame (`steam/btn-bookkeeper-framed.webp`, composited from the two originals); the painted-on "Rininger" dropdown is gone from the clock panel (`steam/clockpanel-big-v2.jpg`, onerror falls back to the old file); `EST_ALIASES` (Trash ← Landfill + Garbage/Janitorial, Water Softener ← Softner) in `estScore` AND byte-identical in `c/index.html` budgetHtml — keep both tables the same; pend `al: 1` stamp = base measured against a page WITH books numbers (`estPageHasBooks()`), unstamped pends under aliased cats are lifted once at the next board open then stamped (three adversarial review rounds; the accepted conservative case: a pre-v6.39 Trash bill Logan had already booked stays on Upcoming until Eric retires it by hand). Known pre-existing hole for EVERY category, not fixed: a bill sent against a brand-new client page (`phases: []`) carries base 0 and can be swept by the first books run — only the aliased categories are protected by the stamp/lift. **Parked:** a Setup list to view/undo the email sender ✓ Always / 🚫 Never choices (`prefs.mailOk` / `prefs.mailNo` have no UI today); a per-item markup %, a refund/credit line, and a category-level note on the estimates board (specs offered 2026-09-06, no go yet).
- Before that: **v6.38** (pushed 2026-09-06 evening; v6.35→v6.38 shipped together after a 4-lens adversarial review of the whole diff). v6.37: 📊 Project Tracker button beside 🧾 Receipts to approve in both job folds (it opens the read-only QuickBooks money page, NOT the client page); client-page upcoming-costs note ends at "once it's invoiced." v6.38: the 📧 NEW EMAIL SENDERS card was inside the crew-only `#crewRoute` block (display:none on Eric's phone) — moved out, so new senders can finally be approved; `renderPortalList` never wipes a cached client list on a bad/empty/half-written `index.json` read (this also cleared the two `_test635` failures); `isOverheadEntry` returns false for HOURS (labor is non-billable only via the clock-out toggle); a receipt SENT before v6.35 under Tools/Fuel/Office stays SENT with its lights. **Email pipe confirmed working end to end** (Yahoo → clore.biz Gmail → Zap "Gmail to Dropbox Text File" → `/Clore DayLog/Inbox/Email -<subject>.txt` with FROM:/SUBJECT: header lines → app). NOTE: the Dropbox MCP `fetch` strips those header lines when reading, so files look body-only when they are not; verify raw bytes via a download link. **Parked for v6.39 (Eric's list + review carry-over):** bookkeeper plate with the grinder's pipe frame (composite ready), the painted-on "Rininger" dropdown in `steam/clockpanel-big.jpg` (patched image ready), and the 'Trash / Construction Debris' ↔ QB Landfill/Garbage-Janitorial alias — must go in BOTH matchers (app `estScore` and the client page's copy in `c/index.html` budgetHtml) with a one-time pend re-baseline, or old lifetime dollars falsely auto-clear a fresh bill.
- Before that: **v6.36** (2026-09-06) — seven categories from Logan's QuickBooks Products & Services joined `EST_DEFAULT_CATS` (48 → 55): Surveying, Water Softener, Bathrooms, Contingency, Trash / Construction Debris (QB's Landfill + Garbage/Janitorial in ONE bucket, Eric's call), Snow Plowing, Temporary Electric. They ride into `RCPT_CATS`/`FC_CATS`; an existing board gets them appended at the BOTTOM. `tests/_test636.js` (15 green). **v6.35 and v6.36 are committed but NOT pushed:** `tests/_test635.js` has 2 failures nobody has cleared — the ③ paid-file light, and the portal render reading `_portalIdx.clients` when `index.json` parses to null. Push on Eric's go. The phase-by-phase comparison PDF (his 55 vs Logan's 12 phases / 71 line items, plus 12 legacy un-numbered leftovers on Logan's list) was built from the All Dates Sales by Product/Service export of 2026-08-25.
- Before that: **v6.35** (2026-09-05, a long day of builds in Claude Code — see the commits). The receipt flow end to end: 🧾 tag → `🔮 → Upcoming` one-tap in the log, or the tracker's `🧾 Receipts to approve` window (tick + approve) → client page **ESTIMATED UPCOMING COSTS** (marked up, vendor-free, publishes whatever the lamps do) → auto-clear when QuickBooks catches up (trail in `estimates-<code>.json` `cleared[]`) → three lights in the receipts window. **v6.33 fixed the real bug:** receipt paths compared the portal name to the app job literally and never matched — always bridge with `portalAppJob(c)`. Overhead categories (`OVERHEAD_CATS`: Tools, Fuel, Office/Admin) are never offered to a client. 🔥 needs-you strip and to-dos are PARKED (body class `parked-630`). Function sources are in `fnsrc/`; bundle with `../boiler-room-tools/build-functions.mjs`. Tests run locally: `node ../boiler-room-tools/run-tests.mjs`.
- GitHub push → Netlify deploy is proven and is the only route. The old "live site may lag" note is retired.
- **Next up (spec agreed, parked):** homeowner push notifications — opt-in on client page, per-client subs walled off from `App Data/push-subs.json`, ONLY two triggers (journal release, budget change), generic message text, one ping per burst, delivery only 9am–5pm Alaska (off-hours actions queue to 9am). Full spec in the claude.ai Project doc `claude/next-up-cards.md`.
- Deeper history: the Project docs (`claude/build-log-*.md`, `claude/how-to-run-boiler-room.md`, `claude/deploy-pipeline.md`) in the "Dashboard App" project on claude.ai.

## Working style

Small, surgical edits to the one big file; comment new blocks with the version + Eric's own words for the why (the file reads as a decision log). Steampunk skin: brass/dark, plates and lamps — match it. When Eric reports a bug, reproduce it in a Playwright test first, fix, keep the test green.
