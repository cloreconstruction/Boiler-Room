# Boiler Room — CLAUDE.md (read this first, every session)

Boiler Room is the construction day-log PWA for **Eric Clore, Clore Construction, Kenai Peninsula, Alaska**. Eric dictates messages and reads on a phone — keep replies **short, plain, no jargon**. He is the owner and only decision-maker; Logan is the bookkeeper, Shevaun handles C&C properties paperwork, Phil/Kevin/Jason/Elodie/Milo are crew.

## What this repo is

- **The repo root IS the live site.** Netlify auto-deploys on every push to `main` at https://cloreconstdailylog.netlify.app
- `index.html` — the entire app: ONE self-contained file (~15,000 lines, HTML+CSS+JS, no build step, no framework). This is deliberate. Keep it one file.
- `c/index.html` — the homeowner (client) page, served at `/c/?c=<code>`; data via `/.netlify/functions/client-portal`
- `netlify/functions/` — `client-portal.mjs`, `notify.mjs` (web-push, VAPID keys in Netlify env), `plan-reminder.mjs` (scheduled 5pm). Functions are pre-BUNDLED single files (esbuild) — bundle locally before committing changes.
- `sw.js` — push service worker. `version.txt` — must match the app version every release.

## Release rules (never skip)

1. Bump `const APP_VER = 'vX.YY'` AND the `<footer>vX.YY` line — both, every build. The app polls `version.txt` and offers "tap to load" when it's newer.
2. Update `version.txt` in the same commit.
3. **Commit author must be `Eric Clore <cloreconstruction@yahoo.com>`** — Netlify's contributor check rejects other authors. Committer may be Claude.
4. **NEVER drag-and-drop files onto Netlify.** A single-file drop replaces the ENTIRE published site (it killed all nine client pages on 2026-08-25). GitHub push is the only deploy route.
5. Test before shipping: Playwright suites named `_testNNN.js` load the file:// page on a 390×844 mobile viewport and assert behavior (see `tests/` if present; earlier suites are in build logs). Run the newest suite plus the last few before any release.

## Hard rules (Eric's, standing, non-negotiable)

- **Personal entries (`personal:true` / a Personal tag) NEVER leave the phone** — not in sync payloads, summaries, crew publishes, or anything client-facing. The lock wins over everything.
- **Markup and vendor names NEVER appear client-side.** Markup folds into client numbers invisibly (`estMk()`), never named. Breakdowns must sum exactly.
- **Per-person wage isolation**: a crew member's files carry ONLY their own rates.
- **No guess-based matching.** Eric rejected AI scoring (v5.26). Prefer tap-to-learn exact-name allow-lists (`prefs.vendCat`, `prefs.jobCats`); pre-light, never auto-commit — his tap decides.
- **Accessibility: nothing depends on colour alone.** Every state = brightness + glyph + spelled word (e.g. "⚠ SURE?", "✓ ON PAGE", "▸ NEXT").
- **Nothing opens in external tabs on the phone** (PWA trap): no target=_blank flows Eric must return from.
- **Bank/loan account numbers: last-four only** in anything summarized or displayed.
- Never put dollar amounts or client names in push-notification text (lock screens).
- Client data files in Dropbox use **create-first replacement**: CREATE `.new` → verify → MOVE old to Backups/Archive → MOVE `.new` live. Never delete in Dropbox; move instead.
- `pending.json` writer contract: writers APPEND unique ids only; the app owns removal.
- **`Client Portal/paid-<code>.json` contract (v6.35, light ③):** written by the books session from the QuickBooks run, read by the app: `{ "eids": [entry ids], "byId": { "<id>": { "inv": "<QB invoice #>", "paidOn": "YYYY-MM-DD" } } }`. Append only — an id listed here means that receipt was invoiced to the homeowner and paid, and the app drops it from the job's receipts window. Until the file exists the app shows "③ ○ NOT YET — comes from the books". Receipt ids are `entries.json` ids; the amount/vendor to match against is in each client's `estimates-<code>.json` under `cats[].pend[]` (open) and `cleared[]` (paired: `why` = `qb` automatic, `qb-eric` Eric's tap, `eric` = taken back, never paid).
- Never shrink lifetime invoiced figures from a WINDOWED QuickBooks export; a true All-Dates ledger IS lifetime truth and may reduce figures.
- **Park ideas, build on Eric's go.** When Eric muses, give feedback and park it (see `claude/next-up-cards.md` in the claude.ai Project); build only when he says go.

## Where the data lives (NOT in this repo)

All app data syncs to Eric's Dropbox under `/Clore DayLog/` (entries.json, master-log.csv, qb-refresh.json + heartbeat/state, profit-ticker.html, qb-costs.json, Client Portal/*.json + estimates, Crew/<name>/). QuickBooks exports land in `/Clore Project Tracker/QB Exports/`. Sessions without Dropbox access work on the app code only — that's fine; the chat (Cowork) sessions handle data runs (a weekly Tuesday 13:00Z chain refreshes the books by hand).

## Current state (as of 2026-09-07)

- Latest LIVE: **v6.61** (pushed 2026-09-11): THE OWNER VIEW. Eric: "I want my project portal to basically look exactly like the client page, and it will also control the client page … I have my edit buttons there and my add photos, and I can hide windows from them." Not a second page: the REAL `c/index.html`, opened by `clientPreviewOpen` with `&pv=1&owner=1` on Eric's phone (never crew). In the page `OWNER = PV && owner=1 && window.parent !== window` — a homeowner pasting `&owner=1` into a browser gets the plain page. `ownerDecorate()` puts a bar on every card (`OWN_CARDS`: journal, money, budget, upcoming, phases — ids `journalCard`/`moneyCard`/`phasesCard` added) and a switch row under the four tiles (`OWN_TILES`: photos, ask, boards, mat): `✓ THEY SEE THIS — tap to hide` / `○ HIDDEN FROM THEM — tap to show`, `✎ Edit`, `➕ Add photos`; hidden cards still draw for Eric, dimmed (`.own-off`). Per-board hide inside the Options list (`page.boardsOff[]`, filtered in `boardsLoad` for the homeowner). Every tap is `ownerMsg()` = `postMessage` to the app — **the page gains no write**. App side: one `message` listener that accepts only `ev.source === cpFrame.contentWindow`, never `CREW_NAME`, card names sanitized against `OWN_KEYS`; hide/show → `pageShowSet` writes `pg.show[key] = true/false` (money and phases are opt-IN — their gate is `d.show.money` truthy — everything else opt-OUT via `sh()`; true/false serves both), boards → `pageBoardSet`, edit/photos → close the preview and open the door that owns the card (`openJournal`/`openEstimates`/`openRcptReview`/`openMaterials`/`jumpSetup('setBoards')`/`openReview`/`openMoneyPage('tracker')`), then `cpReload()`. Budget and upcoming now answer to `sh('budget')`/`sh('upcoming')`. `tests/_test661.js` runs a tiny HTTP server so the app page and the client page share ONE store like Dropbox in production (a file:// fetch never becomes a network request, so this is the only way to test the frame for real).
- Before that: **v6.60** (pushed 2026-09-11): JOURNAL PHOTOS, THE TWO CAPS. Eric (Josten's journal): "the number of photos in the preview is limited … the photos I added are not showing up." (1) `jrnPhotoList` stopped at 24 in array order — now picked-and-unsent first, then newest first, cap 48. (2) `c/index.html` drew a week's first 8 photos and silently dropped the rest (Josten's 9/4 week has 11) — 8 show, the rest sit behind ONE button "📷 +N more photos — tap to see them all" (`jrMore`), `display:none` + `loading="lazy"` so they are not fetched until tapped. `tests/_test660.js` (drives the real client page through a routed `/client-portal` fetch). **Eric's next idea, feedback given 2026-09-11, NOT built:** make the project portal LOOK LIKE the client page and CONTROL it — his edit buttons and add-photos in place, and per-card hide switches (e.g. hide Josten's two option boards from the homeowner). Build on his go.
- Before that: **v6.59** (pushed 2026-09-10): two of Eric's asks. 📎 The Wizard's answer screen lists, in small print, the full Dropbox paths of the files its `files/search_v2` name-search turned up (`wizFilesHtml`; tap = copy; folders skipped; the search itself has run since v5.x, he just never saw the list) — "so if something messes up or its taking too long i can just find the file in dropbox myself." 🎡 The one-tap "recent" job plates on PICK THE JOB (v6.42 item 12) are OFF — the wheel alone, full width — behind `window.QN_JOB_CHIPS`; the code stays. `_test642` item 12 updated, `tests/_test659.js`.
- Before that: **v6.58** (pushed 2026-09-10): CREW LABOR FROM PHIL'S WEEKLY RECAP. Eric's time-clock app (he paid Phil to build it) emails "Job recap, week of …" from `pdkneela@gmail.com` every Thursday; the email pipe already lands it in /Inbox. `mailSort` recognises it by SENDER + SUBJECT (`laborRecapIs`, before the judge — never AI), `laborRecapParse` reads hours per job / work type / person + Phil's homeowner paragraph + the "still missing" list (trap: the pipe glues a job header onto the previous crew line — the header is found by its `· N h` tail), kept in `prefs.laborWeeks[week]`, filed as an email entry under Shop / Admin with `labor: week`. Pricing = `LABOR_RATES` (Eric's printed Labor Hour Rate Schedule, 15 rows × 3 tiers, NO markup — "these numbers are billable rates") × `LABOR_CAT_MAP` (recap work type → schedule row, a fixed readable table; unknown → Misc Labor and the page SAYS so) × tier per person (`LABOR_TIER_DEFAULT`: Canaan/Milo 1, Nathan 2, Phil/Aaron/Eric 3; `prefs.crewTier` overrides; unknown name flagged). The recap has hours by work type and by person but not the cross, so the tier is BLENDED from the week's crew mix on the job (`laborPrice`). The 👷 CREW LABOR section in the job's receipts window (`laborBlockHtml`) shows each week priced line by line with the crew (office only), `✓ SEND WEEK → THEIR PAGE` (`laborSend`: a `kind:'labor'` pend under a synthetic `Labor` category, `inc:true`, `base` = the page's `invoiced` at send) → client page `upcoming` line "Labor — week of Aug 30 (62 h)"; `estAutoClear` clears a labor week when `invoiced` has grown by it (its own pot); `↩ Take back` reopens it. Recap job names bridge to app jobs by exact name or the ONE job that starts with it, else a one-time pick (`prefs.recapJobs`). The journal window offers the crew's paragraph (`renderJrnRecap`, ⤵ Build on it). **Parked:** Phil's app writing a weekly JSON (`Time Clock/<week>.json`, per-person per-day `days[]`, names from `crew.json`) instead of the email — spec given to Eric 2026-09-10; wages (crew pay rates) to be added later. `tests/_test658.js`.
- Before that: **v6.57** (pushed 2026-09-09): NAME A NUMBER, AGAIN. Eric: "wheres the option for me to add a name to the phone numbers from texts?" It existed since v5.26 (✎ name in the 📬 texts digest; the name box in the 💬 thread window since v6.14) but v6.24's `tagLooksJunky` stopped bare numbers becoming tags, and every door was keyed on `(e.tags||[])[0]` — so a text from an unnamed number filed under "?", wore no ✎, had no 💬 thread link. `textSender(e)` (who → tag → the `Text from X:` prefix, through `senderName`) is now the ONE way to ask who sent a text: the digest, `personOf`, `threadWith`, the log's thread link (which now reads `💬 thread ↩ · ✎ name` for a raw number), the incoming-text dedupe `textFP`, and `aliasSender`'s retro-fix (tagless texts get the name as their tag). Email sites still key on the tag on purpose — emails always carry one. `tests/_test657.js`. **v6.56** (2026-09-09, built in a claude.ai/code web session, handed over as a `git format-patch` — the right handoff; ask any other session for "a git patch against current main"): the running-log filter plates are a 3-across equal-width grid (`.rl-filters`, `minmax(0,1fr)` not `1fr`; steam plate border 7px so QUESTIONS fits), Receipts/Search equal width in `.rl-head`; `tests/_test656.js`. **Windows gotcha, per-machine:** the repo's blobs are CRLF; `git am` with `core.autocrlf=true` rewrote line endings and broke `_test643`'s byte-identical checks — this PC's clone now has `core.autocrlf=false` (nothing committed), which makes checkouts and patches land exactly as stored.
- Before that: **v6.47** (pushed 2026-09-07): THE WATCHER ACTUALLY RUNS. Confirmed end to end at 13:22 AKDT: `{"ok":true,"seen":10,"judged":8,"important":0}` then a second run judging the rest — 9 texts `skip:text`, 1 email `important / why=deadline / by=cloud / push=sent`. **v6.47 the fix:** `Dropbox-API-Arg` is an HTTP HEADER and can only carry Latin-1; iOS writes Shortcuts filenames as `TEXT -Aug 27, 2026 at 4_50<U+202F>PM.txt` with a NARROW NO-BREAK SPACE before AM/PM, so the very first download threw `Cannot convert argument to a ByteString ... value of 8239` and the run died before writing anything. index.html has had `const hsafe = o => JSON.stringify(o).replace(/[\u007f-\uffff]/g, ...)` since the beginning — it is now lifted verbatim by `gen-mailwatch.js` alongside MAIL_SYS and used at BOTH `Dropbox-API-Arg` sites. **v6.46 (same day, the step that found it):** the default export passes a real logger (`console.log`) and prints the result; `run()` names a missing `DBX_REFRESH_TOKEN`/`DBX_APP_KEY` up front, catches a refused token, and logs a failed /Inbox listing. Only counts and fixed words can reach the log — `{ok, why, seen, judged, important, pushed}` — asserted by a test. **My v6.46 env diagnosis was WRONG and the log corrected it:** the Netlify env page lists ONLY `ANTHROPIC_API_KEY`, but the guard never tripped, so `DBX_REFRESH_TOKEN`/`DBX_APP_KEY`/VAPID **are** set — that page does not show everything. Do not conclude an env var is missing from that list alone. **Diagnosing this function without a Netlify login:** GET `/.netlify/functions/mail-watch` → 403 = deployed AND scheduled (404 = absent, 405/200 = deployed but unscheduled). With a login: Logs & metrics → Functions → mail-watch has a **Run now** button and a live log. `tests/_test643.js` — 39 green (was 34): `hsafe` byte-diff, every `Dropbox-API-Arg` routed through it, an Inbox of U+202F filenames read with every header Latin-1 clean (mkHarness now records `state.headers` — a stubbed fetch cannot throw for us), plus the missing-env message and log safety. **Verified by reintroducing the bug: 2 of the 3 fail without hsafe.** Every pre-existing case now passes `ENV` (Anthropic + both Dropbox vars); the missing-env case uses `NO_DBX`. 23/23 suites.
- Before that: **v6.45** (pushed 2026-09-07): SORT THE PILE. Eric had 97 waiting in Review and asked to get it to zero. The pending.json contract (app owns removal) means it clears ON HIS TAP in the app: a `🧹` plate (`REV_BULK` table → `renderRevBulk()`) at the top of Review when 6+ are waiting and not CREW; one row per kind with 2+ items — `qb` (verb `false` = ✕ Not job cost), `nag` (`todo` ids starting `jobsilence:` → ✕ No), `text` (`pile`), `crew` (`log`) — each button is that kind's own card button pressed N times, nothing more. Two-tap ⚠ SURE? arm (`_revBulkArm`, 4s self-disarm). `_revBulk` flag makes `reviewAct` skip its per-item renders/save and the sweep swaps `window.toast` for a no-op (`toast` is a function declaration, so the rebinding takes) — one render, one save, one summary toast at the end. Emails, clientask, question, match and plain todos are NEVER swept. **Durable QB skip:** any ✕ on a `qb` card (single or bulk) writes `prefs.qbSkipped[qid] = day` and `qbVerifySweep`'s `taken` set includes it — before, the skip lived only in the phone's last-300 `pendDone` localStorage ring, so the 301st skip or a reinstall re-carded every line from qb-costs.json. Gotcha met: `tagLooksJunky` (v6.24) drops bare phone numbers as tags, so a piled text carries its sender only in the words ("Text from +1208…"). **The real pile on 2026-09-07:** qb 61 (Jul 21–Sep 5, 51K, all no-job, acct 1111 OPEX - R5609), jobsilence 18, hot texts 9 (Aug 28–Sep 4), crew 8 (Phil, Aug 8 — incl. "haven't been paid for Carrick's"), mail 1 (Phil's crew-time email). **Local Dropbox is readable without the MCP:** `C:/Users/miloc/Dropbox/Clore DayLog/App Data/` mirrors the live files (pending.json, mail-rules.json, entries.json…) — read, never write there. `mail-rules.json` landed 12:57 that day, so the v6.44 handshake worked; `mail-judged.json` had not appeared yet (key or first run still pending). `tests/_test645.js` — 24 green; 23/23 suites.
- Before that: **v6.44** (pushed 2026-09-07): THE HANDSHAKE. v6.43 shipped a watcher that FAILS CLOSED with no `App Data/mail-rules.json`, and the only writer of that file was `renderMailSenders()` — i.e. Eric happening to open ⚙ → 📧 Email senders. The cloud half could have idled forever and never said why. `checkInboxTexts()` now calls `publishMailRules()` right after `readMailJudged()`, so every app open hands the cloud his lists; `_mailRulesLast` still means one upload a session and only on a real change, and the `dbx.refreshToken`/`CREW_NAME` guards are untouched. `tests/_test644.js` — 21 green, and the handshake is asserted BOTH ways: every `rules.X` the watcher reaches for is regexed out of `fnsrc/mail-watch.mjs` and must be present in what the phone actually wrote, so a new field on either side fails the suite. Also asserts the rules file carries lists and nothing else (no entries/bodies/wages), and that a CREW phone never publishes his lists (real reload with `daylog-crew-name` set, not a stubbed flag). 22/22 suites. **Verifying a scheduled function is deployed, with no Netlify login:** GET `/.netlify/functions/<name>` — **403** = deployed AND on the timer (Netlify refuses HTTP invocation of scheduled functions), 404 = not deployed, 405/200 = deployed but NOT scheduled. `mail-watch` reads 403.
- Before that: **v6.43** (pushed 2026-09-07): THE MAIL WATCHER. `fnsrc/mail-watch.mjs` (bundled to `netlify/functions/`; `build-functions.mjs` ENTRIES now includes it) on `schedule = "*/15 * * * *"` in netlify.toml. **Needs `ANTHROPIC_API_KEY` in the Netlify env — Eric pastes it; until then the function idles by design and the phone sorts alone.** FAILS CLOSED on: no key, no `App Data/mail-rules.json`, or `rules.sort === false`. Reads /Inbox/*.txt + mail-rules.json (phone-owned) + its own `App Data/mail-judged.json` + push-subs.json; **writes mail-judged.json ONLY** — never pending.json, never entries.json, never moves an Inbox file. 🚫 NEVER and 🔒 PERSONAL (names AND addresses) are matched off the FROM line BEFORE the body is parsed, so those words never reach the classifier from the cloud either. Push: `pushText(n, why)` takes an integer and an enum — no sender/subject/gist can reach it — plus `pushSafe()` refusing $ / @ / 3+ digit runs; quiet hours 8pm-7am Alaska write `push: "held"` and ride the first run after. Cap 8 files/run (10s budget), verdicts pruned after 7 days. **`MAIL_SYS`, `mailClean`, `mailVerdictParse`, `mailParse`, `mailListed` are LIFTED VERBATIM from index.html** by `scratchpad/gen-mailwatch.js` and diffed byte-for-byte by `_test643` — regenerate, never hand-edit the copies in fnsrc. Phone side: `readMailJudged()` at sweep start; a cloud verdict is used as-is (`by: cloud` → "⚙ judged while your phone was closed"), costs no aiCall and does NOT consume `MAIL_JUDGE_CAP`; a missing/corrupt file just means the phone judges for itself; Eric's lists always beat the cloud. `tests/_test643.js` — 34 green; 21/21 suites. NOTE: `netlify/functions/notify.mjs` and `plan-reminder.mjs` show pre-existing bundle DRIFT (newer web-push in the toolchain); left untouched on purpose — do not rebundle them without a reason.
- Before that: **v6.42** (pushed 2026-09-07): THE PUNCH LIST, items 1-13, from a 5-reviewer UX pass Eric picked off a plain-English page. (1) the steam skin laid out 428px wide on a 390px phone — the `.qn-div` running-log header never wrapped; it does now, and _test642 asserts `scrollWidth === clientWidth` in BOTH skins. (2) same-minute clock in/out read 24.00 hrs (`mins <= 0` hit the overnight guard); now `< 0` with a 1-minute floor, both in `coCalc` and `hCalc`. (3) tap targets: `.est-board .pick-chip` 24→34, `.est-board .btn-ghost` 28→36, `.pf-small` 30→40, `.pf-vis` 32→40, `.jc-x` 30→44, the log filter chips 28→38; and the grinder tag chips WRAP (`white-space: normal; overflow-wrap: anywhere`) instead of clipping to "Home Depo". (4) `fcSuggestFolder` falls back to Receipts for any `EST_DEFAULT_CATS` name — CAT_FOLDER only knew the 22 old File Cabinet names, so all 55 estimate categories filed to Inbox. (5) `updateStepFlow`: `next = hasWords ? 5 : (jobPicked ? 2 : 1)` + a `.blank` state on ① reading "○ BLANK — files as unfiled". **This REVERSES the letter of v6.30** (which sent him back to ① once he had written) while keeping v6.30's stated intent, quoted in its own comment; job-first still holds when nothing is said. `_test630` updated with the reasoning. (6) `c/index.html` `#matItemFS` z-index 70→160: the homeowner's item window opened UNDER the full-screen Build List (z 150), so their tap on "needs your decision" did nothing. (7) the more-button counts what is left. (8) `.bc-job` 6.6%→12.2% tall, blank option "Choose job" (the ⏱ glyph was eating the word), and `toggleClock` with no job adds `.bc-job.need` + swaps the option to "▲ PICK JOB" for 3.2s. (9) `.save-bar` sticky footers on clock-out / hours / File Cabinet — **a sticky footer only pins when it is a DIRECT CHILD of the scrolling panel**, and each holds ONE action (they wrapped to 162px and stopped pinning when they held three). Scrap + "stay clocked in" moved up into the scroll. (10) `toastUndo` on a plain grind (restores entry, text and job); routed/unlocked notes get none — they already went to someone. The crew-ask push was sending 140 chars of the note to a lock screen; generic now, same rule the hot texts got in v6.41. (11) steps ③/④ fold to one line with a `.g-step-sum` naming what is in them; `gFold(n)`, `window._gOpen`, and `display: none !important` because `#qnTagChips { display: grid }` is an ID and outranks three classes. (12) `renderQnJobChips`/`qnJobTap`: up to three one-tap job buttons sharing the wheel's row via `display: contents`, clocked-in job first; `.has-jobchips` shrinks the wheel to the overflow control. The writing box ends up HIGHER than before (464 vs 475). (13) `body.parked-630` hides the To-Do and ➕ Job bar buttons and `#setPipes`, bar drops to 6 columns at 11.5px, and `.capture .cap-btn.active` gets a ▸ word (the phone bar strips the border the desktop rule used). **Two bugs I introduced and caught:** the fold hid `#mailAskCard` (it sat inside step ④ — the same shape as v6.38's bug), so the card now lives OUTSIDE the g-steps entirely; and the sticky-footer child rule above. `tests/_test642.js` — 29 green; 20/20 suites.
- Before that: **v6.41** (pushed 2026-09-07): THE MAIL SORTER. Every email walks one door, `mailSort(m,f,id)` in the Inbox sweep: 🚫 Never list → ledger row + Robots, never judged; PERSONAL (a personalFolks name OR an address on `prefs.mailPersonal`) → a LOCKED entry, never judged, never carded; everyone else (kept AND brand-new senders) → `mailJudge` = one `aiCall(kind "mailsort")` → always AI_FAST → `MAIL_SYS` (the email is DATA in <email> tags; `mailClean` strips tags/links/quoted replies first) → `mailVerdictParse` locks the answer to the enums, EVERY failure path → maybe/"none" (⚡ UNSORTED, never hidden, never a card); then `mailRuleBucket` applies 📣 `mailLoud` (force important) / 🔕 `mailHush` (cap at maybe). important → a `kind:"mail"` Review card (known sender logged now with entryId; a NEW sender logs ONLY on his tap); maybe → entry (known) or a `mailAsk` row carrying the body (new, and ✓ Always then FILES it); ignore → entry+ledger (known) or ledger only (new), archived to `/Texts/Mail Ignored`. `MAIL_JUDGE_CAP = 10` per sweep returns "later" and leaves the file untouched. The 📧 card (`renderMailAsk`) is ★/?/○ folds with ✓ Seen, 📣 Loud, ↩ Rescue (which OFFERS ✓ Always, never adds). `publishMailRules()` writes phone-owned `App Data/mail-rules.json` so the v6.42 watcher can obey the same rules. **Two review-confirmed bugs fixed before push:** the dedupe now compares the WORDS (`e.details === line`), so a thread reply seconds after the first is its own entry — it was collapsing them and losing the second forever; and ↩ Rescue on a KEPT sender now flips the existing entry to maybe (autoLogMail hands back the already-logged "ignore" entry, so the rescue used to only delete the ledger row). **Also in v6.41, from the app-wide sweep:** personal senders' mail is LOCKED (v6.12 never did) + a one-time sweep of old ones (`mailLockPersonalOnce`); `jrnStamp`/`journalDraft` refuse emails; `csvString` writes an email's subject only (never the body) and master-log.csv excludes `personal`; the hot-TEXT push no longer carries the sender name or 140 chars of body; emails are dated by arrival (`ts`); sender names `push` (not `unshift`) onto `prefs.tags`; `checkInboxTexts` awaits `window._pendReady` (`checkPending`'s promise — a freshly pushed card could be wiped, hot texts too); the Wizard pool excludes `personal` and carries mail fields; log gets a 📧 chip + address search + "— no job yet"; `#setMail` hidden on crew phones. `tests/_test641.js` — 33 green; 19/19 suites. **NEXT (spec ready, needs Eric's go):** v6.42 the cloud watcher — `fnsrc/mail-watch.mjs`, schedule */15, `ANTHROPIC_API_KEY` in Netlify env, reads mail-rules.json (fail closed if missing), writes `App Data/mail-judged.json` (cloud-owned, phone reads it and skips its own call), ONE generic push per run for new IMPORTANT only, quiet hours 8pm-7am Alaska. Byte-identical `MAIL_SYS` + `mailVerdictParse` in both files, guarded by a diff test.
- **A plain-English punch list for Eric is published** at https://claude.ai/code/artifact/03898d1e-0ee9-4f83-a48c-54f19cd82ce1 — 17 numbered items (1-7 read as bugs and are quick; 8-13 usability; 14-17 need his go) + 4 decisions. Built from a 5-reviewer UX pass (97 notes) and a 5-reader email touchpoint sweep (89 touchpoints); both raw outputs and a Word copy are in Dropbox `/Clore DayLog/Notes/`. Eric could not read the first, engineer-worded version — keep his docs in his words.
- Before that: **v6.40** (pushed 2026-09-06 night): the log's Type wheel has 📧 Emails (`e.mail` entries) and the search box reads sender/subject/body; Setup → 📧 Email senders (`setMail`, `renderMailSenders`/`mailListAdd`/`mailListDel`) edits `prefs.mailOk` / `prefs.mailNo` — one list or the other, never both. Gotcha for future edits: a node replace-script fed through the Bash tool STRIPPED backslashes inside regex literals (`w` → `w`); write regexes with the Edit tool. **In flight / next:** judgment-based email triage (sub invoices, client questions → Important / Maybe / Ignored piles, push for Important, spot-check of ignored) — three designs were drafted and judged 2026-09-06; spec to Eric, build on his go. Also an app-wide sweep of every place an email note sits/sorts/searches/tags/leaks, with recommendations.
- Before that: **v6.39** (pushed 2026-09-06 night): the bookkeeper plate wears the grinder's pipe frame (`steam/btn-bookkeeper-framed.webp`, composited from the two originals); the painted-on "Rininger" dropdown is gone from the clock panel (`steam/clockpanel-big-v2.jpg`, onerror falls back to the old file); `EST_ALIASES` (Trash ← Landfill + Garbage/Janitorial, Water Softener ← Softner) in `estScore` AND byte-identical in `c/index.html` budgetHtml — keep both tables the same; pend `al: 1` stamp = base measured against a page WITH books numbers (`estPageHasBooks()`), unstamped pends under aliased cats are lifted once at the next board open then stamped (three adversarial review rounds; the accepted conservative case: a pre-v6.39 Trash bill Logan had already booked stays on Upcoming until Eric retires it by hand). Known pre-existing hole for EVERY category, not fixed: a bill sent against a brand-new client page (`phases: []`) carries base 0 and can be swept by the first books run — only the aliased categories are protected by the stamp/lift. **Parked:** a Setup list to view/undo the email sender ✓ Always / 🚫 Never choices (`prefs.mailOk` / `prefs.mailNo` have no UI today); a per-item markup %, a refund/credit line, and a category-level note on the estimates board (specs offered 2026-09-06, no go yet).
- Before that: **v6.38** (pushed 2026-09-06 evening; v6.35→v6.38 shipped together after a 4-lens adversarial review of the whole diff). v6.37: 📊 Project Tracker button beside 🧾 Receipts to approve in both job folds (it opens the read-only QuickBooks money page, NOT the client page); client-page upcoming-costs note ends at "once it's invoiced." v6.38: the 📧 NEW EMAIL SENDERS card was inside the crew-only `#crewRoute` block (display:none on Eric's phone) — moved out, so new senders can finally be approved; `renderPortalList` never wipes a cached client list on a bad/empty/half-written `index.json` read (this also cleared the two `_test635` failures); `isOverheadEntry` returns false for HOURS (labor is non-billable only via the clock-out toggle); a receipt SENT before v6.35 under Tools/Fuel/Office stays SENT with its lights. **Email pipe confirmed working end to end** (Yahoo → clore.biz Gmail → Zap "Gmail to Dropbox Text File" → `/Clore DayLog/Inbox/Email -<subject>.txt` with FROM:/SUBJECT: header lines → app). NOTE: the Dropbox MCP `fetch` strips those header lines when reading, so files look body-only when they are not; verify raw bytes via a download link. **Parked for v6.39 (Eric's list + review carry-over):** bookkeeper plate with the grinder's pipe frame (composite ready), the painted-on "Rininger" dropdown in `steam/clockpanel-big.jpg` (patched image ready), and the 'Trash / Construction Debris' ↔ QB Landfill/Garbage-Janitorial alias — must go in BOTH matchers (app `estScore` and the client page's copy in `c/index.html` budgetHtml) with a one-time pend re-baseline, or old lifetime dollars falsely auto-clear a fresh bill.
- Before that: **v6.36** (2026-09-06) — seven categories from Logan's QuickBooks Products & Services joined `EST_DEFAULT_CATS` (48 → 55): Surveying, Water Softener, Bathrooms, Contingency, Trash / Construction Debris (QB's Landfill + Garbage/Janitorial in ONE bucket, Eric's call), Snow Plowing, Temporary Electric. They ride into `RCPT_CATS`/`FC_CATS`; an existing board gets them appended at the BOTTOM. `tests/_test636.js` (15 green). **v6.35 and v6.36 are committed but NOT pushed:** `tests/_test635.js` has 2 failures nobody has cleared — the ③ paid-file light, and the portal render reading `_portalIdx.clients` when `index.json` parses to null. Push on Eric's go. The phase-by-phase comparison PDF (his 55 vs Logan's 12 phases / 71 line items, plus 12 legacy un-numbered leftovers on Logan's list) was built from the All Dates Sales by Product/Service export of 2026-08-25.
- Before that: **v6.35** (2026-09-05, a long day of builds in Claude Code — see the commits). The receipt flow end to end: 🧾 tag → `🔮 → Upcoming` one-tap in the log, or the tracker's `🧾 Receipts to approve` window (tick + approve) → client page **ESTIMATED UPCOMING COSTS** (marked up, vendor-free, publishes whatever the lamps do) → auto-clear when QuickBooks catches up (trail in `estimates-<code>.json` `cleared[]`) → three lights in the receipts window. **v6.33 fixed the real bug:** receipt paths compared the portal name to the app job literally and never matched — always bridge with `portalAppJob(c)`. Overhead categories (`OVERHEAD_CATS`: Tools, Fuel, Office/Admin) are never offered to a client. 🔥 needs-you strip and to-dos are PARKED (body class `parked-630`). Function sources are in `fnsrc/`; bundle with `../boiler-room-tools/build-functions.mjs`. Tests run locally: `node ../boiler-room-tools/run-tests.mjs`.
- GitHub push → Netlify deploy is proven and is the only route. The old "live site may lag" note is retired.
- **Next up (spec agreed, parked):** homeowner push notifications — opt-in on client page, per-client subs walled off from `App Data/push-subs.json`, ONLY two triggers (journal release, budget change), generic message text, one ping per burst, delivery only 9am–5pm Alaska (off-hours actions queue to 9am). Full spec in the claude.ai Project doc `claude/next-up-cards.md`.
- Deeper history: the Project docs (`claude/build-log-*.md`, `claude/how-to-run-boiler-room.md`, `claude/deploy-pipeline.md`) in the "Dashboard App" project on claude.ai.

## Working style

Small, surgical edits to the one big file; comment new blocks with the version + Eric's own words for the why (the file reads as a decision log). Steampunk skin: brass/dark, plates and lamps — match it. When Eric reports a bug, reproduce it in a Playwright test first, fix, keep the test green.
